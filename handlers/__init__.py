"""Пакет хендлеров. Владельцы роутеров инициализируются ПЕРВЫМИ, в порядке `main.py`.

Зачем. Швы (`admin_*.py`, `reg_flow.py`, `reg_steps.py`) регистрируют хендлеры на общий
`router` владельца (`admin.py` / `registration.py`) и импортируются из ТЕЛА владельца в
фиксированной позиции — так воспроизводится порядок регистрации (first-match) исходного файла.
Без строки ниже этот порядок зависел от того, какой модуль пакета импортирован в процессе
первым: шов первым -> либо `ImportError ... partially initialized module 'handlers.admin'`
(у владельца именные импорты из шва: `admin_settings`, `admin_broadcasts`, `admin_moderation`,
`admin_roles`), либо хендлеры шва уезжают в хвост роутера (`admin_cities`, `admin_reg_config`,
`reg_flow`). `main.py` импортирует в каноническом порядке, так что в проде баг был латентным,
но любой скрипт/тест, начинавший со шва, получал другой бот.

`__init__` исполняется при импорте ЛЮБОГО `handlers.x` — поэтому владельцы гарантированно
загружаются до того, как запрошенный модуль начнёт исполняться (если запрошен шов — к этому
моменту он уже полностью загружен владельцем и просто возвращается из `sys.modules`).
Циклов это не создаёт: `handlers.states` / `admin_caps` / `reg_schema` / `game_*` не
импортируют ничего из пакета на уровне модуля, а внешние модули (`keyboards.builders`,
`services.scheduler`) импортируют `handlers.*` только внутри функций.
Порядок ниже = `main.py`; инвариант закреплён в `tests/test_seam_import_order_260819.py`.
"""
from handlers import registration, user_actions, admin, payment  # noqa: F401, E402
