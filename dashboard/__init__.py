"""Phase 15 (STAT-03/D-04): read-only витрина статистики регистраций.

Отдельный процесс/образ, sidecar в docker-compose стека мероприятия — НЕ импортирует
`aiogram`/`handlers`/`gspread`/`database.db`/`config` (бот). Ничего не пишет в БД бота
(`dashboard/db.py::read_conn` открывает файл строго `mode=ro` — физически невозможно
записать даже при ошибке в коде). Данные считаются на лету на каждый запрос, кэша нет
(D-16).
"""
