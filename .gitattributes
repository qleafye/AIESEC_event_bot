# Файл команды (D-11, план 23.1-02): паттерн плиты кладётся побайтно, без пересжатия и БЕЗ
# конвертации переводов строк — иначе `core.autocrlf=true` на Windows подменит LF на CRLF при
# следующем checkout, и `cmp` с исходником из mockups/assets/ перестанет совпадать.
miniapp/static/pattern/*.svg -text
