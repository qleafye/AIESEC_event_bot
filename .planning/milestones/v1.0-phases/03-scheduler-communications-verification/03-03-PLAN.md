---
phase: 03-scheduler-communications-verification
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - handlers/admin.py
  - tests/test_broadcast_429_phase3.py
autonomous: true
requirements: [COMM-04]
must_haves:
  truths:
    - "The broadcast send loop catches Telegram 429 (TelegramRetryAfter), waits retry_after+1, and retries the same user"
    - "A user that succeeds after the 429 wait is counted as delivered, not blocked"
    - "Only genuine failures (bot blocked, chat not found) increment the blocked counter"
  artifacts:
    - path: "handlers/admin.py"
      provides: "hardened process_broadcast loop + hardened _wait_and_send_album loop"
      contains: "TelegramRetryAfter"
    - path: "tests/test_broadcast_429_phase3.py"
      provides: "pure-helper tests for retry-delay math + blocked-vs-delivered classification"
  key_links:
    - from: "handlers/admin.py process_broadcast"
      to: "aiogram TelegramRetryAfter"
      via: "except TelegramRetryAfter: sleep(retry_after+1); retry once"
      pattern: "TelegramRetryAfter"
---

<objective>
Deliver COMM-04: make every broadcast send loop flood-safe. Replace the current bare-`except` loops in `process_broadcast` (admin.py:1020-1026) and the album path `_wait_and_send_album` (admin.py:971-979) — which have NO 429 handling — with the already-written `TelegramRetryAfter` pattern from Phase 2 `_welcome_flipped` (admin.py:1380-1393): on 429, wait `retry_after + 1` and retry the same user; a retry-success is NOT counted as blocked (D-07/D-08).

Purpose: At 1000+ users a broadcast hits Telegram's per-second flood limit; today those users are silently miscounted as "blocked" and never receive the message. This makes mass sends correct and self-throttling.

Output: hardened loops in `handlers/admin.py` plus a pure retry/classification helper unit-tested in `tests/test_broadcast_429_phase3.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduler-communications-verification/03-RESEARCH.md
@.planning/phases/03-scheduler-communications-verification/03-PATTERNS.md

<interfaces>
<!-- Existing code to modify / copy from -->
admin.py:35 — `from aiogram.exceptions import TelegramRetryAfter` (already imported; `.retry_after` attr verified aiogram 3.24).
admin.py:1380-1393 `_welcome_flipped` — canonical 429-safe send: try send; except TelegramRetryAfter as e: sleep(e.retry_after+1); retry once in inner try; except Exception: log. 0.05s pacing.
admin.py:991-1033 `process_broadcast` — current loop at :1020-1026 (send_copy, bare except → blocked+=1).
admin.py:947-989 `_wait_and_send_album` — current loop at :971-979 (send_media_group, bare except → blocked+=1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extract a pure retry-delay + outcome-classification helper with tests</name>
  <files>handlers/admin.py, tests/test_broadcast_429_phase3.py</files>
  <read_first>
    - handlers/admin.py:1380-1393 (_welcome_flipped — the exact 429 shape to factor)
    - handlers/admin.py:1015-1033 (process_broadcast loop to be hardened in Task 2)
    - tests/test_reminders_phase2.py (pure-sync test convention — no async/Telegram)
    - 03-RESEARCH.md:304-322 (process_broadcast-shaped 429 adaptation), 03-VALIDATION.md:51-55 (floodsafe test target)
  </read_first>
  <behavior>
    - _retry_delay(0) == 1 ; _retry_delay(5) == 6 ; _retry_delay(30) == 31  (retry_after + 1)
    - _classify_outcome(first_ok=True, retried_ok=None) == (1, 0)        # delivered, no retry needed
    - _classify_outcome(first_ok=False, retried_ok=True) == (1, 0)       # 429 then retry succeeded -> delivered, NOT blocked (D-08)
    - _classify_outcome(first_ok=False, retried_ok=False) == (0, 1)      # retry failed -> blocked
    - _classify_outcome(first_ok=False, retried_ok="error") == (0, 1)    # genuine non-429 failure -> blocked
  </behavior>
  <action>
    Add two PURE module-level helpers near the top of the broadcast section in `handlers/admin.py` (no async, no Telegram calls): `_retry_delay(retry_after: int) -> int` returning `retry_after + 1`; and `_classify_outcome(first_ok: bool, retried_ok) -> tuple[int, int]` returning `(delivered_inc, blocked_inc)` per the Behavior table — delivered when first_ok or retried_ok is True, blocked otherwise. These encode D-08 (a 429 that succeeds on retry is delivered, not blocked) in a synchronously testable form.
    Create `tests/test_broadcast_429_phase3.py` (mirror test_reminders_phase2.py — plain functions) importing both helpers from handlers.admin and asserting every Behavior case.
  </action>
  <verify>
    <automated>python -m pytest tests/test_broadcast_429_phase3.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_broadcast_429_phase3.py` passes (exit 0).
    - `_retry_delay` and `_classify_outcome` are importable from handlers.admin without constructing a Bot/Dispatcher.
    - `_classify_outcome(False, True) == (1, 0)` (retry-success not counted blocked).
  </acceptance_criteria>
  <done>Pure 429 retry-delay + outcome-classification helpers exist and are tested green.</done>
</task>

<task type="auto">
  <name>Task 2: harden process_broadcast and _wait_and_send_album loops</name>
  <files>handlers/admin.py</files>
  <read_first>
    - handlers/admin.py:1015-1033 (process_broadcast loop) and :971-987 (album loop + completion message)
    - handlers/admin.py:1380-1393 (_welcome_flipped reference pattern)
    - handlers/admin.py (Task 1 helpers _retry_delay / _classify_outcome)
  </read_first>
  <action>
    Rewrite the `process_broadcast` per-user loop (admin.py:1020-1026): for each `chat_id`, try `await message.send_copy(chat_id)`; on `except TelegramRetryAfter as e`: `await asyncio.sleep(_retry_delay(e.retry_after))` then retry `send_copy` in an inner try (set a `retried_ok` flag True on success, "error" on inner Exception); on outer `except Exception`: treat as genuine failure. Update `count`/`blocked` via `_classify_outcome(first_ok, retried_ok)` so a retry-success increments `count`, not `blocked`. Keep the `await asyncio.sleep(0.05)` pacing. Preserve the existing completion summary message (admin.py:1028-1032).
    Apply the identical hardening to the album loop in `_wait_and_send_album` (admin.py:971-979), which sends via `await bot.send_media_group(chat_id, media=media)` — same TelegramRetryAfter catch + single retry + `_classify_outcome` accounting. Preserve the album completion summary (admin.py:981-987).
    Do NOT change the menu, FSM states, or the send-target resolution — only the two send loops.
  </action>
  <verify>
    <automated>python -c "import handlers.admin" && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Both `process_broadcast` and `_wait_and_send_album` reference `TelegramRetryAfter` (`grep -c "TelegramRetryAfter" handlers/admin.py` >= 3, counting the existing _welcome_flipped).
    - Neither hardened loop has a bare `except Exception` that blindly does `blocked += 1` without first handling 429 (the 429 branch precedes the genuine-failure branch).
    - `handlers/admin.py` imports cleanly; `python -m pytest tests/ -q` green.
    - MANUAL (flag in SUMMARY): trigger a real rate limit on a large send, observe TelegramRetryAfter caught, retried, and not counted blocked (03-VALIDATION.md Manual-Only).
  </acceptance_criteria>
  <done>Both broadcast send loops survive Telegram 429: rate-limited users are retried after retry_after+1 and counted as delivered, not blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot → Telegram API (mass send) | rate-limit responses (429) cross back into the loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06 | Denial of Service | process_broadcast / album send loop under flood limit | mitigate | Catch TelegramRetryAfter, honor `e.retry_after` (server-told wait) + `_retry_delay` adds 1s; single bounded retry then count blocked — no infinite loop |
| T-3-16 | Repudiation / Integrity | miscounting delivery results | mitigate | `_classify_outcome` (unit-tested) ensures retry-success is reported delivered and only genuine failures are reported blocked, giving the admin an accurate report |
</threat_model>

<verification>
- `python -m pytest tests/ -q` green.
- Both send loops handle 429 before counting blocked.
- MANUAL: real 429 retried, not miscounted.
</verification>

<success_criteria>
COMM-04 met: broadcast and album send loops catch `TelegramRetryAfter`, wait `retry_after + 1`, retry the same user, and exclude rate-limited-then-delivered users from the blocked count.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduler-communications-verification/03-03-SUMMARY.md`.
</output>
