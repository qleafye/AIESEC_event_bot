---
phase: 03-scheduler-communications-verification
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - database/db.py
  - handlers/admin.py
  - handlers/states.py
  - services/scheduler.py
  - tests/test_filters_phase3.py
autonomous: true
requirements: [COMM-01, COMM-02, COMM-03]
must_haves:
  truths:
    - "Admin builds an AND-combined audience filter over city / university / status / source / registration-date"
    - "Admin sees the matching user count before sending"
    - "Only the matched users receive the broadcast (immediate or scheduled)"
  artifacts:
    - path: "database/db.py"
      provides: "count_and_list_filtered(filters) with column whitelist + parameterized WHERE; pure _build_filter_clause helper"
      contains: "_FILTER_COLUMNS"
    - path: "handlers/admin.py"
      provides: "step-by-step inline filter builder feeding count preview -> send-now or schedule"
      contains: "broadcast_filter"
    - path: "handlers/states.py"
      provides: "filter-builder FSM states"
    - path: "tests/test_filters_phase3.py"
      provides: "pure-helper tests for WHERE builder (whitelist, AND, date op, injection rejection)"
  key_links:
    - from: "handlers/admin.py filter builder"
      to: "database/db.py count_and_list_filtered"
      via: "materialize filtered telegram_id list -> len() preview"
      pattern: "count_and_list_filtered"
    - from: "handlers/admin.py filtered send"
      to: "_start_segment_broadcast (now) / create_scheduled_broadcast (later)"
      via: "hand the matched id list to the existing send path or persist filter_spec"
      pattern: "_start_segment_broadcast"
---

<objective>
Deliver COMM-01/02/03: a step-by-step inline AND filter builder in the broadcast menu. The admin picks a field (city / university / status / source / registration-date after|before), picks/enters a value, optionally adds more filters (combined with AND), sees the matching user COUNT before sending, then sends now (reusing the existing send path) or schedules for later (reusing Plan 01's scheduler).

Purpose: Targeted broadcasts ("город=Москва AND статус=approved AND зарегистрирован после 01.06.2026") with a count preview so the admin knows the audience size before committing (ROADMAP success-criterion #2).

Output: `count_and_list_filtered` + a pure `_build_filter_clause` in `database/db.py`, the filter-builder FSM + UI in `handlers/admin.py`, filter states in `handlers/states.py`, the scheduled-broadcast target-resolution edit in `services/scheduler.py`, and pure-helper tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduler-communications-verification/03-RESEARCH.md
@.planning/phases/03-scheduler-communications-verification/03-PATTERNS.md

<interfaces>
<!-- Existing contracts to reuse -->
users table columns (db.py:23-76): city, university, status, source, registration_date (stored "%Y-%m-%d %H:%M:%S" — registration.py:969 — so string `>= '2026-06-01'` is a valid date filter, COMM-02).
db.py:439-444 get_non_subscriber_ids — the parameterized list-returning query idiom to extend.
admin.py:870-888 _start_segment_broadcast(callback, state, user_ids, prompt) — builds target_type="list"/target_users and hands off to Broadcast.message send path; re-checks ADMIN_IDS at :872.
Plan 01: create_scheduled_broadcast(text, photo_file_id, filter_spec, scheduled_at, created_by), schedule_broadcast_job(id, when); send_scheduled_broadcast branches on filter_spec (currently NULL → all users; THIS plan makes it use the filtered list).
states.py:45-47 Broadcast FSM. 03-RESEARCH.md:259-279 Pattern 3 (dynamic AND query).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: count_and_list_filtered + pure _build_filter_clause with tests</name>
  <files>database/db.py, tests/test_filters_phase3.py</files>
  <read_first>
    - database/db.py:439-444 (get_non_subscriber_ids — list-returning query idiom)
    - 03-RESEARCH.md:259-279 (Pattern 3 whitelist + parameterized AND builder), :381-383 (Pitfall 5 injection)
    - 03-PATTERNS.md:215-229, :489-492 (parameterized SQL only)
    - tests/test_reminders_phase2.py (pure-sync test convention)
  </read_first>
  <behavior>
    - _build_filter_clause([]) == ("", [])
    - _build_filter_clause([{"field":"city","value":"Москва"}]) == (" WHERE city = ?", ["Москва"])
    - _build_filter_clause([{"field":"city","value":"Москва"},{"field":"status","value":"approved"}]) == (" WHERE city = ? AND status = ?", ["Москва","approved"])
    - _build_filter_clause([{"field":"registration_date","op":"after","value":"2026-06-01"}]) == (" WHERE registration_date >= ?", ["2026-06-01"])
    - _build_filter_clause([{"field":"registration_date","op":"before","value":"2026-06-01"}]) == (" WHERE registration_date < ?", ["2026-06-01"])
    - _build_filter_clause([{"field":"DROP TABLE users","value":"x"}]) == ("", [])   # non-whitelisted field ignored, never interpolated
  </behavior>
  <action>
    In `database/db.py` add module constant `_FILTER_COLUMNS = {"city", "university", "status", "source"}` (whitelist — guards injection, Pitfall 5). Add PURE function `_build_filter_clause(filters: list[dict]) -> tuple[str, list]`: iterate filters, for a whitelisted `field` append `f"{field} = ?"` + value param; for `field == "registration_date"` choose op `>=` when `op=="after"` else `<` and append `f"registration_date {op} ?"` + value; silently skip any non-whitelisted field (the only place a column name is composed — values are NEVER f-stringed). Return `(" WHERE " + " AND ".join(clauses)) if clauses else ""` and the params list. Column names come only from the hardcoded whitelist; values bind with `?`.
    Add `async def count_and_list_filtered(filters: list[dict]) -> list[int]`: build `(where, params)` via `_build_filter_clause`, run `SELECT telegram_id FROM users{where}` with `params` (db.py:439-444 idiom), return the id list. The COUNT preview (D-06) is `len(...)` of this list.
    Create `tests/test_filters_phase3.py` (pure-sync) importing `_build_filter_clause`, asserting all Behavior cases including the injection-rejection case.
  </action>
  <verify>
    <automated>python -m pytest tests/test_filters_phase3.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_filters_phase3.py` passes (exit 0), including the `DROP TABLE`-field rejection case returning `("", [])`.
    - `_build_filter_clause` never places a filter VALUE into the SQL string (only `?` placeholders); column names come only from `_FILTER_COLUMNS` or the literal `registration_date`.
    - `count_and_list_filtered` returns a `list[int]` and uses `?` binds.
  </acceptance_criteria>
  <done>Whitelisted, parameterized AND filter query + pure clause builder exist and are tested green (incl. injection rejection).</done>
</task>

<task type="auto">
  <name>Task 2: filter-builder FSM + inline UI (field -> value -> add more / count)</name>
  <files>handlers/admin.py, handlers/states.py, services/scheduler.py</files>
  <read_first>
    - handlers/admin.py:768-783 / :800-810 (broadcast menus — add the "🎯 По фильтру" entry to BOTH)
    - handlers/admin.py:870-914 (_start_segment_broadcast + segment handlers — per-callback ADMIN_IDS re-check at :872/:893/:906; the build-ids → Broadcast.message handoff)
    - handlers/states.py:45-47 (Broadcast FSM to extend)
    - services/scheduler.py send_scheduled_broadcast (Plan 01 left a target-resolution branch on filter_spec for this plan to fill)
    - 03-CONTEXT.md D-04/D-05/D-06; 03-RESEARCH.md:149-156 (filter-builder flow)
  </read_first>
  <action>
    Add a "🎯 По фильтру" button to the broadcast keyboard in BOTH `show_admin_broadcast` (admin.py:774-780) and `cmd_broadcast` (admin.py:802-808).
    Add FSM states to the `Broadcast` group in states.py: `filter_field = State()`, `filter_value = State()` (the builder terminates by handing off to the existing `Broadcast.message`, so extending Broadcast is lowest-friction — 03-PATTERNS.md:412-423).
    Add the entry callback `F.data == "broadcast_filter"` (state Broadcast.target_selection): re-check ADMIN_IDS (admin.py:872 precedent); initialize an empty filter list in FSM state; show an inline keyboard of filter fields — Город / ВУЗ / Статус / Источник / Дата регистрации — plus, once ≥1 filter exists, "➕ Ещё фильтр" and "📊 Показать N и отправить". Set state Broadcast.filter_field.
    Add field-pick callbacks (re-check ADMIN_IDS each): for city/university/source prompt a free-text value (state Broadcast.filter_value); for status offer inline value buttons (approved/pending/rejected); for registration_date first ask after|before then prompt a `ДД.ММ.ГГГГ` value (normalize to `YYYY-MM-DD` before storing, matching the ISO compare in COMM-02). Append `{field, op?, value}` to the FSM filter list. html.escape any echoed value (V5).
    Add the value-capture handler (Broadcast.filter_value): append the entry, then re-render the field menu with the running filter summary + the count-or-add options.
    Add the "📊 Показать N и отправить" callback: re-check ADMIN_IDS; call `count_and_list_filtered(filters)`; show the count N. Offer "📨 Отправить сейчас" → call `_start_segment_broadcast(callback, state, ids, prompt)` (existing send path), and "🕓 Запланировать" → ask datetime (reuse Plan 01 `_parse_schedule_dt`) then `create_scheduled_broadcast(text, photo, json.dumps(filters), _fmt_dt(when), admin_id)` + `schedule_broadcast_job(id, when)`.
    Extend Plan 01's `send_scheduled_broadcast` (services/scheduler.py) ONLY at its target-resolution branch: when `filter_spec` is non-empty, `json.loads` it and use `count_and_list_filtered(filters)` for the recipient list instead of all users. (This is the one cross-file edit — services/scheduler.py is owned upstream by Plans 01/02, both in earlier waves, so this additive branch fill is wave-safe.)
  </action>
  <verify>
    <automated>python -c "import handlers.admin, handlers.states; assert hasattr(handlers.states.Broadcast,'filter_field') and hasattr(handlers.states.Broadcast,'filter_value')" && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - "🎯 По фильтру" present in both broadcast keyboards (`grep -c "broadcast_filter" handlers/admin.py` >= 2).
    - Every new callback handler re-checks `config.ADMIN_IDS` (count of "Недостаточно прав" in admin.py increased).
    - The count step calls `count_and_list_filtered` and shows `len(...)` before any send; "Отправить сейчас" routes through `_start_segment_broadcast`.
    - `send_scheduled_broadcast` uses `count_and_list_filtered` when `filter_spec` is set.
    - `python -m pytest tests/ -q` green.
    - MANUAL (flag in SUMMARY): build a multi-field AND filter, confirm the preview count equals the number of recipients who actually receive it (03-VALIDATION.md Manual-Only).
  </acceptance_criteria>
  <done>Admin builds AND filters, sees an accurate count preview, and sends immediately or schedules to exactly the matched users.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin callback/free-text input → SQL WHERE | admin-typed filter values flow toward a query |
| admin callback → bot | filter-builder callbacks bypass message-level is_admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-07 | Tampering (SQL injection) | _build_filter_clause / count_and_list_filtered | mitigate | Hardcoded `_FILTER_COLUMNS` whitelist for column names; all values bound with `?`; non-whitelisted fields dropped (unit-tested) — Pitfall 5 |
| T-3-08 | Elevation of Privilege | filter-builder callbacks (admin.py) | mitigate | Per-callback `config.ADMIN_IDS` re-check on every new handler (precedent admin.py:872) |
| T-3-12 | Information Disclosure (XSS-ish echo) | echoing admin/sheet filter values back in HTML | mitigate | `html.escape` any value rendered into the running-filter summary (V5) |
</threat_model>

<verification>
- `python -m pytest tests/ -q` green incl. injection-rejection test.
- All new callbacks re-check ADMIN_IDS; values parameterized.
- MANUAL: preview count == actual recipients.
</verification>

<success_criteria>
COMM-01/02/03 met: admin assembles AND-combined filters over city/university/status/source/registration-date, sees the matching count before send, and only matched users receive the message (now or scheduled).
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduler-communications-verification/03-04-SUMMARY.md`.
</output>
