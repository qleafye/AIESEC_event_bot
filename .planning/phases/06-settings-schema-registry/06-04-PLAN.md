---
phase: 06-settings-schema-registry
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - settings_schema.py
  - handlers/registration.py
  - handlers/admin.py
  - tests/test_settings_groups_c0x.py
autonomous: true
requirements: [REG-01, REG-02]
must_haves:
  truths:
    - "Every reg_q_* toggle is described in SETTINGS_SCHEMA with type toggle and the correct on/off default"
    - "Every feature-switch key (party_enabled, party_fork_question, reg_bonus_enabled, payment_enabled, consent_enabled, payment_reminders_enabled, edu_conditional, reg_show_progress, reg_university_mode, registration_mode, pending_notify_mode, full_approval, short_approval, party_approval) is described in SETTINGS_SCHEMA with type enum and its exact `or`-idiom default — ready for its consumers to be wired in 06-05/06-06"
    - "REG_DEFAULTS is no longer a hand-maintained literal dict — it is derived from the registry, so its name stays importable and its contents are byte-identical"
    - "The three duplicated reg_q on/off read-sites (admin.py:501, _is_question_on at :2097, :2248) resolve on/off through the registry default, byte-for-byte with the old REG_DEFAULTS.get(key,'on')=='on' idiom"
    - "A full-registry coverage test iterates EVERY SETTINGS_SCHEMA entry (including the 14 enum feature-switch entries): each key in exactly one group, valid type, default parses without error"
    - "Existing REG_DEFAULTS-asserting tests (test_registration_phase4/5) stay green; no live toggle setting is reset"
  artifacts:
    - path: "settings_schema.py"
      provides: "all reg_q toggle entries + all feature-switch enum entries with correct defaults"
      contains: "\"reg_q_age\""
    - path: "handlers/registration.py"
      provides: "REG_DEFAULTS derived from SETTINGS_SCHEMA (computed re-export, name preserved)"
    - path: "handlers/admin.py"
      provides: "the three reg_q toggle read-sites resolving default via the registry"
    - path: "tests/test_settings_groups_c0x.py"
      provides: "byte-for-byte toggle parse-equivalence + full-registry coverage (all types incl. enum)"
  key_links:
    - from: "handlers/registration.py::REG_DEFAULTS"
      to: "settings_schema.SETTINGS_SCHEMA"
      via: "computed from toggle entries"
      pattern: "SETTINGS_SCHEMA"
    - from: "handlers/admin.py::_is_question_on"
      to: "settings_schema (default lookup)"
      via: "toggle on/off resolution"
      pattern: "_parse_setting|get_setting_typed|SETTINGS_SCHEMA"
---

<objective>
Register the toggle/enum layer in the registry (D-06/D-12) and wire the reg_q question read-sites: every `reg_q_*` default becomes a `type: toggle` entry, every feature-switch key becomes a `type: enum` entry carrying its exact `or`-idiom default, `REG_DEFAULTS` becomes a computed re-export, and the three duplicated reg_q on/off read-sites resolve their default through the registry — byte-for-byte (SC#2).

Purpose: This is the registry-first half of the toggle wave. It puts ALL remaining keys in the one file (North Star, D-09) so the feature-switch CONSUMER read-sites can be migrated against real entries in the next wave (06-05 admin, 06-06 registration/payment/scheduler). The locked decision (06-DISCUSSION-LOG.md:48) requires the toggle wave to absorb REG_DEFAULTS AND the hardcoded button reads — the entries land here, the button/gate wiring lands in 06-05/06-06.
Output: toggle + enum registry entries, a registry-derived `REG_DEFAULTS`, migrated reg_q read-sites, byte-for-byte toggle parse-equivalence, and the full-registry coverage test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** DXP manager, **I want to** every registration-question toggle and feature switch to take its default from the one registry file, **so that** turning a question or feature on/off by default is a one-line registry edit — with the exact same behavior the ~590 live delegates already experience.

<context>
@.planning/phases/06-settings-schema-registry/06-CONTEXT.md
@.planning/phases/06-settings-schema-registry/06-PATTERNS.md
@.planning/phases/06-settings-schema-registry/06-02-SUMMARY.md

<interfaces>
Toggle keys + defaults (type TOGGLE, bool via the reg_q idiom — from handlers/registration.py:197-241 REG_DEFAULTS, copy EXACTLY, 44 keys):
  reg_q_age=on, reg_q_vk=on, reg_q_email=off, reg_q_phone=off, reg_q_city=off, reg_q_source=on,
  reg_q_lc=off, reg_q_position=off, reg_q_education=on, reg_q_university=on, reg_q_course=on,
  reg_q_study_field=on, reg_q_specialty=off, reg_q_work=on, reg_q_work_sphere=on, reg_q_skills=on,
  reg_q_expectations=on, reg_q_attendance=off, reg_q_informal_day=off, reg_q_comments=off,
  reg_q_department=off, reg_q_aiesec_role=off, reg_q_certificate=off, reg_q_alumni_status=off,
  reg_q_english=off, reg_q_allergies=off, reg_q_food=off, reg_q_arrival=off, reg_q_housing=off,
  reg_q_bed_sharing=off, reg_q_bed_partner=off, reg_q_transport=off, reg_q_payment_date=off,
  reg_q_cc_shop=off, reg_q_exp_organizers=off, reg_q_exp_content=off, reg_q_volunteer=off,
  reg_q_arrival_date=off, reg_q_birth_date=off, reg_q_goal=off, reg_q_formats=off,
  reg_q_ambassador=off, reg_q_resume=off

Feature-switch keys (type ENUM — string via the `or "<default>"` idiom; consumers wired in 06-05/06-06).
Defaults verified byte-for-byte from the live call sites — DO NOT guess:
  party_enabled -> default "off"          (admin.py:556, registration.py:885/1522/1831, admin.py:2229)
  party_fork_question -> default "off"     (registration.py:882)
  reg_bonus_enabled -> default "off"       (admin.py:525; registration.py:1445 reads WITHOUT `or` as `== "on"` — "off" default reproduces it)
  payment_enabled -> default "off"         (admin.py:535, handlers/payment.py:88)
  consent_enabled -> default "off"         (admin.py:536)
  payment_reminders_enabled -> default "on" (admin.py:539, services/scheduler.py:277/325)
  edu_conditional -> default "on"          (registration.py:438, admin.py:546)
  reg_show_progress -> default "off"       (registration.py:1520, admin.py:549)
  reg_university_mode -> default "text"    options ["text","list"]  (registration.py:582, admin.py:543)
  registration_mode -> default "short"     options ["short","full"] (admin.py:522, registration.py:2219)
  pending_notify_mode -> default "batched" options ["instant","batched"] (admin.py:530/692, registration.py:2379)
  full_approval -> default "manual"        options ["manual","auto"] (admin.py:528)
  short_approval -> default "auto"         options ["manual","auto"] (admin.py:529, registration.py:2377)
  party_approval -> default "manual"       options ["manual","auto"] (admin.py:562; registration.py:2379 reads raw, _decide_status applies `or "manual"`)
  RATIONALE: enum type (not toggle) because get_setting_typed for these must return the resolved STRING so the
  existing `!= "on"` / `== "on"` / string comparisons at the call sites stay byte-identical (D-15). The enum branch
  `raw if raw else default` (06-01) reproduces `(get_setting(k) or "<default>")` exactly, incl. empty-string.

REG_DEFAULTS name is consumed at: handlers/admin.py:59 (import), :501, :2097 (_is_question_on), :2248,
  and iterated at :2335 `for key in REG_DEFAULTS` and :2411 (preset loop). It MUST remain a dict with the
  SAME keys/values -> derive it, do not delete the name.

Canonical reg_q toggle idiom (byte-for-byte target, admin.py:501/2097/2248):
  (v == "on") if v is not None else (REG_DEFAULTS.get(sk, "on") == "on")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing toggle parse-equivalence + REG_DEFAULTS-parity + full-registry coverage tests</name>
  <files>tests/test_settings_groups_c0x.py</files>
  <read_first>
    - tests/test_settings_groups_c0x.py (full — extend)
    - handlers/registration.py:197-241 (REG_DEFAULTS — the parity oracle)
    - handlers/admin.py:2095-2097 (_is_question_on — the toggle read-site oracle)
    - settings_schema.py (_parse_setting toggle + enum branches from 06-01)
  </read_first>
  <behavior>
    - test_toggle_parse_equivalence_all_keys: for EVERY reg_q toggle key, over raw in {None, "on", "off", "", "garbage"}, _parse_setting(key, raw) equals `(raw == "on") if raw is not None else (default == "on")` computed from the frozen 44-key oracle.
    - test_reg_defaults_parity: the derived REG_DEFAULTS equals the frozen 44-key map byte-for-byte (same keys, same on/off values).
    - test_toggle_keys_coverage: every REG_DEFAULTS key exists in SETTINGS_SCHEMA with type toggle; every SETTINGS_SCHEMA type-toggle key is in REG_DEFAULTS.
    - test_enum_feature_switch_defaults: for every feature-switch key in the interfaces table, the registry entry has type "enum" and the exact listed default; and _parse_setting(key, "") == default AND _parse_setting(key, None) == default AND _parse_setting(key, "on") == "on" (proves the `or`-idiom byte-for-byte, incl. empty-string).
    - test_full_registry_coverage (WARNING-2 fix, D-17): iterate ALL SETTINGS_SCHEMA.items() UNCONDITIONALLY — assert each entry's group is a non-empty string, type is in {toggle,int,list,date,text,enum,photo,file}, no key appears under two groups, and _parse_setting(key, entry["default"]) does not raise. This is the single terminal coverage gate covering text/int/list/date/enum/toggle/photo/file alike.
  </behavior>
  <action>Extend tests/test_settings_groups_c0x.py. Freeze the 44-key REG_DEFAULTS oracle map inline (copied from registration.py:197-241) so the test is independent of the migrated derivation. Add the enum-default matrix from the interfaces table as inline expected data. The full-registry coverage test must iterate the WHOLE dict with no group/type filter — it is the catch-all that WARNING 2 requires. These tests FAIL now because the toggle+enum keys are not yet in SETTINGS_SCHEMA.</action>
  <acceptance_criteria>
    - test_toggle_parse_equivalence_all_keys, test_reg_defaults_parity, test_toggle_keys_coverage, test_enum_feature_switch_defaults, test_full_registry_coverage all exist.
    - `python -m pytest tests/test_settings_groups_c0x.py -k "toggle or reg_defaults_parity or enum_feature or full_registry_coverage" -q` FAILS (keys absent) — proves RED.
    - The frozen 44-key oracle map matches registration.py:197-241 exactly.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py -k "toggle or reg_defaults_parity or enum_feature or full_registry_coverage" -q</automated>
  </verify>
  <done>Toggle parity, enum-default, and full-registry coverage tests fail because toggle+enum keys are not yet registered.</done>
</task>

<task type="auto">
  <name>Task 2: Add toggle+enum entries to the registry, derive REG_DEFAULTS, migrate the three reg_q read-sites</name>
  <files>settings_schema.py, handlers/registration.py, handlers/admin.py</files>
  <read_first>
    - settings_schema.py (extend SETTINGS_SCHEMA; reuse _parse_setting toggle/enum branches — add NO new parse code)
    - handlers/registration.py:197-241 (REG_DEFAULTS to derive), :243-287 (REG_LABELS for toggle labels)
    - handlers/admin.py:59 (REG_DEFAULTS import), :501, :2095-2097 (_is_question_on), :2247-2248, :2330-2411 (preset loop iterating REG_DEFAULTS)
  </read_first>
  <action>
  (1) settings_schema.py: add a `type: toggle` entry for every reg_q_* key with its exact on/off `default` from the interfaces list, `group` "reg_questions", and a `label` reused from REG_LABELS (registration.py:243+) where present. Add a `type: enum` entry for every feature-switch key in the interfaces table with its EXACT verified default and `options` list, `group` "toggles". Reuse the existing toggle/enum branches of `_parse_setting` — write NO new parse code.
  (2) handlers/registration.py: replace the literal REG_DEFAULTS dict with a computed one derived from the registry — `REG_DEFAULTS = {k: v["default"] for k, v in SETTINGS_SCHEMA.items() if v["type"] == "toggle"}` (import SETTINGS_SCHEMA at module top; settings_schema imports database.db only, so registration.py importing settings_schema is safe — no cycle). Same key set and values (test_reg_defaults_parity enforces). Add a `# REG-01/D-06:` comment on why the NAME is retained (admin.py imports and iterates it).
  (3) handlers/admin.py: at the three reg_q read-sites (:501, _is_question_on at :2097, :2248) replace the `REG_DEFAULTS.get(sk, "on") == "on"` fallback with the registry-driven equivalent — prefer routing through `get_setting_typed(sk)` (returns the parsed bool directly) where the surrounding code can consume a bool, else `SETTINGS_SCHEMA[sk]["default"]`-based resolution. Behavior MUST stay byte-identical (the frozen oracle guards this). Leave the preset loop `for key in REG_DEFAULTS` (:2335/:2411) working against the derived REG_DEFAULTS. Tag with `# REG-02:` comments.
  DO NOT touch the feature-switch consumer read-sites in this plan — those are wired in 06-05 (admin) and 06-06 (registration/payment/scheduler). This plan only registers their entries (interface-first ordering).
  </action>
  <acceptance_criteria>
    - settings_schema.py contains `"reg_q_age"` with `"type": "toggle"`/`"default": "on"`, `"reg_q_email"` with `"default": "off"`, `"party_enabled"` with `"type": "enum"`/`"default": "off"`, `"edu_conditional"` with `"default": "on"`, and `"registration_mode"` with `"default": "short"`.
    - handlers/registration.py: `grep -n "REG_DEFAULTS = {" handlers/registration.py` shows a derivation referencing SETTINGS_SCHEMA (comprehension), NOT a 44-line literal.
    - handlers/admin.py: the three reg_q read-sites no longer call `REG_DEFAULTS.get(...)` for the default; the preset loop still iterates REG_DEFAULTS.
    - `python -m pytest tests/test_settings_groups_c0x.py -q` exits 0 (toggle parity + enum defaults + full coverage + all prior tests green).
    - `python -m pytest tests/test_registration_phase4.py tests/test_registration_phase5.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py tests/test_registration_phase4.py tests/test_registration_phase5.py -q</automated>
  </verify>
  <done>All reg_q toggle + feature-switch enum keys are registry-described; REG_DEFAULTS is derived (same contents, name preserved); the three reg_q read-sites resolve defaults via the registry byte-for-byte; feature-switch consumer wiring is queued for 06-05/06-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot_settings toggle/enum row -> resolution | The most duplicated parse in the codebase; a default flip silently changes which registration questions or features are active for every user. |
| REG_DEFAULTS consumers (preset bulk-write, admin.py:2330-2411) | Iterating the derived dict must yield the identical key set or presets would write a different question set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Data integrity | A single toggle default flip during migration silently changes the live registration form for ~590 users | mitigate | test_toggle_parse_equivalence_all_keys + test_reg_defaults_parity compare against a frozen 44-key oracle over the full raw matrix; any flip fails the build. |
| T-06-13 | Correctness | Deriving REG_DEFAULTS changes its key set, breaking the preset bulk-write loop | mitigate | Derivation includes exactly the type-toggle keys = the original 44; test_toggle_keys_coverage asserts set equality both directions; registration_phase4/5 exercise the preset path. |
| T-06-14 | Availability | registration.py importing settings_schema creating a cycle | mitigate | settings_schema imports only database.db (one-directional); import smoke in verification; lazy import fallback available per CONVENTIONS.md. |
| T-06-18 | Data integrity | An enum default typed wrong here silently changes feature behavior once 06-05/06-06 wire the consumer | mitigate | test_enum_feature_switch_defaults pins each of the 14 defaults + the empty-string/None/"on" outputs; defaults were read byte-for-byte from the live call sites (interfaces table), not guessed. |
</threat_model>

<verification>
- `python -m pytest tests/test_settings_groups_c0x.py tests/test_registration_phase4.py tests/test_registration_phase5.py -q` green.
- `python -c "import settings_schema, handlers.registration, handlers.admin, main"` exits 0 (no cycle).
- `grep -c` (excluding comments) confirms REG_DEFAULTS is no longer a literal 44-entry dict.
</verification>

<success_criteria>
- REG-01: ALL keys (reg_q toggles + feature-switch enums) described in the one registry file (North Star holds for on/off + enums too).
- REG-02 (partial): reg_q on/off resolution flows through the registry, byte-for-byte; feature-switch entries ready for consumer wiring in 06-05/06-06.
- REG_DEFAULTS absorbed (D-06): derived, not duplicated; name retained for its remaining consumers.
- Full-registry coverage gate (D-17) green across every type incl. enum.
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-schema-registry/06-04-SUMMARY.md` documenting the toggle-vs-enum typing decision (reg_q = toggle/bool, feature switches = enum/string to preserve the `or`-idiom byte-for-byte), the REG_DEFAULTS derivation, how each reg_q read-site was rewired, and the enum-default table used so 06-05/06-06 wire against the same values.
</output>
