---
phase: 06-settings-schema-registry
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - settings_schema.py
  - handlers/registration.py
  - handlers/admin.py
  - tests/test_settings_groups_c0x.py
autonomous: true
requirements: [REG-01, REG-02]
must_haves:
  truths:
    - "Every reg_q_* toggle plus party_enabled / edu_conditional / reg_university_mode (and the other toggle keys) is described in SETTINGS_SCHEMA with type toggle and the correct on/off default"
    - "REG_DEFAULTS is no longer a hand-maintained literal dict — it is derived from the registry, so its name stays importable and its contents are byte-identical"
    - "The three duplicated toggle read-sites (admin.py:501, _is_question_on at :2097, :2248) resolve on/off through the registry default, byte-for-byte identical to the old REG_DEFAULTS.get(key,'on')=='on' idiom"
    - "Existing REG_DEFAULTS-asserting tests (test_registration_phase4/5) stay green; no live toggle setting is reset"
  artifacts:
    - path: "settings_schema.py"
      provides: "all toggle keys with type toggle + on/off default"
      contains: "\"reg_q_age\""
    - path: "handlers/registration.py"
      provides: "REG_DEFAULTS derived from SETTINGS_SCHEMA (computed re-export, name preserved)"
    - path: "handlers/admin.py"
      provides: "toggle read-sites resolving default via the registry"
    - path: "tests/test_settings_groups_c0x.py"
      provides: "byte-for-byte toggle parse-equivalence over every toggle key"
  key_links:
    - from: "handlers/registration.py::REG_DEFAULTS"
      to: "settings_schema.SETTINGS_SCHEMA"
      via: "computed from toggle entries"
      pattern: "SETTINGS_SCHEMA"
    - from: "handlers/admin.py::_is_question_on"
      to: "settings_schema (default lookup)"
      via: "toggle on/off resolution"
      pattern: "_parse_setting|get_setting_typed|SETTINGS_SCHEMA"
---

<objective>
Migrate the toggle keys into the registry and absorb `REG_DEFAULTS` (D-06/D-12): every `reg_q_*` default plus `party_enabled`/`edu_conditional`/`reg_university_mode` (and the remaining toggle keys) lives as a `type: toggle` registry entry, `REG_DEFAULTS` becomes a computed re-export from the registry, and the three duplicated on/off read-sites resolve their default through the registry — byte-for-byte identical (SC#2).

Purpose: Toggles are the last and highest-duplication slice; completing them makes the registry the single source for on/off/default across the whole settings surface (North Star), with zero behavior change for the ~590 live users.
Output: toggle registry entries, a registry-derived `REG_DEFAULTS`, migrated read-sites, and byte-for-byte toggle parse-equivalence tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** DXP manager, **I want to** every registration-question toggle and feature switch to take its default from the one registry file, **so that** turning a question on/off by default is a one-line registry edit — with the exact same on/off behavior the ~590 live delegates already experience.

<context>
@.planning/phases/06-settings-schema-registry/06-CONTEXT.md
@.planning/phases/06-settings-schema-registry/06-PATTERNS.md
@.planning/phases/06-settings-schema-registry/06-02-SUMMARY.md

<interfaces>
Toggle keys + defaults (from handlers/registration.py:197-241 REG_DEFAULTS — copy EXACTLY, 44 keys):
  reg_q_age=on, reg_q_vk=on, reg_q_email=off, reg_q_phone=off, reg_q_city=off, reg_q_source=on,
  reg_q_lc=off, reg_q_position=off, reg_q_education=on, reg_q_university=on, reg_q_course=on,
  reg_q_study_field=on, reg_q_specialty=off, reg_q_work=on, reg_q_work_sphere=on, reg_q_skills=on,
  reg_q_expectations=on, reg_q_attendance=off, reg_q_informal_day=off, reg_q_comments=off,
  reg_q_department=off, reg_q_aiesec_role=off, reg_q_certificate=off, reg_q_alumni_status=off,
  reg_q_english=off, reg_q_allergies=off, reg_q_food=off, reg_q_arrival=off, reg_q_housing=off,
  reg_q_bed_sharing=off, reg_q_bed_partner=off, reg_q_transport=off, reg_q_payment_date=off,
  reg_q_cc_shop=off, reg_q_exp_organizers=off, reg_q_exp_content=off, reg_q_volunteer=off,
  reg_q_arrival_date=off, reg_q_birth_date=off, reg_q_goal=off, reg_q_formats=off,
  reg_q_ambassador=off, reg_q_resume=off

Non-reg_q toggle/feature keys (from build_settings_keyboard, admin.py:521-590 — type toggle or enum-ish;
  keep their existing `or "<default>"` default in the entry):
  party_enabled(off), party_fork_question(off), reg_bonus_enabled(off), payment_enabled(off),
  consent_enabled(off), payment_reminders_enabled(on), edu_conditional(on), reg_show_progress(off),
  reg_university_mode(default "text" — this one is an enum text/list, NOT on/off — model as type enum with options ["text","list"], default "text"),
  registration_mode(enum short/full, default "short"), pending_notify_mode(enum instant/batched, default "batched"),
  full_approval/short_approval/party_approval(enum manual/auto). MODEL these enum-defaulted switches as type enum
  where they are not on/off; only true on/off keys get type toggle. (Discretion per D-04 taxonomy.)

REG_DEFAULTS name is consumed at: handlers/admin.py:59 (import), :501, :2097 (_is_question_on), :2248,
  and iterated at :2335 `for key in REG_DEFAULTS` and :2411 (preset loop). It MUST remain a dict with the
  SAME keys/values -> derive it, do not delete the name.

Canonical toggle idiom (byte-for-byte target, admin.py:501/2097/2248):
  (v == "on") if v is not None else (REG_DEFAULTS.get(sk, "on") == "on")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing byte-for-byte toggle parse-equivalence + REG_DEFAULTS-parity tests</name>
  <files>tests/test_settings_groups_c0x.py</files>
  <read_first>
    - tests/test_settings_groups_c0x.py (full — extend)
    - handlers/registration.py:197-241 (REG_DEFAULTS — the parity oracle)
    - handlers/admin.py:2095-2097 (_is_question_on — the toggle read-site oracle)
    - settings_schema.py (_parse_setting toggle branch from 06-01)
  </read_first>
  <behavior>
    - test_toggle_parse_equivalence_all_keys: for EVERY toggle key, over raw in {None, "on", "off", "", "garbage"}, _parse_setting(key, raw) equals the old idiom result: for a boolean parse, `(raw == "on") if raw is not None else (default == "on")`. The test computes the oracle from the ORIGINAL REG_DEFAULTS values (hard-code the 44-key map in the test as the frozen oracle, or import the pre-migration values) so a default flip is caught.
    - test_reg_defaults_parity: the derived REG_DEFAULTS (after migration) equals the frozen 44-key map byte-for-byte (same keys, same on/off values).
    - test_toggle_keys_coverage: every REG_DEFAULTS key exists in SETTINGS_SCHEMA with type toggle; every SETTINGS_SCHEMA type-toggle key is in REG_DEFAULTS.
  </behavior>
  <action>Extend tests/test_settings_groups_c0x.py. Freeze the 44-key REG_DEFAULTS oracle map inline in the test (copied from registration.py:197-241) so the test is independent of the migrated derivation. Assert _parse_setting toggle output matches the boolean idiom for the full raw matrix. These tests FAIL now because the toggle keys are not yet in SETTINGS_SCHEMA (06-01/06-02 added text/int/list/date/enum + only event photo/file, not reg_q_* toggles).</action>
  <acceptance_criteria>
    - test_toggle_parse_equivalence_all_keys, test_reg_defaults_parity, test_toggle_keys_coverage exist.
    - `python -m pytest tests/test_settings_groups_c0x.py -k "toggle or reg_defaults_parity" -q` FAILS (toggle keys absent from registry) — proves RED.
    - The frozen 44-key oracle map in the test matches registration.py:197-241 exactly.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py -k "toggle or reg_defaults_parity" -q</automated>
  </verify>
  <done>Toggle parse-equivalence + REG_DEFAULTS-parity tests fail because toggle keys are not yet registered.</done>
</task>

<task type="auto">
  <name>Task 2: Add toggle entries to the registry, derive REG_DEFAULTS, migrate the three read-sites</name>
  <files>settings_schema.py, handlers/registration.py, handlers/admin.py</files>
  <read_first>
    - settings_schema.py (extend SETTINGS_SCHEMA; reuse _parse_setting toggle/enum branches — add NO new parse code)
    - handlers/registration.py:197-241 (REG_DEFAULTS to derive) and its consumers
    - handlers/admin.py:59 (REG_DEFAULTS import), :501, :2095-2097 (_is_question_on), :2247-2248, :2330-2411 (preset loop iterating REG_DEFAULTS)
  </read_first>
  <action>
  (1) settings_schema.py: add a `type: toggle` entry for every reg_q_* key with its exact on/off `default` from the interfaces list, plus `party_enabled`/`party_fork_question`/`reg_bonus_enabled`/`payment_enabled`/`consent_enabled`/`payment_reminders_enabled`/`edu_conditional`/`reg_show_progress` as type toggle with their `or "<default>"` defaults; model reg_university_mode/registration_mode/pending_notify_mode/full_approval/short_approval/party_approval as type enum with their string defaults (not on/off). Assign a sensible `group` (e.g. "reg_questions" for reg_q_*, "toggles" for feature switches) and a `label` (reuse REG_LABELS at registration.py:243+ for reg_q_* where a screen label exists; label is not rendered for these in this wave but keep it populated for the one-file goal, D-09). Reuse the existing toggle branch of `_parse_setting`.
  (2) handlers/registration.py: replace the literal REG_DEFAULTS dict with a computed one derived from the registry — `REG_DEFAULTS = {k: v["default"] for k, v in SETTINGS_SCHEMA.items() if v["type"] == "toggle"}` (import SETTINGS_SCHEMA at module top; watch import direction — settings_schema imports database.db only, registration.py importing settings_schema is safe, no cycle). Keep the exact same key set and values (test_reg_defaults_parity enforces this). Add a `# REG-01/D-06:` comment noting the absorption and why the NAME is retained (admin.py imports and iterates it).
  (3) handlers/admin.py: at the three read-sites (:501, _is_question_on at :2097, :2248) replace the `REG_DEFAULTS.get(sk, "on") == "on"` fallback with the registry-driven equivalent — prefer routing through `get_setting_typed(sk)` (returns the parsed boolean directly) where the surrounding code can consume a bool, else keep `SETTINGS_SCHEMA[sk]["default"]`-based resolution. Behavior MUST stay byte-identical (the frozen oracle test guards this). Leave the preset loop `for key in REG_DEFAULTS` (:2335/:2411) working against the derived REG_DEFAULTS (same keys). Tag with `# REG-02:` comments.
  </action>
  <acceptance_criteria>
    - settings_schema.py contains `"reg_q_age"` with `"type": "toggle"` and `"default": "on"`, and `"reg_q_email"` with `"default": "off"`.
    - handlers/registration.py: `grep -n "REG_DEFAULTS = {" handlers/registration.py` shows a derivation referencing SETTINGS_SCHEMA (comprehension), NOT a 44-line literal.
    - handlers/admin.py: the three read-sites no longer call `REG_DEFAULTS.get(...)` for the default (grep shows registry-based resolution); the preset loop still iterates REG_DEFAULTS.
    - `python -m pytest tests/test_settings_groups_c0x.py -q` exits 0 (toggle parity + all prior tests green).
    - `python -m pytest tests/test_registration_phase4.py tests/test_registration_phase5.py -q` exits 0 (REG_DEFAULTS-asserting tests unbroken).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py tests/test_registration_phase4.py tests/test_registration_phase5.py -q</automated>
  </verify>
  <done>All toggle keys are registry-described; REG_DEFAULTS is derived (same contents, name preserved); the three read-sites resolve defaults via the registry byte-for-byte; existing registration tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot_settings toggle row -> on/off resolution | The most duplicated parse in the codebase (3 sites); a default flip silently changes which registration questions appear for every user. |
| REG_DEFAULTS consumers (preset bulk-write, admin.py:2330-2411) | Iterating the derived dict must yield the identical key set or presets would write a different question set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Data integrity | A single toggle default flip during migration silently changes the live registration form for ~590 users | mitigate | test_toggle_parse_equivalence_all_keys + test_reg_defaults_parity compare against a frozen 44-key oracle over the full raw matrix; any flip fails the build. |
| T-06-13 | Correctness | Deriving REG_DEFAULTS changes its key set, breaking the preset bulk-write loop | mitigate | Derivation includes exactly the type-toggle keys = the original 44; test_toggle_keys_coverage asserts set equality both directions; registration_phase4/5 tests exercise the preset path. |
| T-06-14 | Availability | registration.py importing settings_schema creating a cycle (registration is imported widely) | mitigate | settings_schema imports only database.db (one-directional); import smoke in verification; lazy import fallback available per CONVENTIONS.md. |
| T-06-15 | Tampering | garbage toggle raw ("yes","1") | accept | Idiom treats anything != "on" (when present) as off; identical to current behavior — no new surface, covered by the "garbage" matrix case. |
</threat_model>

<verification>
- `python -m pytest tests/test_settings_groups_c0x.py tests/test_registration_phase4.py tests/test_registration_phase5.py -q` green.
- `python -c "import settings_schema, handlers.registration, handlers.admin, main"` exits 0 (no cycle).
- `grep -c` (excluding comments) confirms REG_DEFAULTS is no longer a literal 44-entry dict.
</verification>

<success_criteria>
- REG-01: all toggle keys described in the one registry file (North Star holds for on/off too).
- REG-02: on/off/default resolution flows through the registry, byte-for-byte identical.
- REG_DEFAULTS absorbed (D-06): derived, not duplicated; name retained for its remaining consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-schema-registry/06-04-SUMMARY.md` documenting the toggle/enum typing decisions (which keys are toggle vs enum), the REG_DEFAULTS derivation, how each of the three read-sites was rewired, and confirmation of parity test results.
</output>
