---
phase: 06-settings-schema-registry
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - settings_schema.py
  - handlers/admin.py
  - tests/test_settings_groups_c0x.py
autonomous: true
requirements: [REG-01, REG-03]
must_haves:
  truths:
    - "The reg, pay, party, and consent settings groups render their admin sub-screens from SETTINGS_SCHEMA, not from literal SETTINGS_FIELDS tuples"
    - "Each migrated key's typed value (int for pending_reminder_interval, date for payment_deadline, list for the *_options keys) parses byte-for-byte identically to the pre-migration ad-hoc parse"
    - "Adding or editing any migrated text/int/list/date key requires editing only its settings_schema.py entry"
    - "No migrated setting is reset or lost for the ~590 live users; the render snapshot for every migrated group is byte-identical before and after"
  artifacts:
    - path: "settings_schema.py"
      provides: "reg/pay/party/consent group entries with correct type taxonomy (text/int/list/date/enum)"
      contains: "\"pending_reminder_interval\""
    - path: "handlers/admin.py"
      provides: "reg/pay/party/consent portions of SETTINGS_FIELDS/SETTINGS_GROUPS generated from the registry"
    - path: "tests/test_settings_groups_c0x.py"
      provides: "parse-equivalence for int/date/list + per-group render snapshots"
  key_links:
    - from: "handlers/admin.py SETTINGS_FIELDS/SETTINGS_GROUPS"
      to: "settings_schema.SETTINGS_SCHEMA"
      via: "computed generation for reg/pay/party/consent groups"
      pattern: "SETTINGS_SCHEMA"
---

<objective>
Migrate the remaining text-based settings groups (reg, pay, party, consent) into `SETTINGS_SCHEMA`, so the entire settings-value surface of the admin UI (except toggles/photos, handled in later waves) is registry-driven with correct per-type metadata.

Purpose: Extends the proven `event` pilot pattern (D-11) to the rest of the text groups (D-13/D-14), collapsing the literal SETTINGS_FIELDS/SETTINGS_GROUPS tables toward a single source of truth and locking parse-equivalence for the int/date/list types that downstream consumers (plan 06-03) will read.
Output: registry entries for reg/pay/party/consent keys, their admin metadata generated from the registry, and parse-equivalence + render-snapshot regression tests for each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** DXP manager configuring an AIESEC event, **I want to** edit every registration/payment/party/consent setting from the one registry file, **so that** a single edit changes both the admin screen and the parsed value — with no drift in what the ~590 live delegates experience.

<context>
@.planning/ROADMAP.md
@.planning/phases/06-settings-schema-registry/06-CONTEXT.md
@.planning/phases/06-settings-schema-registry/06-PATTERNS.md
@.planning/phases/06-settings-schema-registry/06-01-SUMMARY.md

<interfaces>
<!-- Group -> keys (from SETTINGS_GROUPS admin.py:402-417). Types per D-04. -->
reg group ("📝 Регистрация"): source_options(list), reg_complete_text(text), approve_text(text),
  reject_text(text), pending_reminder_interval(INT — default 1800, must match services/reminders.py DEFAULT_INTERVAL),
  city_options(list), study_field_options(list), goal_options(list), formats_options(list), university_options(list)
pay group ("💳 Оплата"): payment_options(list), payment_requisites(text), payment_requisites_by_lc(list/text),
  payment_deadline(DATE — "%d.%m.%Y %H:%M"), payment_reminder_text(text), payment_overdue_text(text), penalty_schedule(list)
party group ("🎉 Party"): party_closed_text(text — display default "Регистрация на вечеринку сейчас закрыта."),
  party_sheet_tab(text — display default "Party"), approve_text__party(text)
consent group ("📋 Согласия"): consent_button_text(text), consent_list(list)

Also in reg group but NOT here yet: consent_button_text? (it is consent group). Keys not in any SETTINGS_FIELDS
row stay literal. _SETTINGS_DISPLAY_DEFAULTS (admin.py:389-392) values move into the registry entries' `default` field.

Parse analogs (already lifted into _parse_setting in 06-01): int=services/reminders.py:22-28,
date=services/scheduler.py:45-50, list=handlers/registration.py:188-195 / keyboards/builders.py:56-64.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing parse-equivalence + per-group render-snapshot tests for reg/pay/party/consent</name>
  <files>tests/test_settings_groups_c0x.py</files>
  <read_first>
    - tests/test_settings_groups_c0x.py (full — extend, reuse scaffold)
    - settings_schema.py (06-01 — _parse_setting signature, get_setting_typed)
    - services/reminders.py:22-28 (DEFAULT_INTERVAL=1800 int semantics), services/scheduler.py:45-50 (date), handlers/registration.py:188-195 (list) — the byte-for-byte comparison targets
    - handlers/admin.py:389-417, 593-654 (display defaults, group render for snapshot)
  </read_first>
  <behavior>
    - test_parse_equivalence_int: table-driven over pending_reminder_interval raw inputs {None, "", "abc", "0", "-5", "900", "1800"} — _parse_setting("pending_reminder_interval", raw) == _reminder_interval(raw) from services.reminders (import and call the real helper as oracle). Registry default MUST be 1800.
    - test_parse_equivalence_date: over payment_deadline raw {None, "", "garbage", "15.08.2026 23:59"} — _parse_setting == _parse_schedule_dt from services.scheduler.
    - test_parse_equivalence_list: over source_options raw {None, "", "a\nb\n\n c ", "a; b ;c"} — _parse_setting == the splitlines/strip (and `;`) list result matching _get_options behavior.
    - test_registry_coverage_all_text_groups: every reg/pay/party/consent key has exactly one group, valid type, and default parses without raising; no key appears in both a literal SETTINGS_FIELDS tuple AND the registry (no double source).
    - test_render_snapshot_reg / _pay / _party / _consent: render_settings_group_text(token) + build_settings_group_keyboard(token) callback_data equal the captured pre-migration snapshot for each token (labels, order, «── не настроено ──» collapse, «по умолчанию» flag for party_closed_text/party_sheet_tab).
  </behavior>
  <action>Extend tests/test_settings_groups_c0x.py. For parse-equivalence, import the real oracle helpers (`from services.reminders import _reminder_interval`; `from services.scheduler import _parse_schedule_dt`) and assert `_parse_setting(key, raw) == oracle(raw)` across the input matrices — this is the D-15 byte-for-byte proof. For render snapshots, capture the CURRENT output of each group's render/keyboard BEFORE Task 2 edits the source (run the helper against the literal-backed admin module and hard-code the expected label/order/callback list in the test). Tests must FAIL after Task 2 only if metadata drifts — so write them to assert the exact current strings; they will pass once Task 2 preserves them and fail if it doesn't. Parse-equivalence and coverage tests fail now (registry lacks these keys / wrong default).</action>
  <acceptance_criteria>
    - New test functions test_parse_equivalence_int, test_parse_equivalence_date, test_parse_equivalence_list, test_registry_coverage_all_text_groups, and four test_render_snapshot_* exist.
    - Before Task 2: `python -m pytest tests/test_settings_groups_c0x.py -k "parse_equivalence or coverage_all_text" -q` FAILS (keys absent / default mismatch) — proves RED.
    - Oracle helpers are imported from the real modules, not re-implemented in the test.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py -k "parse_equivalence or coverage_all_text" -q</automated>
  </verify>
  <done>Parse-equivalence/coverage tests fail because reg/pay/party/consent keys are not yet in the registry with correct types/defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Add reg/pay/party/consent entries to SETTINGS_SCHEMA and generate their admin views</name>
  <files>settings_schema.py, handlers/admin.py</files>
  <read_first>
    - settings_schema.py (06-01 — extend SETTINGS_SCHEMA and reuse _parse_setting; add NO new parse code)
    - handlers/admin.py:341-448 (SETTINGS_FIELDS/_SETTINGS_DISPLAY_DEFAULTS/SETTINGS_GROUPS + leftover-safety generators)
    - handlers/admin.py:593-654 (generic render/keyboard consumers — leave unchanged, D-14)
  </read_first>
  <action>In settings_schema.py, add registry entries for every reg/pay/party/consent key listed in the interfaces block, copying label/prompt byte-for-byte from SETTINGS_FIELDS (admin.py:341-385) and setting the correct `type`: pending_reminder_interval -> `"int"` with `"default": 1800`; payment_deadline -> `"date"`; the `*_options` keys, payment_options, penalty_schedule, consent_list, payment_requisites_by_lc -> `"list"`; the rest -> `"text"`. For party_closed_text and party_sheet_tab, put the current _SETTINGS_DISPLAY_DEFAULTS strings into the entry `default` field (so the «по умолчанию» flag path still fires — render_settings_group_text checks `key in _SETTINGS_DISPLAY_DEFAULTS`; keep that dict OR derive it from the registry — see below). Reuse the existing `_parse_setting` dispatch; do NOT add per-type functions.
  In handlers/admin.py, extend the computed-view mechanism from 06-01 so the reg/pay/party/consent group rows of SETTINGS_GROUPS and their SETTINGS_FIELDS tuples are generated from the registry (same explicit-ordered-build approach used for event, preserving on-screen order per group). Remove the now-duplicated literal tuples for these migrated keys from SETTINGS_FIELDS/SETTINGS_GROUPS (D-13 single source — no key defined in both places). Derive `_SETTINGS_DISPLAY_DEFAULTS` for migrated keys from the registry `default` (e.g. `{k: v["default"] for k,v in SETTINGS_SCHEMA.items() if v.get("default") not in (None, "")}` unioned with any unmigrated literal entries) so the «по умолчанию» branch stays byte-identical. Keep render_settings_group_text/build_settings_group_keyboard and all settings_edit callbacks unchanged. Tag with `# REG-01/REG-03:` provenance comments.</action>
  <acceptance_criteria>
    - settings_schema.py source contains `"pending_reminder_interval"` with `"type": "int"` and `"default": 1800`, and `"payment_deadline"` with `"type": "date"`.
    - `grep -c` (excluding comment lines) shows no reg/pay/party/consent key label/prompt remaining as a hand-written literal tuple inside SETTINGS_FIELDS in admin.py (they are generated); event + any unmigrated keys may remain generated/literal respectively.
    - `python -m pytest tests/test_settings_groups_c0x.py -q` exits 0 (parse-equivalence, coverage, ALL render snapshots, and the pre-existing 260724-c0x tests green).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py -q</automated>
  </verify>
  <done>reg/pay/party/consent metadata is registry-sourced; every group renders byte-identically; int/date/list parse-equivalence proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot_settings row -> _parse_setting (int/date/list) | Arbitrary stored strings for numeric/date/list keys parsed by type; largest injection/DoS surface of the phase. |
| admin write path | Unchanged; must not regress the ADMIN_IDS gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | DoS/Tampering | int/date/list parse on garbage raw (e.g. "abc", huge string, malformed date) | mitigate | Parse bodies are the already-hardened try/except analogs; test_parse_equivalence_* asserts identical output to the production oracle helpers across None/empty/garbage — no new failure mode introduced. |
| T-06-06 | Data integrity | Silent default drift when moving _SETTINGS_DISPLAY_DEFAULTS into the registry (party_closed_text/party_sheet_tab) | mitigate | test_render_snapshot_party asserts the «по умолчанию» flag still fires with the exact same default strings; default derived from registry equals the old literal. |
| T-06-07 | Data integrity | pending_reminder_interval default mismatch would silently change reminder cadence for live admins | mitigate | Registry default pinned to 1800 and test_parse_equivalence_int compares against services.reminders._reminder_interval (which uses DEFAULT_INTERVAL=1800) — byte-for-byte. |
| T-06-08 | Repudiation | Migrated keys losing provenance | accept | `# REG-01/REG-03:` comments tag every migrated block per the codebase audit-trail convention. |
</threat_model>

<verification>
- `python -m pytest tests/test_settings_groups_c0x.py -q` fully green.
- `python -m pytest tests/test_registration_phase4.py tests/test_registration_phase5.py -q` green.
- Manual grep confirms no key exists in both the registry and a literal SETTINGS_FIELDS tuple.
</verification>

<success_criteria>
- REG-01: reg/pay/party/consent keys described once, in the registry, with correct type taxonomy.
- REG-03: those groups' admin sub-screens render from the registry, byte-identical.
- Parse-equivalence proven for int/date/list (D-15), unlocking the consumer wave (06-03).
- Coexistence holds: toggle/photo/file surfaces and any unmigrated keys still work.
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-schema-registry/06-02-SUMMARY.md` documenting each migrated key's assigned type, the default values moved from _SETTINGS_DISPLAY_DEFAULTS, how per-group order was preserved, and the parse-equivalence oracle results.
</output>
