---
phase: 06-settings-schema-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings_schema.py
  - handlers/admin.py
  - tests/test_settings_groups_c0x.py
autonomous: true
requirements: [REG-01, REG-02, REG-03]
must_haves:
  truths:
    - "A single new module settings_schema.py exists and describes every bot_settings key it owns as {type, group, label, default, prompt}"
    - "get_setting_typed(key) returns a parsed value using the raw get_setting read plus the registry default — never duplicating raw I/O"
    - "_parse_setting's enum branch resolves falsy raw (None AND empty-string) to the registry default, byte-for-byte with the live `get_setting(k) or '<default>'` idiom"
    - "The admin settings screen renders the event group (labels, order, flags) byte-identically before and after the registry becomes its source"
    - "No migrated event key loses its value or resets for the ~590 live users — coexistence with unmigrated literal entries holds"
  artifacts:
    - path: "settings_schema.py"
      provides: "SETTINGS_SCHEMA registry (event group), _parse_setting type-dispatch, get_setting_typed accessor"
      contains: "SETTINGS_SCHEMA = {"
    - path: "handlers/admin.py"
      provides: "event portion of SETTINGS_FIELDS / SETTINGS_GROUPS generated from the registry (computed view), coexisting with unmigrated literal entries"
    - path: "tests/test_settings_groups_c0x.py"
      provides: "parse-equivalence (text/enum/photo/file), event coverage, event render snapshot tests"
  key_links:
    - from: "settings_schema.py::get_setting_typed"
      to: "database.db.get_setting"
      via: "raw read then _parse_setting dispatch"
      pattern: "get_setting_typed"
    - from: "handlers/admin.py SETTINGS_FIELDS/SETTINGS_GROUPS"
      to: "settings_schema.SETTINGS_SCHEMA"
      via: "computed generation for the event group"
      pattern: "SETTINGS_SCHEMA"
---

<objective>
Stand up the `settings_schema.py` registry module (metadata + type-driven parse machinery + typed async accessor) and prove the full registry → typed-read → admin-render → regression loop end-to-end on the low-risk `event` text group (D-11 pilot).

Purpose: This is the enabling scaffold for the whole phase (REG-01) and the first working vertical slice — after this plan a manager still sees the identical `event` settings screen, but that screen is now driven by one file instead of four. Removes the 4-file coordination tax for every migrated key (North Star).
Output: `settings_schema.py` (event entries + `_parse_setting` full dispatch + `get_setting_typed`), the `event` group of `SETTINGS_FIELDS`/`SETTINGS_GROUPS` computed from the registry, and the first three of the four regression layers (parse-equivalence, coverage, render snapshot).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** DXP manager configuring an AIESEC event, **I want to** have every bot setting described in one registry file that both the admin screen and the code read from, **so that** adding or editing a setting requires editing only that one registry entry instead of four scattered files — with zero change to what I see on screen or what the ~590 live delegates experience.

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-schema-registry/06-CONTEXT.md
@.planning/phases/06-settings-schema-registry/06-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Executor must read these current-state anchors; do not assume, read. -->
Raw I/O being wrapped, unchanged (D-07) — database/db.py:184-205:
  async def get_setting(key: str) -> str | None   (SELECT value FROM bot_settings WHERE key = ?)
  async def set_setting(key, value)  / async def delete_setting(key)  — stay as-is.

Existing literal tables to generalize (handlers/admin.py):
  - SETTINGS_FIELDS   admin.py:341-385   list of (key, label, prompt)
  - _SETTINGS_DISPLAY_DEFAULTS  admin.py:389-392  fallback display text -> becomes registry `default`
  - SETTINGS_GROUPS   admin.py:397-417   list of (label, token, [keys])
  - _settings_group_keys / _settings_group_label / _settings_nav_groups  admin.py:420-448  (leftover-safety generator — REUSE)
  - PHOTO_FIELDS admin.py:450-455 / FILE_FIELDS admin.py:457-459  (key=prefix, label, prompt)
  - render_settings_group_text admin.py:593-621 / build_settings_group_keyboard admin.py:624-654  (already generic consumers of the above — do NOT rewrite call sites, D-14)

Pure-parse analogs to lift VERBATIM into _parse_setting branches:
  - int:   services/reminders.py:22-28 (_reminder_interval)  and services/scheduler.py:34-42 (_int_or_default)
  - date:  services/scheduler.py:45-50 (_parse_schedule_dt, "%d.%m.%Y %H:%M")
  - list:  handlers/registration.py:188-195 (_get_options, splitlines + strip) / keyboards/builders.py:56-64
  - toggle: handlers/admin.py:2095-2097 (_is_question_on):  (val == "on") if val is not None else (default == "on")

CRITICAL enum contract (the feature-switch idiom later waves depend on, byte-for-byte):
  Live feature-switch reads across the codebase use `await get_setting(k) or "<default>"` (e.g.
  admin.py:522/535-536, registration.py:582/2219). This coerces BOTH None AND empty-string to the
  default. Therefore _parse_setting's `enum` branch MUST be `raw if raw else default` (falsy->default),
  NOT `raw if raw is not None else default` — the two differ on empty-string, and the empty-string case
  is part of the byte-for-byte mandate (D-15).

event group keys (the 10 text pilot keys, admin.py:398-401 SETTINGS_GROUPS "event" token):
  event_date, event_time, event_place_name, event_place_address, contact_person,
  contact_vk, contact_tg, start_text, event_name, event_type
  (labels/prompts are in SETTINGS_FIELDS admin.py:342-359; event_type is enum forum/conference/custom, default None)
photo/file keys rendered under the event sub-screen (admin.py:450-459, 612-619):
  program, speakers, start, venue  (type: photo);  reg_bonus (type: file)
  NOTE derived-key convention stays: actual bot_settings row is f"{prefix}_photo_file_id"/f"{prefix}_doc_file_id" — registry key is the prefix only (D-10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing regression tests (parse-equivalence + event coverage + event render snapshot)</name>
  <files>tests/test_settings_groups_c0x.py</files>
  <read_first>
    - tests/test_settings_groups_c0x.py (full — reuse its `_admin_ready`, `FakeCallback`, `_flat_callback_data`, asyncio.run scaffold; pytest-asyncio is NOT available)
    - handlers/admin.py:462-621 (render_settings_text / render_settings_group_text — snapshot target for the event group)
    - .planning/phases/06-settings-schema-registry/06-PATTERNS.md (D-15/D-16/D-17 test shapes)
  </read_first>
  <behavior>
    - test_parse_setting_text_passthrough: for a text-type event key (e.g. "start_text"), _parse_setting(key, raw) returns raw unchanged for a non-empty string, and returns the registry `default` (None for event text keys) when raw is None.
    - test_parse_setting_enum_falsy_to_default: for the enum key "event_type", _parse_setting returns raw when raw is a non-empty string, and returns the registry default for BOTH raw=None AND raw="" (empty-string falls through to default — the byte-for-byte enum contract).
    - test_parse_setting_photo_file_passthrough: for a photo key ("program") and file key ("reg_bonus"), _parse_setting returns the raw file_id unchanged (passthrough, D-10).
    - test_registry_coverage_event: every SETTINGS_SCHEMA entry has group in the allowed set and type in the taxonomy {toggle,int,list,date,text,enum,photo,file}; every event-group key appears exactly once; _parse_setting(key, entry["default"]) does not raise for any entry.
    - test_event_render_snapshot: render_settings_group_text("event") and the flattened callback_data of build_settings_group_keyboard("event") equal a captured expected snapshot (labels, order, the photo/file settings_photo:/settings_file: callbacks, the "── не настроено ──" collapse). Assert the exact event field labels ("🗓 Дата", "⌚ Время", ...) still render.
  </behavior>
  <action>Extend tests/test_settings_groups_c0x.py (do NOT create a new file — D-17 mandates extension) with the five tests above, following the existing plain-function + asyncio.run + tmp_path style. Import the new module as `from settings_schema import SETTINGS_SCHEMA, _parse_setting, get_setting_typed`. The parse-equivalence and coverage tests are pure (no DB fixture needed); the render-snapshot test uses `_admin_ready(tmp_path)` and reuses `_flat_callback_data`. Capture the event render snapshot by asserting on the concrete label strings and callback_data tokens currently produced (read them from admin.py:342-359 and the settings_photo:/settings_file: builders at admin.py:634-647) — this is the byte-for-byte-render invariant (D-16, SC#3). Tests MUST fail now because settings_schema.py does not yet exist.</action>
  <acceptance_criteria>
    - `python -m pytest tests/test_settings_groups_c0x.py -k "parse_setting or registry_coverage_event or event_render_snapshot" -q` runs and FAILS with ModuleNotFoundError / AttributeError on `settings_schema` (proves RED).
    - The file contains functions named test_parse_setting_text_passthrough, test_parse_setting_enum_falsy_to_default, test_parse_setting_photo_file_passthrough, test_registry_coverage_event, test_event_render_snapshot.
    - No existing test in the file is deleted or weakened (the 260724-c0x tests remain).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py -k "parse_setting or registry_coverage_event or event_render_snapshot" -q</automated>
  </verify>
  <done>Five new tests exist and fail (RED) solely because settings_schema.py / its symbols are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create settings_schema.py — registry (event group), _parse_setting dispatch, get_setting_typed</name>
  <files>settings_schema.py</files>
  <read_first>
    - handlers/admin.py:341-385 (SETTINGS_FIELDS — copy exact event labels/prompts), :389-392 (_SETTINGS_DISPLAY_DEFAULTS), :450-459 (PHOTO_FIELDS/FILE_FIELDS)
    - database/db.py:184-205 (get_setting signature to wrap, unchanged — D-07)
    - services/reminders.py:22-28, services/scheduler.py:34-50, handlers/registration.py:188-195 (verbatim parse bodies for int/date/list)
    - handlers/admin.py:2095-2097 (_is_question_on — verbatim toggle semantics)
    - handlers/admin.py:522, 535-536 and registration.py:582, 2219 (the live `get_setting(k) or "<default>"` enum idiom the enum branch must reproduce)
    - .planning/codebase/CONVENTIONS.md (plain-dict domain entities, named import of database.db functions, `_private` pure helper extraction)
  </read_first>
  <action>Create the new top-level module `settings_schema.py`. It imports ONLY `from database.db import get_setting` (one-directional dependency — no import of handlers.* to avoid cycles per D-01). Define, per D-02/D-03/D-04:
  (1) `SETTINGS_SCHEMA = { ... }` — a plain dict-by-key. Populate ONLY the event group in this plan: the 10 event text keys with `{"type": "text", "group": "event", "label": <exact label from SETTINGS_FIELDS>, "prompt": <exact prompt>, "default": None}` (event_type gets `"type": "enum"` with an `"options"` list ["forum","conference","custom"] and `"default": None`); plus photo keys program/speakers/start/venue as `{"type": "photo", "group": "event", "label": ..., "prompt": ..., "default": None}` and file key reg_bonus as `{"type": "file", ...}`. Copy labels/prompts byte-for-byte from admin.py so the render snapshot matches.
  (2) `_parse_setting(key, raw)` — a PURE sync helper (no DB, no async — the unit-test surface, D-08). It looks up `SETTINGS_SCHEMA[key]` for `type` and `default`, then dispatches by type:
    - `text` -> `raw if raw is not None else default` (text default is None; the render's own truthiness check handles empty display).
    - `enum` -> `raw if raw else default` (falsy->default: empty-string AND None both resolve to default — this is the byte-for-byte contract for the `get_setting(k) or "<default>"` idiom that every feature-switch consumer relies on; do NOT use `is not None` here).
    - `photo`/`file` -> passthrough raw (default when None).
    - `int` -> lift the `_int_or_default` body verbatim using the entry's `default` as the fallback.
    - `date` -> lift `_parse_schedule_dt` body.
    - `list` -> lift `_get_options` splitlines/strip body (also accept `;` separator per the Telegram Enter=send convention already used elsewhere).
    - `toggle` -> reproduce `(raw == "on") if raw is not None else (entry_default == "on")` EXACTLY (returns bool; this is the reg_q question idiom, admin.py:2097).
    Support an optional per-entry `"parse"` callable override (D-03): if present, call it instead of the type branch. If `key` is not in the registry, return raw unchanged (fail-soft — supports coexistence with unmigrated keys).
  (3) `async def get_setting_typed(key)` — `raw = await get_setting(key)` then `return _parse_setting(key, raw)` (D-05). Thin wrapper, no duplicated SQL.
  Add a module docstring and `# REG-01:` provenance comments per the audit-trail convention.</action>
  <acceptance_criteria>
    - `settings_schema.py` exists at repo root and `python -c "import settings_schema"` exits 0 with no circular-import error.
    - Source contains `SETTINGS_SCHEMA = {`, an `"event_date"` entry with `type` and `default` keys, and a `"program"` entry with `"type": "photo"`.
    - The enum branch of `_parse_setting` uses `raw if raw else default` (falsy->default), NOT `is not None` — confirmed by test_parse_setting_enum_falsy_to_default passing for raw="".
    - `_parse_setting` is a plain `def` (not `async`); `get_setting_typed` is `async def` and its body calls `get_setting` exactly once.
    - `python -m pytest tests/test_settings_groups_c0x.py -k "parse_setting or registry_coverage_event" -q` exits 0 (these suites now GREEN).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import settings_schema" && python -m pytest tests/test_settings_groups_c0x.py -k "parse_setting or registry_coverage_event" -q</automated>
  </verify>
  <done>Registry module imports cleanly, event entries present, pure `_parse_setting` (with the falsy->default enum contract) and async `get_setting_typed` behave per the RED tests which are now GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the event group of SETTINGS_FIELDS/SETTINGS_GROUPS from the registry (REG-03 pilot, coexistence)</name>
  <files>handlers/admin.py</files>
  <read_first>
    - handlers/admin.py:341-448 (SETTINGS_FIELDS, _SETTINGS_DISPLAY_DEFAULTS, SETTINGS_GROUPS, and the leftover-safety generators _settings_group_keys/_settings_group_label/_settings_nav_groups — REUSE, do not fork)
    - handlers/admin.py:593-654 (render_settings_group_text / build_settings_group_keyboard — the generic consumers that must keep reading the same module-level names, D-14)
    - settings_schema.py (from Task 2 — the source of truth for event metadata)
  </read_first>
  <action>In handlers/admin.py, `from settings_schema import SETTINGS_SCHEMA` at the top (respect existing import ordering). Make the `event`-group entries of the module-level `SETTINGS_FIELDS` and `SETTINGS_GROUPS` COMPUTED from the registry rather than literal (D-13, single source of truth), while leaving every unmigrated group's literal tuples exactly as-is (coexistence invariant, SC#3):
  - Build the event portion as `[(k, v["label"], v["prompt"]) for k, v in SETTINGS_SCHEMA.items() if v["group"] == "event" and v["type"] in ("text","enum")]` and splice it in place of the current literal event tuples in SETTINGS_FIELDS — the remaining literal tuples (reg/pay/party/consent keys) stay untouched. Order MUST match the current on-screen order (event keys first, in the SETTINGS_GROUPS "event" order); if list-comprehension order differs from the current screen, build the event list explicitly from the ordered key list so the render snapshot stays byte-identical.
  - Replace the literal `("🎪 Событие/Медиа", "event", [...])` row of SETTINGS_GROUPS with a computed row whose key list is generated from the registry (`[k for k,v in SETTINGS_SCHEMA.items() if v["group"]=="event" and v["type"] in ("text","enum")]`), keeping the same label token "event". Do NOT touch the reg/pay/party/consent rows.
  - Confirm PHOTO_FIELDS/FILE_FIELDS still drive the event sub-screen photo/file buttons (the registry now documents these as type photo/file, but the upload-flow and derived-key lookup stay special-cased per D-10 — leave build_settings_group_keyboard's event branch unchanged).
  - Leave `_SETTINGS_DISPLAY_DEFAULTS`, render_settings_group_text, build_settings_group_keyboard, and every settings_edit/photo/file/toggle callback byte-identical (no call-site rewrites, D-14). Add `# REG-03:` provenance comments where the computed splice happens.</action>
  <acceptance_criteria>
    - `python -m pytest tests/test_settings_groups_c0x.py -q` exits 0 (ALL tests green, including the pre-existing 260724-c0x coverage/landing/subscreen tests AND the new event_render_snapshot).
    - `grep -n "from settings_schema import" handlers/admin.py` shows the import.
    - The event tuples inside SETTINGS_FIELDS are no longer hand-written literals for label/prompt — grep confirms they are produced from SETTINGS_SCHEMA (a comprehension or explicit build referencing SETTINGS_SCHEMA precedes the SETTINGS_FIELDS assignment or replaces the event block).
    - render_settings_group_text("event") output and build_settings_group_keyboard("event") callback_data are unchanged vs pre-migration (event_render_snapshot passes).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py -q</automated>
  </verify>
  <done>The event group's admin metadata is sourced from settings_schema.py; the settings screen renders byte-identically; unmigrated groups still work from their literal tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot_settings row -> _parse_setting | Stored raw strings (admin-authored, but arbitrary/legacy/empty/garbage) are parsed into typed values by the registry. |
| admin callback -> set_setting write path | Only config.ADMIN_IDS may write settings (existing gate in show_settings_group / edit handlers). This plan reads only; must not weaken it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering/DoS | _parse_setting int/date/list branches on arbitrary stored raw | mitigate | Lift the existing try/except bodies verbatim (TypeError/ValueError/AttributeError -> default); garbage input can never raise out of _parse_setting (covered by test_registry_coverage_event asserting default parses without raising). |
| T-06-02 | Data integrity | Event key values for ~590 live users during the literal->computed splice | mitigate | Computed view changes only WHERE metadata is defined, never the bot_settings values; get_setting_typed default-fallback preserves current display; event_render_snapshot proves no value/label drift. |
| T-06-03 | Data integrity | enum empty-string semantics silently diverging from the live `or`-idiom for downstream feature switches | mitigate | Enum branch pinned to `raw if raw else default`; test_parse_setting_enum_falsy_to_default asserts raw="" -> default, locking the contract the toggle/enum consumer waves rely on. |
| T-06-04 | Elevation of privilege | Admin-only write path | accept | This plan adds no write path and does not touch the ADMIN_IDS gate; long-polling bot has no webhook/web surface to attack. |
</threat_model>

<verification>
- `python -m pytest tests/test_settings_groups_c0x.py -q` fully green.
- `python -m pytest tests/test_registration_phase4.py tests/test_registration_phase5.py -q` green (no regression from touching admin.py imports).
- `python -c "import settings_schema, handlers.admin"` exits 0 (no import cycle).
</verification>

<success_criteria>
- REG-01 scaffold live: settings_schema.py is the single source of event-group metadata + a type-driven `_parse_setting` (with the falsy->default enum contract) + a `get_setting_typed` accessor that does not duplicate raw I/O.
- REG-03 pilot: the admin `event` sub-screen renders from the registry, byte-identical to before.
- REG-02 accessor available for downstream consumer waves.
- Coexistence invariant holds: unmigrated groups still render from their literal tables; no live setting reset.
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-schema-registry/06-01-SUMMARY.md` documenting: the registry entry shape chosen, the exact `_parse_setting` type branches and their source analogs (especially the enum falsy->default contract), the get_setting_typed signature, how the event computed-splice preserves order, and any deviation from the snapshot expectations.
</output>
