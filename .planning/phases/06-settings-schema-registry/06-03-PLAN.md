---
phase: 06-settings-schema-registry
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - services/reminders.py
  - services/scheduler.py
  - keyboards/builders.py
  - tests/test_settings_consumers_phase6.py
autonomous: true
requirements: [REG-02]
must_haves:
  truths:
    - "services/reminders.py reads pending_reminder_interval through the registry accessor instead of its own get_setting + _reminder_interval parse"
    - "services/scheduler.py reads its date/int settings through the registry accessor"
    - "keyboards/builders.py reads source_options (and any other list setting it owns) through the registry accessor"
    - "Every migrated consumer read produces byte-for-byte the same typed value it produced before (no behavior change)"
  artifacts:
    - path: "services/reminders.py"
      provides: "pending_reminder_interval read via get_setting_typed"
    - path: "services/scheduler.py"
      provides: "date/int settings read via get_setting_typed"
    - path: "keyboards/builders.py"
      provides: "list settings read via get_setting_typed"
    - path: "tests/test_settings_consumers_phase6.py"
      provides: "consumer parse-equivalence + behavior-preservation tests"
  key_links:
    - from: "services/reminders.py::pending_reminder_loop"
      to: "settings_schema.get_setting_typed"
      via: "typed read of pending_reminder_interval"
      pattern: "get_setting_typed"
    - from: "keyboards/builders.py::get_source_kb"
      to: "settings_schema.get_setting_typed"
      via: "typed read of source_options"
      pattern: "get_setting_typed"
---

<objective>
Switch the value CONSUMERS (services/reminders.py, services/scheduler.py, keyboards/builders.py) to read their migrated int/date/list settings through `get_setting_typed`, replacing each site's hand-rolled `get_setting` + local parse — with byte-for-byte behavior preserved (REG-02, SC#2).

Purpose: Completes the "registry is the source of truth for parsing/defaults" half of the keystone for the non-toggle types. After this, editing one of these keys' parse/default rule means editing only its registry entry, not the consumer.
Output: three migrated consumers plus a dedicated parse-equivalence/behavior test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** DXP manager, **I want to** the bot's reminder/scheduler/keyboard code to read setting values from the same registry the admin screen writes to, **so that** on/off/default semantics stay consistent everywhere and a rule change touches one file only — with the ~590 live delegates seeing no behavior change.

<context>
@.planning/phases/06-settings-schema-registry/06-CONTEXT.md
@.planning/phases/06-settings-schema-registry/06-PATTERNS.md
@.planning/phases/06-settings-schema-registry/06-02-SUMMARY.md

<interfaces>
Consumer sites to migrate (read current state before editing):
  - services/reminders.py:35-38  interval = _reminder_interval(await get_setting("pending_reminder_interval"))
        -> await get_setting_typed("pending_reminder_interval")   (int, default 1800)
        NOTE: pending_reminder_enabled is a TOGGLE — DEFER to the toggle wave (06-04); do NOT migrate _reminder_enabled here.
  - services/scheduler.py: sites using _int_or_default / _parse_schedule_dt on registry-migrated keys
        (e.g. payment_deadline date). Migrate ONLY keys that are in SETTINGS_SCHEMA with a non-toggle type after 06-02.
        Keys still holding scheduler-only defaults not yet in the registry stay as-is.
  - keyboards/builders.py:56-64  get_source_kb reads source_options -> list via splitlines
        -> items = await get_setting_typed("source_options")   (list; keep DEFAULT_SOURCE_OPTIONS fallback semantics —
        the registry list default should reproduce the current empty->DEFAULT_SOURCE_OPTIONS behavior, OR keep the
        `if items: else DEFAULT_SOURCE_OPTIONS` guard around the typed read to preserve exact behavior).

get_setting_typed / _parse_setting live in settings_schema.py (06-01). Import via `from settings_schema import get_setting_typed`.
Pure oracle helpers to compare against stay in place: _reminder_interval, _parse_schedule_dt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing consumer behavior-preservation tests</name>
  <files>tests/test_settings_consumers_phase6.py</files>
  <read_first>
    - tests/test_settings_groups_c0x.py:1-55 (copy the asyncio.run + tmp_path + config.DB_PATH scaffold — new file per naming convention test_<feature>_<phase>.py)
    - services/reminders.py (full — the loop and helpers), services/scheduler.py:30-60, keyboards/builders.py:45-70
    - settings_schema.py (get_setting_typed)
  </read_first>
  <behavior>
    - test_reminders_interval_via_registry_matches_oracle: for stored pending_reminder_interval in {unset, "900", "0", "abc"}, the value the reminder loop resolves equals _reminder_interval(await get_setting("pending_reminder_interval")). Assert via calling get_setting_typed and comparing to the oracle over a real tmp DB.
    - test_source_kb_unchanged: get_source_kb() produces the same button set for {source_options unset -> DEFAULT_SOURCE_OPTIONS, source_options="a\nb" -> [a,b]} as before migration.
    - test_scheduler_date_via_registry_matches_oracle: payment_deadline typed read equals _parse_schedule_dt(await get_setting("payment_deadline")) for valid/invalid/None.
  </behavior>
  <action>Create tests/test_settings_consumers_phase6.py (new file — avoids write-conflict with the group tests, keeping this plan parallelizable). Use the asyncio.run scaffold with a tmp_path DB. Import oracle helpers from their real modules and assert the typed read equals the oracle. For get_source_kb, extract the button texts from the returned ReplyKeyboardMarkup and compare to the expected list. Tests fail now because the consumers have not yet been switched (they still call the local parse, which is fine) — write the assertions against get_setting_typed so they fail if 06-02's registry entries are wrong, and add a post-migration assertion (Task 2) that the CONSUMER function itself calls get_setting_typed (e.g. monkeypatch settings_schema.get_setting_typed and assert it was invoked by pending_reminder_loop's interval resolution / get_source_kb).</action>
  <acceptance_criteria>
    - tests/test_settings_consumers_phase6.py exists with the three test functions.
    - `python -m pytest tests/test_settings_consumers_phase6.py -q` FAILS on the "consumer calls get_setting_typed" assertion (RED) while the oracle-equivalence assertions pass (registry from 06-02 is correct).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_consumers_phase6.py -q</automated>
  </verify>
  <done>New consumer test file exists; the "reads via registry" assertions fail because consumers are not yet migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate reminders/scheduler/builders to get_setting_typed</name>
  <files>services/reminders.py, services/scheduler.py, keyboards/builders.py</files>
  <read_first>
    - services/reminders.py:31-55, services/scheduler.py:30-70, keyboards/builders.py:45-70 (exact current call sites)
    - settings_schema.py (get_setting_typed signature)
    - .planning/codebase/CONVENTIONS.md (lazy import to break cycles if builders<->handlers cycle appears; named-import convention)
  </read_first>
  <action>Replace the migrated non-toggle read sites with `get_setting_typed`:
  - services/reminders.py: import get_setting_typed; change the interval line to `interval = await get_setting_typed("pending_reminder_interval")`. LEAVE `_reminder_enabled`/pending_reminder_enabled untouched (toggle wave 06-04). Keep `_reminder_interval` defined (still the test oracle and referenced for DEFAULT_INTERVAL) unless fully unused — if it becomes unused, keep it with a `# REG-02: retained as parse oracle` note rather than deleting (existing imports/tests may reference it).
  - services/scheduler.py: for each site reading a registry-migrated non-toggle key via `_int_or_default`/`_parse_schedule_dt`, replace with `await get_setting_typed(key)`. Do NOT migrate keys not yet in SETTINGS_SCHEMA. Preserve any downstream None-handling (payment_deadline None path).
  - keyboards/builders.py: import get_setting_typed (module-level if no cycle; else lazy import inside get_source_kb per the keyboards/builders.py:27-34 pattern). Replace the source_options splitlines block with `items = await get_setting_typed("source_options")` and keep the `if items: ... else: items = DEFAULT_SOURCE_OPTIONS` guard so empty/unset behavior is byte-identical.
  Tag each site with `# REG-02:` provenance comments.</action>
  <acceptance_criteria>
    - `grep -n "get_setting_typed" services/reminders.py services/scheduler.py keyboards/builders.py` shows the accessor used at each migrated site.
    - `grep -n "pending_reminder_enabled\|_reminder_enabled" services/reminders.py` still shows the toggle read UNCHANGED (deferred).
    - `python -m pytest tests/test_settings_consumers_phase6.py -q` exits 0 (behavior preserved + consumers now call the registry).
    - `python -c "import services.reminders, services.scheduler, keyboards.builders"` exits 0 (no import cycle).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_consumers_phase6.py -q && python -c "import services.reminders, services.scheduler, keyboards.builders"</automated>
  </verify>
  <done>The three consumers read their int/date/list settings via get_setting_typed with identical behavior; toggle reads deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot_settings -> get_setting_typed (consumer read) | Same stored-string parse surface, now centralized; consumers no longer each re-implement parsing. |
| scheduler job execution | payment_deadline drives scheduled sends; a wrong parse could mis-fire reminders to live users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Data integrity/DoS | reminder cadence / scheduler firing if typed read differs from old parse | mitigate | Parse-equivalence tests compare get_setting_typed to the real _reminder_interval/_parse_schedule_dt oracles across None/empty/garbage; identical output required to pass. |
| T-06-10 | Availability | New import of settings_schema into keyboards/builders.py creating a circular import that breaks startup | mitigate | settings_schema imports only database.db; if a cycle appears, use the established function-local lazy import (CONVENTIONS.md); import smoke test in acceptance. |
| T-06-11 | Correctness | source_options empty->DEFAULT fallback lost | mitigate | Keep the explicit `if items else DEFAULT_SOURCE_OPTIONS` guard; test_source_kb_unchanged asserts the unset case still yields DEFAULT_SOURCE_OPTIONS. |
</threat_model>

<verification>
- `python -m pytest tests/test_settings_consumers_phase6.py -q` green.
- `python -m pytest tests/test_scheduler_reconcile_block6.py -q` (and any reminders test) green — no scheduler regression.
- Import smoke: `python -c "import services.reminders, services.scheduler, keyboards.builders, main"` exits 0.
</verification>

<success_criteria>
- REG-02: the non-toggle value consumers read through the registry accessor; behavior byte-for-byte unchanged.
- Toggle reads explicitly deferred to 06-04 (no scope bleed).
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-schema-registry/06-03-SUMMARY.md` listing each migrated call site, whether a lazy import was needed, and the oracle-equivalence results.
</output>
