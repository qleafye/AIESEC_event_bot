---
phase: 06-settings-schema-registry
plan: 05
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified: []
autonomous: false
requirements: [REG-01, REG-02, REG-03]
must_haves:
  truths:
    - "On a real bot instance the ⚙️ Настройки forum screen and every migrated group sub-screen render exactly as before the migration"
    - "Editing a setting in each migrated group persists and re-renders correctly, sourced from the registry"
    - "No live setting value was reset by the migration"
  artifacts:
    - path: ".planning/phases/06-settings-schema-registry/06-SMOKE-CHECKLIST.md"
      provides: "recorded manual smoke result per migrated group"
  key_links:
    - from: "admin settings UI"
      to: "settings_schema.SETTINGS_SCHEMA"
      via: "live render + edit round-trip"
      pattern: "manual"
---

<objective>
Consolidated manual smoke verification (D-18) of the whole migrated settings surface on a real bot instance, after all four automated regression layers (parse-equivalence, render snapshot, coverage, consumer behavior) are green. The env has no CI/linter, so a human must confirm the live admin screens (D-18, prior-phase deferred-UAT pattern).

Purpose: Final gate that the byte-for-byte automated proofs hold on the real Telegram UI and that no live setting silently reset for the ~590 users.
Output: a recorded smoke checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-settings-schema-registry/06-01-SUMMARY.md
@.planning/phases/06-settings-schema-registry/06-02-SUMMARY.md
@.planning/phases/06-settings-schema-registry/06-03-SUMMARY.md
@.planning/phases/06-settings-schema-registry/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated regression net and record results</name>
  <files>.planning/phases/06-settings-schema-registry/06-SMOKE-CHECKLIST.md</files>
  <read_first>
    - tests/test_settings_groups_c0x.py, tests/test_settings_consumers_phase6.py
    - all four 06-0N-SUMMARY.md files
  </read_first>
  <action>Run the complete relevant test suite and capture the pass counts into a new 06-SMOKE-CHECKLIST.md: `python -m pytest tests/test_settings_groups_c0x.py tests/test_settings_consumers_phase6.py tests/test_registration_phase4.py tests/test_registration_phase5.py -q` plus a full `python -m pytest -q` to confirm no cross-suite regression. Record the totals and any skips. Then write the human smoke steps (Task 2) into the same file as an unchecked checklist.</action>
  <acceptance_criteria>
    - `python -m pytest tests/test_settings_groups_c0x.py tests/test_settings_consumers_phase6.py tests/test_registration_phase4.py tests/test_registration_phase5.py -q` exits 0.
    - `python -m pytest -q` exits 0 (no unrelated regression).
    - 06-SMOKE-CHECKLIST.md exists with recorded pass counts and the human smoke checklist.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>Full suite green; checklist file created with automated results and pending manual steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The settings surface (event, reg, pay, party, consent groups + toggles) is now registry-driven; all four automated regression layers are green.</what-built>
  <how-to-verify>
    On a real bot instance with a copy of (or the live) bot_settings data, as an admin:
    1. Open /admin → ⚙️ Настройки форума. Confirm the landing screen text and toggle buttons look exactly as before (compare against a screenshot from before the migration if available).
    2. Open each migrated group sub-screen: 🎪 Событие/Медиа, 📝 Регистрация, 💳 Оплата, 🎉 Party, 📋 Согласия. For each, confirm the field labels, order, «✏️ задано / — не задано / по умолчанию» flags, and the «── не настроено ──» collapse all match prior behavior.
    3. Edit one setting in each group (e.g. change 🗓 Дата, then a payment field, then a party text). Confirm it saves and the flag flips to «задано» with the new value behind the edit tap-through.
    4. Confirm party_closed_text / party_sheet_tab still show «по умолчанию» when unset.
    5. Confirm a toggle question (e.g. flip a reg_q_* question in 📋 Вопросы регистрации) still toggles and the default-off/on questions are unchanged from before.
    6. Confirm the pending-application reminder still fires on its interval and any payment-deadline reminder timing is unchanged (spot check, or confirm no error in logs).
    7. Confirm NO setting reset to blank/default unexpectedly after restart.
    Record pass/fail per step in 06-SMOKE-CHECKLIST.md.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe the drift/regression observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live bot_settings data -> migrated render/parse | Final human confirmation that no live value drifted or reset on the real UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Data integrity | A drift invisible to automated snapshots (e.g. locale/emoji rendering, restart-persistence) reaching live users | mitigate | Human smoke on a real instance across every migrated group + a restart-persistence check (step 7); blocking gate before phase close. |
| T-06-17 | Elevation of privilege | Admin gate regression | accept | No code changed in this plan; the ADMIN_IDS gate was untouched across 06-01..06-04 and is covered by test_show_settings_group_rejects_non_admin. |
</threat_model>

<verification>
- Full `python -m pytest -q` green.
- Human sign-off recorded in 06-SMOKE-CHECKLIST.md across all migrated groups + toggle + restart-persistence.
</verification>

<success_criteria>
- All four regression layers (D-15 parse-equivalence, D-16 render snapshot, D-17 coverage, consumer behavior) green.
- D-18 manual smoke passed on a real instance for every migrated group.
- No live setting reset; REG-01/REG-02/REG-03 confirmed end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-schema-registry/06-05-SUMMARY.md` summarizing the automated totals and the human smoke outcome per group, and note the phase is ready for verify-phase.
</output>
