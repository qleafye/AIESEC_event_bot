---
phase: 06-settings-schema-registry
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - handlers/admin.py
  - tests/test_settings_groups_c0x.py
autonomous: true
requirements: [REG-02]
must_haves:
  truths:
    - "render_settings_text reads every feature-switch value (reg_bonus_enabled, full_approval, short_approval, pending_notify_mode, payment_enabled, consent_enabled, payment_reminders_enabled, party_enabled, party_fork_question, party_approval) through get_setting_typed, not `get_setting(k) or '<literal>'`"
    - "build_settings_keyboard reads every toggle-button value through get_setting_typed while keeping its bespoke button-text ternaries and callback-data strings byte-identical (D-12: no button-generation refactor)"
    - "The admin toggle-handler read-sites (registration_mode at :692, pending_notify_mode at :833, reg_bonus_enabled at :847, party_enabled gate at :2229) resolve the current value through get_setting_typed"
    - "The settings landing text and the toggle-button block (button texts + callback_data) render byte-identically before and after"
  artifacts:
    - path: "handlers/admin.py"
      provides: "admin feature-switch reads migrated to get_setting_typed; button structure unchanged"
    - path: "tests/test_settings_groups_c0x.py"
      provides: "render_settings_text + build_settings_keyboard button-text/callback snapshot (automated toggle-button regression)"
  key_links:
    - from: "handlers/admin.py::build_settings_keyboard"
      to: "settings_schema.get_setting_typed"
      via: "typed read of each feature-switch key"
      pattern: "get_setting_typed"
    - from: "handlers/admin.py::render_settings_text"
      to: "settings_schema.get_setting_typed"
      via: "typed read of each feature-switch key"
      pattern: "get_setting_typed"
---

<objective>
Migrate the admin-side feature-switch value READS (settings landing render + toggle-button keyboard + the admin toggle-handler read-sites) from the hardcoded `get_setting(k) or "<default>"` idiom to `get_setting_typed`, satisfying the locked decision that the toggle wave absorbs the hardcoded button reads (06-DISCUSSION-LOG.md:48) — byte-for-byte (REG-02, SC#2).

Purpose: The admin settings screen is where the "one file describes everything" promise is most visible; after this plan the on-screen defaults for these ~14 keys come from the registry, not from literals scattered across render_settings_text and build_settings_keyboard. Button STRUCTURE (bespoke callback-data) is intentionally left as-is per D-12 — only the value reads move.
Output: migrated admin reads + an automated snapshot locking the landing text and toggle-button block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** DXP manager, **I want to** the ⚙️ Настройки screen to show every feature toggle's default from the one registry file, **so that** changing a default is a one-line registry edit — with the toggle buttons looking and behaving exactly as they do today for the ~590 live delegates.

<context>
@.planning/phases/06-settings-schema-registry/06-CONTEXT.md
@.planning/phases/06-settings-schema-registry/06-PATTERNS.md
@.planning/phases/06-settings-schema-registry/06-04-SUMMARY.md

<interfaces>
Admin feature-switch read-sites to migrate — LINE NUMBERS RE-VERIFIED against current handlers/admin.py.
Replace ONLY the value read, keep the surrounding label/ternary/comparison so output is byte-identical;
get_setting_typed(enum) returns the same string `(get_setting(k) or default)` would:
  - render_settings_text (handlers/admin.py:465-496): reg_bonus_enabled(:469), full_approval(:473),
      short_approval(:474), pending_notify_mode(:475), payment_enabled(:482), consent_enabled(:483),
      payment_reminders_enabled(:486), party_enabled(:491), party_fork_question(:492), party_approval(:493).
  - build_settings_keyboard (handlers/admin.py:522-564): registration_mode(:522), reg_bonus_enabled(:525),
      full_approval(:528), short_approval(:529), pending_notify_mode(:530), payment_enabled(:535),
      consent_enabled(:536), payment_reminders_enabled(:539), reg_university_mode(:543), edu_conditional(:546),
      reg_show_progress(:549), party_enabled(:556), party_fork_question(:559), party_approval(:562).
      KEEP the button-text ternaries + callback-data strings (settings_toggle_reg, toggle_party_enabled, ...)
      EXACTLY — D-12 forbids collapsing them into a generated loop in this phase.
  - admin toggle handlers (current-value read at the top of each handler, before the set_setting write):
      registration_mode at :692 (toggle_registration_mode), pending_notify_mode at :833 (toggle_notify_mode),
      reg_bonus_enabled at :847 (toggle_bonus), and the party_enabled gate at :2229 — migrate the READ; the
      write path (set_setting) is unchanged.

get_setting_typed / _parse_setting live in settings_schema.py; enum entries + defaults were added in 06-04.
Import via `from settings_schema import get_setting_typed` (SETTINGS_SCHEMA import already present from 06-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing snapshot test for settings landing text + toggle-button block</name>
  <files>tests/test_settings_groups_c0x.py</files>
  <read_first>
    - tests/test_settings_groups_c0x.py (full — extend, reuse `_admin_ready`, `_flat_callback_data`)
    - handlers/admin.py:462-590 (render_settings_text + build_settings_keyboard — the snapshot targets)
    - settings_schema.py (get_setting_typed)
  </read_first>
  <behavior>
    - test_settings_landing_text_snapshot: over a fresh tmp DB (all feature switches unset -> defaults), render_settings_text() equals the captured pre-migration string (all the "📝 Форма регистрации", "✅ Модерация...", "🎉 Трек вечеринки" default labels). Also assert one non-default case: set party_enabled="on", full_approval="auto" -> the two lines flip exactly as before.
    - test_settings_toggle_button_snapshot (partial WARNING-3 automation): build_settings_keyboard() button TEXTS and callback_data (via _flat_callback_data) equal the captured pre-migration lists for both the all-default case and a mixed case (party_enabled="on", payment_enabled="on", pending_notify_mode="instant"). This locks the bespoke callback-data strings (settings_toggle_reg, toggle_party_enabled, ...) and the exact toggle-text ternary output.
  </behavior>
  <action>Extend tests/test_settings_groups_c0x.py. Capture the CURRENT (pre-migration) render_settings_text and build_settings_keyboard outputs by reading admin.py:462-590 and hard-coding the expected label strings + button texts + callback_data into the test. Cover both the all-default (empty DB) case and at least one mixed non-default case per assertion so a default drift is caught. Because Task 2 preserves the output byte-for-byte, these tests fail now ONLY if written against not-yet-migrated code that already produces the same output — so instead write them to PASS against current code and act as the regression lock (they must still pass after Task 2). Mark them clearly as the toggle-button regression net. (If preferred RED-first: assert on get_setting_typed being invoked by the two functions via monkeypatch, which fails until Task 2 wires it.)</action>
  <acceptance_criteria>
    - test_settings_landing_text_snapshot and test_settings_toggle_button_snapshot exist and PASS against the current code (regression lock established).
    - The toggle-button snapshot asserts the exact callback_data strings settings_toggle_reg AND toggle_party_enabled are present and in the same positions.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py -k "landing_text_snapshot or toggle_button_snapshot" -q</automated>
  </verify>
  <done>An automated snapshot locks the landing text and toggle-button block before the reads are migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate admin feature-switch reads to get_setting_typed</name>
  <files>handlers/admin.py</files>
  <read_first>
    - handlers/admin.py:462-590 (render_settings_text + build_settings_keyboard)
    - handlers/admin.py:687-695 (toggle_registration_mode), :829-850 (toggle_notify_mode + toggle_bonus), :2225-2232 (party_enabled gate)
    - settings_schema.py (get_setting_typed, enum entries from 06-04)
  </read_first>
  <action>Add `get_setting_typed` to the settings_schema import in handlers/admin.py. At each site listed in the interfaces block, replace the value read `await get_setting("<key>") or "<literal>"` with `await get_setting_typed("<key>")` (for the `reg_bonus_enabled` site at :469/:525 that reads `await get_setting("reg_bonus_enabled")` and compares `== "on"` with no `or`, the enum entry default "off" makes get_setting_typed return "off" for unset — byte-identical). KEEP every surrounding label ternary, `== "on"`/`!= "on"` comparison, `appr_lbl(...)` call, and callback-data string byte-for-byte. In build_settings_keyboard do NOT restructure the button list or callback-data (D-12). For the toggle handlers (registration_mode :692, pending_notify_mode :833, reg_bonus_enabled :847) and the party gate (:2229), migrate only the read used to compute display/branch; the `set_setting` write path is untouched. Tag each with `# REG-02:`. Remove now-dead literal defaults only where they were inline in the migrated read (do not touch unrelated defaults).</action>
  <acceptance_criteria>
    - Specific per-key assertions — each MUST return a hit: `grep -n 'get_setting_typed("full_approval")' handlers/admin.py`, `grep -n 'get_setting_typed("registration_mode")' handlers/admin.py`, `grep -n 'get_setting_typed("pending_notify_mode")' handlers/admin.py`, `grep -n 'get_setting_typed("party_enabled")' handlers/admin.py`, `grep -n 'get_setting_typed("reg_bonus_enabled")' handlers/admin.py`.
    - `grep -c "get_setting_typed" handlers/admin.py` (excluding comments) >= 24 (every migrated read: 10 render + 14 keyboard + 4 handler/gate reads, minus shared where applicable).
    - `grep -n "or \"batched\"\|or \"manual\"\|or \"short\"\|or \"off\"\|or \"on\"" handlers/admin.py` shows NONE of the migrated feature-switch reads still using the inline `or "<literal>"` idiom in render_settings_text/build_settings_keyboard/the three toggle handlers (the literal now lives in the registry).
    - The bespoke callback-data strings (settings_toggle_reg, toggle_party_enabled, toggle_payment_enabled, ...) are unchanged (grep confirms they still exist).
    - `python -m pytest tests/test_settings_groups_c0x.py -q` exits 0 (landing + toggle-button snapshots and all prior tests green).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py -q</automated>
  </verify>
  <done>Admin feature-switch reads flow through the registry; the settings landing text and toggle buttons render byte-identically; button structure/callback-data untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot_settings -> admin render/keyboard reads | Feature-switch values shown to the admin and used to compute toggle-button state; a wrong default mislabels a live feature's on/off state. |
| admin toggle write path | set_setting writes gated by ADMIN_IDS; unchanged here — only the preceding read moves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | Data integrity | A migrated read returning a different default flips a toggle-button label / a module on-off state for live admins | mitigate | enum get_setting_typed returns exactly `(get_setting(k) or default)`; test_settings_landing_text_snapshot + test_settings_toggle_button_snapshot lock the byte-for-byte output over default + mixed cases. |
| T-06-20 | Correctness | Accidentally refactoring the bespoke callback-data during the read swap (D-12 violation) breaks the toggle handlers | mitigate | Acceptance greps for the exact callback-data strings; snapshot asserts their positions; action explicitly forbids restructuring. |
| T-06-21 | Elevation of privilege | Toggle write path | accept | Only reads change; set_setting + ADMIN_IDS gate untouched; covered by existing admin-rejection tests. |
</threat_model>

<verification>
- `python -m pytest tests/test_settings_groups_c0x.py -q` green.
- `python -c "import handlers.admin, main"` exits 0.
- grep confirms callback-data strings and button structure unchanged.
</verification>

<success_criteria>
- REG-02: admin feature-switch reads sourced from the registry, byte-for-byte.
- Locked decision honored: hardcoded button reads absorbed (values), button structure preserved (D-12).
- Automated toggle-button snapshot now guards the previously un-snapshotted block (partial WARNING-3 automation; manual smoke in 06-07 remains mandatory).
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-schema-registry/06-05-SUMMARY.md` listing each migrated admin read, confirming the callback-data strings are unchanged, and the snapshot results.
</output>
