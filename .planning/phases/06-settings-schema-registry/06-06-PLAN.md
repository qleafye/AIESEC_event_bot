---
phase: 06-settings-schema-registry
plan: 06
type: execute
wave: 4
depends_on: ["06-03", "06-04"]
files_modified:
  - handlers/registration.py
  - handlers/payment.py
  - services/scheduler.py
  - tests/test_settings_consumers_phase6.py
autonomous: true
requirements: [REG-02]
must_haves:
  truths:
    - "The registration-flow feature-switch gates (edu_conditional, reg_university_mode, party_fork_question, party_enabled, reg_bonus_enabled, reg_show_progress, registration_mode, short_approval, pending_notify_mode) read through get_setting_typed, byte-for-byte with their old `get_setting(k) or '<default>'` / `== 'on'` idiom"
    - "handlers/payment.py's payment_enabled gate reads through get_setting_typed"
    - "services/scheduler.py's payment_reminders_enabled gate (both fire-time checks) reads through get_setting_typed"
    - "Sites that intentionally read raw WITHOUT a default (registration.py:1831 registration_mode, :2379 party_approval feeding _decide_status's own `or 'manual'`) are handled so the final behavior is provably identical"
  artifacts:
    - path: "handlers/registration.py"
      provides: "reg-flow feature-switch gates migrated to get_setting_typed"
    - path: "handlers/payment.py"
      provides: "payment_enabled gate via get_setting_typed"
    - path: "services/scheduler.py"
      provides: "payment_reminders_enabled gates via get_setting_typed"
    - path: "tests/test_settings_consumers_phase6.py"
      provides: "gate behavior-preservation tests for the migrated feature-switch reads"
  key_links:
    - from: "handlers/registration.py feature-switch gates"
      to: "settings_schema.get_setting_typed"
      via: "typed enum read replacing `get_setting(k) or default`"
      pattern: "get_setting_typed"
    - from: "services/scheduler.py::payment reminder gates"
      to: "settings_schema.get_setting_typed"
      via: "typed read of payment_reminders_enabled"
      pattern: "get_setting_typed"
---

<objective>
Migrate the behavioral feature-switch gates in handlers/registration.py, handlers/payment.py, and services/scheduler.py from the hardcoded `get_setting(k) or "<default>"` / `== "on"` idiom to `get_setting_typed`, completing the locked-decision requirement that the toggle wave absorb every hardcoded feature-switch read — byte-for-byte (REG-02, SC#2).

Purpose: These gates decide whether questions appear, whether the party fork shows, whether payment/consent modules run, and whether reminders fire — the highest-stakes reads in the app. Centralizing them on the registry finishes the "one file" promise and removes the last hardcoded defaults. Byte-for-byte preservation is enforced per key.
Output: migrated gates + a behavior-preservation test file for each affected key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** DXP manager, **I want to** the bot's registration/payment/reminder logic to read every feature switch from the one registry file, **so that** on/off/default behavior is consistent with the admin screen and a change is a one-line registry edit — with the ~590 live delegates experiencing no change in which questions, tracks, or reminders they get.

<context>
@.planning/phases/06-settings-schema-registry/06-CONTEXT.md
@.planning/phases/06-settings-schema-registry/06-PATTERNS.md
@.planning/phases/06-settings-schema-registry/06-04-SUMMARY.md

<interfaces>
Consumer feature-switch gates to migrate (read current state first; replace ONLY the value read, keep the
`!= "on"` / `== "on"` / string comparison so behavior is byte-identical — get_setting_typed(enum) returns the
same string `(get_setting(k) or default)` would, incl. empty-string):
  handlers/registration.py:
    :438  edu_conditional     `(await get_setting("edu_conditional") or "on") == "on"`
    :582  reg_university_mode `mode = await get_setting("reg_university_mode") or "text"`
    :882  party_fork_question `(await get_setting("party_fork_question") or "off") != "on"`
    :885  party_enabled       `(await get_setting("party_enabled") or "off") != "on"`
    :1445 reg_bonus_enabled   `await get_setting("reg_bonus_enabled") == "on"`  (no `or`; "off" default reproduces)
    :1520 reg_show_progress   `(await get_setting("reg_show_progress") or "off") == "on"`
    :1522 party_enabled       `(await get_setting("party_enabled") or "off") != "on"`
    :1831 registration_mode   `mode = await get_setting("registration_mode")`  (RAW, no default — see rule below)
    :2219 registration_mode   `reg_mode = await get_setting("registration_mode") or "short"`
    :2377 short_approval      `short_setting = await get_setting("short_approval") or "auto"`
    :2379 party_approval      `party_setting = await get_setting("party_approval")`  (RAW; _decide_status does `or "manual"`)
    :2379-area pending_notify_mode `(await get_setting("pending_notify_mode") or "batched") == "instant"`
  handlers/payment.py:
    :88   payment_enabled     `(await get_setting("payment_enabled") or "off") != "on"`
  services/scheduler.py:
    :277  payment_reminders_enabled `(await get_setting("payment_reminders_enabled") or "on") != "on"`
    :325  payment_reminders_enabled `(await get_setting("payment_reminders_enabled") or "on") == "on"`

RAW-read rule (:1831 registration_mode, :2379 party_approval): these read WITHOUT an inline default. Before
migrating, READ the downstream usage. If the downstream applies its own default equivalent to the registry
default (e.g. _decide_status does `party_setting or "manual"` — passing "manual" instead of None is identical),
migrate to get_setting_typed. If passing the resolved default instead of None would change a branch, LEAVE the
raw `get_setting` read untouched and add a `# REG-02: left raw — downstream applies its own default` comment.
Do NOT guess — verify each of these two sites against its consumer.

get_setting_typed lives in settings_schema.py; enum entries + defaults added in 06-04.
Import via `from settings_schema import get_setting_typed` (watch cycle direction — settings_schema imports
database.db only; use the function-local lazy import per CONVENTIONS.md only if a cycle actually appears).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing gate behavior-preservation tests</name>
  <files>tests/test_settings_consumers_phase6.py</files>
  <read_first>
    - tests/test_settings_consumers_phase6.py (from 06-03 — extend; asyncio.run + tmp_path scaffold)
    - handlers/registration.py:436-440, 580-585, 880-895, 1443-1447, 1518-1523, 2215-2225, 2373-2381
    - handlers/payment.py:85-92, services/scheduler.py:273-280, 321-327
    - settings_schema.py (get_setting_typed, enum entries)
  </read_first>
  <behavior>
    - test_edu_conditional_gate_equiv: for edu_conditional in {unset, "", "on", "off"}, the boolean the gate computes via get_setting_typed matches `(get_setting or "on") == "on"`. Include the empty-string case (default "on" -> True) — the exact trap the enum type fixes.
    - test_party_enabled_gate_equiv / test_payment_enabled_gate_equiv / test_payment_reminders_gate_equiv: for each key over {unset, "", "on", "off"}, the `!= "on"` / `== "on"` result via the registry read matches the old `(get_setting or "<default>") <cmp> "on"`.
    - test_registration_mode_and_reg_university_mode_equiv: the resolved string via get_setting_typed equals `(get_setting or "short")` / `(get_setting or "text")` over {unset, "", "full"/"list"}.
    - test_raw_read_sites_preserved: assert the behavior at registration.py:1831 and :2379 is unchanged (whichever path the executor chose — migrated-with-equivalent-default OR left-raw — the downstream branch result is identical for {unset, "", value}).
  </behavior>
  <action>Extend tests/test_settings_consumers_phase6.py (created in 06-03). Drive each gate through a real tmp DB (set_setting the raw, then read via get_setting_typed) and assert the resolved bool/string equals the frozen old-idiom oracle computed inline. Cover None + "" + on/off/value for every key — the empty-string case is mandatory (proves the enum `or`-idiom equivalence). Tests fail now because the consumers still use the local idiom OR (if written as "consumer invokes get_setting_typed" via monkeypatch) because the gates are not yet migrated.</action>
  <acceptance_criteria>
    - The listed test functions exist in tests/test_settings_consumers_phase6.py.
    - `python -m pytest tests/test_settings_consumers_phase6.py -k "gate_equiv or mode_equiv or raw_read" -q` FAILS before Task 2 (RED).
    - Every key's matrix includes the None AND "" inputs.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_consumers_phase6.py -k "gate_equiv or mode_equiv or raw_read" -q</automated>
  </verify>
  <done>Gate behavior tests fail until the reg/payment/scheduler gates are migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the reg/payment/scheduler feature-switch gates to get_setting_typed</name>
  <files>handlers/registration.py, handlers/payment.py, services/scheduler.py</files>
  <read_first>
    - each cited line range in handlers/registration.py, handlers/payment.py, services/scheduler.py (interfaces block)
    - settings_schema.py (get_setting_typed, enum defaults from 06-04)
    - handlers/registration.py:1826-1835 and :2373-2381 (verify the two RAW-read sites' downstream before deciding)
  </read_first>
  <action>At each site in the interfaces block, replace `await get_setting("<key>") or "<default>"` (or the bare `await get_setting("<key>") == "on"` at :1445) with `await get_setting_typed("<key>")`, keeping the surrounding `!= "on"` / `== "on"` / string-assignment / `appr` logic byte-identical. Import get_setting_typed at module top in each file (lazy-import inside the function only if an actual import cycle surfaces, per CONVENTIONS.md). For the two RAW-read sites (registration.py:1831 registration_mode, :2379 party_approval): apply the RAW-read rule from the interfaces block — verify the downstream, migrate only if provably identical, else leave raw with the documented comment. Tag every migrated site with `# REG-02:`. Do not touch the date/int consumer sites already migrated in 06-03.</action>
  <acceptance_criteria>
    - `grep -c "get_setting_typed" handlers/registration.py` (excluding comments) >= 9; `grep -c "get_setting_typed" handlers/payment.py` >= 1; `grep -c "get_setting_typed" services/scheduler.py` >= 2.
    - No migrated feature-switch site still uses `await get_setting("<key>") or "<literal>"` for the keys in the interfaces block (grep the specific keys).
    - `python -m pytest tests/test_settings_consumers_phase6.py -q` exits 0.
    - `python -m pytest tests/test_registration_phase4.py tests/test_registration_phase5.py tests/test_payment_phase5.py -q` exits 0 (no behavioral regression in the gated flows).
    - `python -c "import handlers.registration, handlers.payment, services.scheduler, main"` exits 0 (no cycle).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_settings_consumers_phase6.py tests/test_registration_phase4.py tests/test_registration_phase5.py tests/test_payment_phase5.py -q</automated>
  </verify>
  <done>Every reg/payment/scheduler feature-switch gate reads through the registry, byte-for-byte; the two raw-read sites handled provably; gated flows unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot_settings -> behavioral gates | These reads decide question visibility, party fork, payment/consent modules, and reminder firing — the highest-consequence reads in the bot. |
| scheduler fire-time gate | payment_reminders_enabled is re-read at fire time so the admin toggle takes effect live; the migration must preserve that timing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-22 | Data integrity | A migrated gate flipping which questions/tracks a live delegate sees (esp. default-"on" edu_conditional empty-string case) | mitigate | enum get_setting_typed reproduces `(get_setting or default)` incl. empty-string; per-key gate tests cover None/""/on/off; registration_phase4/5 exercise the flows. |
| T-06-23 | Availability | payment/consent/reminder module silently disabled by a wrong default | mitigate | Defaults pinned + verified in 06-04; payment_phase5 + scheduler tests run in acceptance; gate tests assert the `!= "on"` result identical. |
| T-06-24 | Correctness | A RAW-read site (registration_mode:1831 / party_approval:2379) migrated naively changes a None-dependent branch | mitigate | Explicit RAW-read rule: verify downstream, migrate only if provably identical else leave raw; test_raw_read_sites_preserved guards the branch result. |
| T-06-25 | Availability | new settings_schema import into widely-imported registration.py creating a cycle | mitigate | one-directional import (settings_schema -> database.db only); import smoke; lazy-import fallback available. |
</threat_model>

<verification>
- `python -m pytest tests/test_settings_consumers_phase6.py tests/test_registration_phase4.py tests/test_registration_phase5.py tests/test_payment_phase5.py -q` green.
- Any scheduler test suite green (no reminder-firing regression).
- `python -c "import handlers.registration, handlers.payment, services.scheduler, main"` exits 0.
</verification>

<success_criteria>
- REG-02 complete: EVERY feature-switch consumer read (admin from 06-05 + reg/payment/scheduler here) flows through the registry, byte-for-byte.
- Locked decision fully honored: all hardcoded button/gate reads absorbed (06-DISCUSSION-LOG.md:48).
- The two raw-read edge sites handled provably, not guessed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-schema-registry/06-06-SUMMARY.md` listing each migrated gate, the decision taken for each of the two raw-read sites (migrated vs left-raw + why), whether any lazy import was needed, and the gate-equivalence results.
</output>
