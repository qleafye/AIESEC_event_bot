---
phase: 06-settings-schema-registry
plan: 07
type: execute
wave: 5
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05", "06-06"]
files_modified: []
autonomous: false
requirements: [REG-01, REG-02, REG-03]
must_haves:
  truths:
    - "On a real bot instance the ⚙️ Настройки forum screen and every migrated group sub-screen render exactly as before the migration"
    - "The toggle-button block (button texts + callback-data behavior) is confirmed identical before/after via an explicit comparison, not a best-effort screenshot check"
    - "Editing a setting in each migrated group persists and re-renders correctly, sourced from the registry"
    - "No live setting value was reset by the migration"
  artifacts:
    - path: ".planning/phases/06-settings-schema-registry/06-SMOKE-CHECKLIST.md"
      provides: "recorded manual smoke result per migrated group + toggle-button comparison"
  key_links:
    - from: "admin settings UI"
      to: "settings_schema.SETTINGS_SCHEMA"
      via: "live render + edit round-trip"
      pattern: "manual"
---

<objective>
Consolidated manual smoke verification (D-18) of the whole migrated settings surface on a real bot instance, after all four automated regression layers (parse-equivalence, render snapshot, coverage, consumer behavior) plus the admin toggle-button snapshot are green. The env has no CI/linter, so a human must confirm the live admin screens (prior-phase deferred-UAT pattern).

Purpose: Final gate that the byte-for-byte automated proofs hold on the real Telegram UI — including the toggle-button block whose bespoke callback-data strings have no analog to auto-generate against — and that no live setting silently reset for the ~590 users.
Output: a recorded smoke checklist with an explicit before/after toggle-button comparison.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-settings-schema-registry/06-01-SUMMARY.md
@.planning/phases/06-settings-schema-registry/06-02-SUMMARY.md
@.planning/phases/06-settings-schema-registry/06-03-SUMMARY.md
@.planning/phases/06-settings-schema-registry/06-04-SUMMARY.md
@.planning/phases/06-settings-schema-registry/06-05-SUMMARY.md
@.planning/phases/06-settings-schema-registry/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated regression net and record results</name>
  <files>.planning/phases/06-settings-schema-registry/06-SMOKE-CHECKLIST.md</files>
  <read_first>
    - tests/test_settings_groups_c0x.py, tests/test_settings_consumers_phase6.py
    - all six 06-0N-SUMMARY.md files
  </read_first>
  <action>Run the complete relevant test suite and capture the pass counts into a new 06-SMOKE-CHECKLIST.md: `python -m pytest tests/test_settings_groups_c0x.py tests/test_settings_consumers_phase6.py tests/test_registration_phase4.py tests/test_registration_phase5.py tests/test_payment_phase5.py -q` plus a full `python -m pytest -q` to confirm no cross-suite regression. Record totals and any skips. Then write the human smoke steps (Task 2) into the same file as an unchecked checklist.</action>
  <acceptance_criteria>
    - `python -m pytest tests/test_settings_groups_c0x.py tests/test_settings_consumers_phase6.py tests/test_registration_phase4.py tests/test_registration_phase5.py tests/test_payment_phase5.py -q` exits 0.
    - `python -m pytest -q` exits 0 (no unrelated regression).
    - 06-SMOKE-CHECKLIST.md exists with recorded pass counts and the human smoke checklist.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>Full suite green; checklist file created with automated results and pending manual steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The settings surface (event, reg, pay, party, consent groups + reg_q toggles + feature-switch enums) is now registry-driven across admin render, toggle buttons, and reg/payment/scheduler gates; all automated regression layers + the admin toggle-button snapshot are green.</what-built>
  <how-to-verify>
    On a real bot instance with a copy of (or the live) bot_settings data, as an admin:
    1. Open /admin → ⚙️ Настройки форума. Confirm the landing screen text (all «📝 Форма регистрации», «✅ Модерация…», «🎉 Трек вечеринки», «⏰ Автонапоминания…» lines) reads exactly as before.
    2. MANDATORY toggle-button comparison: for EVERY button in the settings keyboard (📝 Регистрация toggle, 🎁 Бонус, ✅ Полная/Краткая форма, 🔔 Уведомление, 💳 Оплата, ⏰ Автонапоминания, 📋 Согласия, 🏫 ВУЗ, 🎓 ВУЗ/курс, 🔢 Нумерация, 🎉 Трек вечеринки, 🔀 Вопрос-развилка, ✅ Модерация вечеринки), record the button TEXT before and after the deploy and confirm each is identical, AND tap each toggle once to confirm its callback still flips the setting and the button text updates correctly. This is a required step, not "compare if a screenshot exists".
    3. Open each migrated group sub-screen: 🎪 Событие/Медиа, 📝 Регистрация, 💳 Оплата, 🎉 Party, 📋 Согласия. Confirm field labels, order, «✏️ задано / — не задано / по умолчанию» flags, and the «── не настроено ──» collapse all match prior behavior.
    4. Edit one setting in each group (e.g. 🗓 Дата, a payment field, a party text). Confirm it saves and the flag flips to «задано».
    5. Confirm party_closed_text / party_sheet_tab still show «по умолчанию» when unset.
    6. Flip a reg_q_* question in 📋 Вопросы регистрации; confirm it toggles and the default-off/on question set is unchanged.
    7. Confirm the pending-application reminder still fires on its interval and payment-deadline / payment-reminders timing is unchanged (spot check or confirm no error in logs).
    8. Restart the bot; confirm NO setting reset to blank/default.
    Record pass/fail per step (including the per-button comparison table) in 06-SMOKE-CHECKLIST.md.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe the drift/regression observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live bot_settings data -> migrated render/parse/gates | Final human confirmation that no live value drifted or reset on the real UI, including the toggle-button block with no auto-generatable analog. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Data integrity | A drift invisible to automated snapshots (locale/emoji rendering, restart-persistence, live toggle callback) reaching live users | mitigate | Human smoke on a real instance across every migrated group + a MANDATORY per-button before/after comparison + tap-through + restart-persistence check; blocking gate before phase close. |
| T-06-17 | Elevation of privilege | Admin gate regression | accept | No code changed in this plan; the ADMIN_IDS gate was untouched across 06-01..06-06 and is covered by test_show_settings_group_rejects_non_admin. |
</threat_model>

<verification>
- Full `python -m pytest -q` green.
- Human sign-off recorded in 06-SMOKE-CHECKLIST.md across all migrated groups, the mandatory per-toggle-button comparison, and restart-persistence.
</verification>

<success_criteria>
- All four regression layers (D-15 parse-equivalence, D-16 render snapshot, D-17 coverage, consumer behavior) + admin toggle-button snapshot green.
- D-18 manual smoke passed on a real instance for every migrated group AND the toggle-button block (explicit before/after per button).
- No live setting reset; REG-01/REG-02/REG-03 confirmed end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-schema-registry/06-07-SUMMARY.md` summarizing the automated totals and the human smoke outcome per group + the toggle-button comparison table, and note the phase is ready for verify-phase.
</output>
