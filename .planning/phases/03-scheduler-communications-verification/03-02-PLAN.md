---
phase: 03-scheduler-communications-verification
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - services/scheduler.py
  - database/db.py
  - handlers/admin.py
  - tests/test_nudge_phase3.py
requirements: [SCHED-03]
must_haves:
  truths:
    - "A user who starts registration and abandons it for ~2h receives exactly one reminder nudge"
    - "A user nudged once is never nudged again (nudged_at one-shot dedup)"
    - "Already-registered users are never nudged (their reg_started row was cleared on completion)"
  artifacts:
    - path: "database/db.py"
      provides: "reg_started.nudged_at column + get_nudge_candidates + mark_nudged"
      contains: "nudged_at"
    - path: "services/scheduler.py"
      provides: "nudge_incomplete_registrations interval job registered in init_scheduler"
      contains: "nudge_incomplete_registrations"
    - path: "tests/test_nudge_phase3.py"
      provides: "pure-helper tests for cutoff format + nudge settings parsing"
  key_links:
    - from: "services/scheduler.py init_scheduler"
      to: "nudge_incomplete_registrations interval job"
      via: "add_job(...,'interval', minutes=nudge_scan_minutes, id='nudge_scan', replace_existing=True)"
      pattern: "interval"
    - from: "nudge_incomplete_registrations"
      to: "database/db.py get_nudge_candidates / mark_nudged"
      via: "scan reg_started older than cutoff and nudged_at IS NULL, send, mark"
      pattern: "get_nudge_candidates"
---

<objective>
Deliver SCHED-03: an automatic one-shot dropout nudge. An APScheduler interval job scans the Phase-1 `reg_started` table every `nudge_scan_minutes` (~15 min) for rows older than ~2h with `nudged_at IS NULL`, sends exactly one reminder, and stamps `nudged_at` so the user is never nudged twice.

Purpose: Recover abandoned registrations without spamming. Reuses the single `AsyncIOScheduler` from Plan 01 (D-14 — no second scheduling mechanism) and the existing `reg_started` table (D-15 — do NOT create a duplicate table).

Output: `reg_started.nudged_at` migration + scan/mark helpers in `database/db.py`, the `nudge_incomplete_registrations` interval job in `services/scheduler.py`, nudge settings in the `/settings_guide`, and pure-helper tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduler-communications-verification/03-RESEARCH.md
@.planning/phases/03-scheduler-communications-verification/03-PATTERNS.md
@.planning/phases/03-01-PLAN.md is the upstream scheduler (see services/scheduler.py)

<interfaces>
<!-- From Plan 01 (already merged in Wave 1): -->
services/scheduler.py: `_scheduler`, `_bot`, `init_scheduler(bot)`, `_int_or_default(raw, default)`, `_fmt_dt(dt)`, per-iteration fail-soft discipline. init_scheduler has a marked insertion point for the nudge interval job.

Existing reg_started helpers (database/db.py:403-425): mark_reg_started, clear_reg_started, get_incomplete_user_ids. reg_started columns: telegram_id (PK), username, started_at ("%Y-%m-%d %H:%M:%S", db.py:404). Completion already calls clear_reg_started (Phase 1) so registered users self-exclude (D-16).
_ensure_column(db, table, column, definition) (db.py:11-19). get_setting/set_setting (db.py:109-124).
APPROVAL_SETTINGS_DOC list + _render_settings_guide (admin.py:1412-1437) — the /settings_guide registry to extend.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: nudged_at migration + scan/mark helpers + pure cutoff/settings helpers</name>
  <files>database/db.py, services/scheduler.py, tests/test_nudge_phase3.py</files>
  <read_first>
    - database/db.py:11-19 (_ensure_column), :59-76 (existing Phase-1 _ensure_column call cluster — add the new call alongside), :403-425 (reg_started helpers — copy the connection idiom)
    - 03-RESEARCH.md:409-427 (get_nudge_candidates + mark_nudged reference); 03-PATTERNS.md:189-213
    - services/reminders.py:17-28 (settings-parse helper shape)
    - tests/test_reminders_phase2.py (pure-sync test convention)
  </read_first>
  <behavior>
    - _nudge_cutoff(datetime(2026,7,1,12,0,0), 120) == "2026-07-01 10:00:00" (now minus minutes, ISO formatted, lexicographic-safe)
    - _nudge_enabled(None) is True ; _nudge_enabled("on") is True ; _nudge_enabled("off") is False
    - _int_or_default reused from Plan 01 for nudge_after_minutes / nudge_scan_minutes (default 120 / 15)
  </behavior>
  <action>
    In `database/db.py` `init_db()` add `await _ensure_column(db, "reg_started", "nudged_at", "TEXT")` alongside the existing Phase-1 `_ensure_column` calls (db.py:59-76) — additive, safe (D-15). Do NOT create a new table.
    Add `get_nudge_candidates(cutoff: str) -> list[int]` per 03-RESEARCH.md:412-419: `SELECT telegram_id FROM reg_started WHERE started_at < ? AND nudged_at IS NULL` (lexicographic `<` valid because started_at is ISO, db.py:404). Add `mark_nudged(telegram_id: int)` per RESEARCH.md:421-425: `UPDATE reg_started SET nudged_at = ? WHERE telegram_id = ?` with `datetime.now().strftime("%Y-%m-%d %H:%M:%S")`. Both use the db.py:403-425 connection idiom and `?` binds.
    In `services/scheduler.py` add PURE helpers: `_nudge_cutoff(now, minutes)` = `(now - timedelta(minutes=minutes)).strftime("%Y-%m-%d %H:%M:%S")`, and `_nudge_enabled(raw)` = `raw != "off"` (mirror reminders.py:17-19). Reuse `_int_or_default` from Plan 01 for thresholds.
    Create `tests/test_nudge_phase3.py` (pure-sync, mirror test_reminders_phase2.py) asserting the Behavior cases for `_nudge_cutoff` and `_nudge_enabled`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_nudge_phase3.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_nudge_phase3.py` passes (exit 0).
    - `database/db.py` contains `_ensure_column(db, "reg_started", "nudged_at", "TEXT")` and a `get_nudge_candidates` whose WHERE includes `nudged_at IS NULL`.
    - No new table named other than reg_started is created for dropout tracking (`grep -c "CREATE TABLE IF NOT EXISTS reg_in_progress" database/db.py` == 0).
    - `_nudge_cutoff` is pure (no DB/async) and importable standalone.
  </acceptance_criteria>
  <done>nudged_at column + scan/mark helpers live; pure cutoff/settings helpers tested green.</done>
</task>

<task type="auto">
  <name>Task 2: nudge_incomplete_registrations interval job + settings-guide entries</name>
  <files>services/scheduler.py, handlers/admin.py</files>
  <read_first>
    - services/scheduler.py (Plan 01 output — init_scheduler insertion point comment; send_scheduled_broadcast as the module-level-job + fail-soft model; confirm init_scheduler can await get_setting in the async startup)
    - services/reminders.py:31-49 (per-iteration + per-send fail-soft try/except discipline)
    - handlers/admin.py:1380-1393 (_welcome_flipped 429-safe send to reuse for the nudge sends)
    - handlers/admin.py:1412-1437 (APPROVAL_SETTINGS_DOC + _render_settings_guide to extend)
    - 03-RESEARCH.md:216-231 (interval job registration), :355-356 (settings keys)
  </read_first>
  <action>
    Add module-level async job `nudge_incomplete_registrations()` to `services/scheduler.py` (importable, picklable, NO args — D-14). Body (fail-soft per reminders.py:47-48): read `nudge_enabled` via `_nudge_enabled(await get_setting("nudge_enabled"))` — if disabled, return; read `nudge_after_minutes` via `_int_or_default(await get_setting("nudge_after_minutes"), 120)`; compute `cutoff = _nudge_cutoff(datetime.now(), after_minutes)`; `candidates = await get_nudge_candidates(cutoff)`; for each id send `await get_setting("nudge_text") or <default RU nudge>` via `_bot.send_message`, using the 429-safe pattern from _welcome_flipped (sleep retry_after+1, single retry, fail-soft log); on a successful send call `await mark_nudged(id)` (one-shot dedup — D-14). Pace with `asyncio.sleep(0.05)`. Import db helpers inside the function (avoid circulars).
    In `init_scheduler`, read `scan_minutes = _int_or_default(await get_setting("nudge_scan_minutes"), 15)` at the top of the function (init_scheduler runs inside the async startup, so it can await get_setting; if Plan 01 left init_scheduler synchronous, make it async / await the read in the same async startup context). Then at the marked insertion point register the interval job: `_scheduler.add_job(nudge_incomplete_registrations, "interval", minutes=scan_minutes, id="nudge_scan", replace_existing=True)`. The scan cadence is now DRIVEN by `nudge_scan_minutes` (default 15, applied at startup) — the setting is no longer inert. The per-run threshold `nudge_after_minutes` stays settings-driven at job run time. `replace_existing=True` prevents duplicate jobs after a restart (RESEARCH.md:216-220).
    Extend `APPROVAL_SETTINGS_DOC` (admin.py:1412-1421) with: `("nudge_enabled","Авто-напоминание о дорегистрации (on/off)","on")`, `("nudge_after_minutes","Через сколько минут бездействия слать напоминание","120")`, `("nudge_scan_minutes","Период сканирования дропаутов, мин (вступает в силу после перезапуска)","15")`, `("nudge_text","Текст напоминания о дорегистрации","(стандартный)")` so /settings_guide documents them (D-16 carryover).
  </action>
  <verify>
    <automated>python -c "import services.scheduler as s; assert hasattr(s,'nudge_incomplete_registrations')"</automated>
  </verify>
  <acceptance_criteria>
    - `nudge_incomplete_registrations` is a module-level async function taking no arguments (picklable target, D-14).
    - `init_scheduler` registers exactly one interval job with `id="nudge_scan"`, `replace_existing=True`, and `minutes` driven by `_int_or_default(await get_setting("nudge_scan_minutes"), 15)` — `nudge_scan_minutes` is NOT inert (`grep -c "nudge_scan_minutes" services/scheduler.py` >= 1 and `grep -c "nudge_scan" services/scheduler.py` >= 1).
    - The job calls `mark_nudged` only after a successful send (one-shot dedup).
    - `APPROVAL_SETTINGS_DOC` contains `nudge_enabled`, `nudge_after_minutes`, `nudge_scan_minutes`, `nudge_text` (`grep -c "nudge_" handlers/admin.py` >= 4).
    - `python -m pytest tests/ -q` green.
    - MANUAL (flag in SUMMARY): start+abandon a registration, age the reg_started row past threshold, confirm exactly one nudge and that registered users get none (03-VALIDATION.md Manual-Only).
  </acceptance_criteria>
  <done>Interval nudge job runs on the Plan-01 scheduler at the `nudge_scan_minutes` cadence, sends one-shot nudges to aged incomplete registrations, and is documented in /settings_guide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interval job → users | scheduled job sends to a DB-derived candidate set, no user input |
| settings (bot_settings) → job behavior | admin-set thresholds/text drive the job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04 | Denial of Service | nudge job re-sending to the same users every scan | mitigate | `nudged_at` one-shot stamp + `WHERE nudged_at IS NULL` + `coalesce=True` (Plan 01 job_defaults) collapse missed runs |
| T-3-05 | Tampering | nudge_text echoed to users | mitigate | Text is admin-controlled (bot_settings), not end-user input; sent as plain message; no SQL interpolation (parameterized helpers) |
| T-3-15 | Information Disclosure | nudging an already-registered user | accept | Completion clears reg_started (Phase 1) so candidates naturally exclude registered users (D-16); residual risk negligible |
</threat_model>

<verification>
- `python -m pytest tests/ -q` green.
- reg_started reused (no duplicate table); nudged_at column present.
- nudge job is a no-arg module-level coroutine on the single AsyncIOScheduler; scan cadence read from nudge_scan_minutes.
- MANUAL: aged dropout gets exactly one nudge; registered users none.
</verification>

<success_criteria>
SCHED-03 met: a stalled registration (>~2h, nudged_at NULL) receives exactly one automatic nudge via the shared scheduler at the `nudge_scan_minutes` cadence; already-registered users are never nudged.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduler-communications-verification/03-02-SUMMARY.md`.
</output>
