---
phase: 04-universal-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/db.py
  - handlers/states.py
autonomous: true
requirements:
  - PAY-05
  - CONS-01
must_haves:
  truths:
    - "After init_db(), users table has columns payment_status (DEFAULT 'not_paid'), payment_option, receipt_file_id, payment_due, paid_at — existing rows keep their current data unchanged"
    - "After init_db(), user_consents table exists with columns id, user_id, consent_key, accepted_at and UNIQUE(user_id, consent_key) constraint plus an index on user_id"
    - "Registration.consent_pending, Registration.receipt_upload, Registration.payment_option are importable FSM states"
    - "ReceiptReview.reject_reason is importable from handlers.states"
    - "record_user_consent, get_receipt_pending_users, get_receipt_pending_count, update_payment_status are importable from database.db"
  artifacts:
    - path: "database/db.py"
      provides: "Phase 4 additive migrations + Phase 4 DB helper functions"
      contains: "user_consents"
    - path: "handlers/states.py"
      provides: "FSM states for consent step and payment FSM"
      exports: ["Registration.consent_pending", "Registration.receipt_upload", "Registration.payment_option", "ReceiptReview"]
  key_links:
    - from: "handlers/registration.py"
      to: "database/db.py"
      via: "record_user_consent(user_id, consent_key)"
      pattern: "record_user_consent"
    - from: "handlers/admin.py"
      to: "database/db.py"
      via: "get_receipt_pending_users(limit), update_payment_status(telegram_id, status)"
      pattern: "get_receipt_pending"
---

## Phase Goal

**As a** DXP manager, **I want to** toggle consent and payment modules in the admin panel and have them activate immediately for new registrations, **so that** the bot works for both free forums and paid conferences without redeploying code.

<objective>
Lay the Phase 4 DB schema and FSM state foundation: additive migrations for 5 payment columns on `users`, a new normalized `user_consents` table, DB helper functions that later plans call, and new FSM states for the consent and payment flows.

Purpose: Every subsequent Phase 4 plan depends on this schema being present. Migrations are idempotent — running init_db() against the production DB (590+ live users) must produce zero data loss or status changes.

Output: Updated database/db.py with Phase 4 migration block + 4 helper functions; updated handlers/states.py with 4 new states across 2 StatesGroups.
</objective>

<execution_context>
@C:/Users/alexe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alexe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-universal-modules/04-CONTEXT.md
@.planning/phases/04-universal-modules/04-PATTERNS.md

<interfaces>
<!-- Existing patterns in database/db.py the executor must replicate exactly. -->

Migration pattern (lines 17–19 — copy verbatim for every new column):
  _ensure_column(db, "users", "column_name", "TYPE DEFAULT value")

Existing Phase 1 migration block style (lines 59–77):
  # Phase 1 migrations (additive, idempotent — safe against ~590 live users)
  await _ensure_column(db, "users", "status", "TEXT DEFAULT 'approved'")

CREATE TABLE IF NOT EXISTS + index style (lines 86–96, coins table):
  await db.execute('''CREATE TABLE IF NOT EXISTS coins (...)''')
  await db.execute('CREATE INDEX IF NOT EXISTS idx_coins_user ON coins(user_id)')

add_user ON CONFLICT DO UPDATE resume_file_id precedent (line 197):
  resume_file_id=COALESCE(excluded.resume_file_id, users.resume_file_id)

Existing async helper signature style:
  async def get_setting(key: str) -> str | None:
      async with aiosqlite.connect(config.DB_PATH) as db:
          ...

Existing StatesGroup pattern (handlers/states.py lines 3–37):
  class Registration(StatesGroup):
      full_name = State()
      ...
      confirm = State()   # last existing state

  class Approval(StatesGroup):
      reason = State()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 4 additive DB migrations — payment columns + user_consents table</name>
  <files>database/db.py</files>
  <read_first>
    - database/db.py lines 1–124 (full init_db, _ensure_column, existing migration blocks — read in one pass)
    - .planning/phases/04-universal-modules/04-PATTERNS.md §"database/db.py — Phase 4 additive migrations" (exact SQL to replicate)
    - .planning/phases/04-universal-modules/04-CONTEXT.md §D-10 (payment column names and types) §D-02 (user_consents schema)
  </read_first>
  <action>
    Inside init_db(), immediately after the Phase 3 `scheduled_broadcasts` CREATE TABLE block (line ~122), before `await db.commit()`, add a clearly labelled "Phase 4 migrations" block.

    Payment columns on users — five _ensure_column calls in this order:
      await _ensure_column(db, "users", "payment_status", "TEXT DEFAULT 'not_paid'")
      await _ensure_column(db, "users", "payment_option", "TEXT")
      await _ensure_column(db, "users", "receipt_file_id", "TEXT")
      await _ensure_column(db, "users", "payment_due", "TEXT")
      await _ensure_column(db, "users", "paid_at", "TEXT")

    Use DEFAULT 'not_paid' on payment_status so existing rows get a defined initial value — no NULL handling needed in handlers. The other four columns have no DEFAULT (NULL is correct sentinel for "not set").

    New user_consents table (CREATE TABLE IF NOT EXISTS — idempotent):
      CREATE TABLE IF NOT EXISTS user_consents (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          user_id INTEGER NOT NULL,
          consent_key TEXT NOT NULL,
          accepted_at TEXT NOT NULL,
          UNIQUE(user_id, consent_key)
      )
    Then: CREATE INDEX IF NOT EXISTS idx_consents_user ON user_consents(user_id)

    Also extend add_user() INSERT column list and ON CONFLICT DO UPDATE SET block to include the five new payment columns. Use COALESCE(excluded.receipt_file_id, users.receipt_file_id) for receipt_file_id (mirrors the resume_file_id precedent at line 197). For payment_status, payment_option, payment_due, paid_at use the plain excluded.col pattern — a re-registration resets payment state intentionally (new application = new payment cycle). Pass None for all five in the VALUES tuple (data.get('payment_status') is None on registration — column DEFAULT fills in 'not_paid').

    Do NOT use ALTER TABLE DROP COLUMN, INSERT OR REPLACE on users, or any destructive migration. Keep the comment "additive, idempotent — safe against ~590 live users" on the Phase 4 block.
  </action>
  <acceptance_criteria>
    - python -m py_compile database/db.py exits 0 (no syntax errors)
    - grep -c "user_consents" database/db.py returns >= 3 (CREATE TABLE + index + at least one function reference added in Task 2)
    - grep -c "payment_status" database/db.py returns >= 2 (_ensure_column call + add_user column list)
    - grep -c "payment_due" database/db.py returns >= 2 (_ensure_column call + add_user column list)
    - grep -c "receipt_file_id.*COALESCE\|COALESCE.*receipt_file_id" database/db.py returns >= 1 (safe re-registration)
    - "Phase 4" comment label present in init_db block
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile database/db.py && python -c "import asyncio, tempfile, os; os.environ.setdefault('DB_PATH', tempfile.mktemp(suffix='.db')); from database.db import init_db; print('compile+import OK')"</automated>
  </verify>
  <done>init_db() contains Phase 4 migration block: 5 _ensure_column calls on users + CREATE TABLE IF NOT EXISTS user_consents with UNIQUE constraint + index. add_user() extended with 5 new columns. No existing migration removed or modified.</done>
</task>

<task type="auto">
  <name>Task 2: DB helper functions for consent + payment + FSM states</name>
  <files>database/db.py, handlers/states.py</files>
  <read_first>
    - database/db.py lines 126–260 (get_setting/set_setting style, add_user style — for async helper shape)
    - handlers/states.py lines 1–59 (full file — existing StatesGroup classes)
    - .planning/phases/04-universal-modules/04-PATTERNS.md §"handlers/states.py" (new states to add)
  </read_first>
  <action>
    In database/db.py, after the existing helper functions (after add_user, get_user, etc.), add four new async helper functions:

    record_user_consent(user_id: int, consent_key: str) -> None
      INSERT OR IGNORE INTO user_consents (user_id, consent_key, accepted_at) VALUES (?, ?, ?)
      accepted_at = datetime.now().isoformat()
      Use INSERT OR IGNORE so re-tapping Принимаю does not raise a UNIQUE error (idempotent).

    get_user_consents(user_id: int) -> list[str]
      SELECT consent_key FROM user_consents WHERE user_id = ? ORDER BY accepted_at
      Returns list of consent_key strings (e.g. ["data_processing", "photo_video"]).

    get_receipt_pending_users(limit: int = 50) -> list[dict]
      SELECT telegram_id, full_name, payment_option, receipt_file_id, payment_status
      FROM users WHERE payment_status = 'receipt_sent'
      ORDER BY rowid LIMIT ?
      Returns list of dicts (use db.row_factory = aiosqlite.Row then [dict(r) for r in rows]).

    get_receipt_pending_count() -> int
      SELECT COUNT(*) FROM users WHERE payment_status = 'receipt_sent'
      Returns int.

    update_payment_status(telegram_id: int, status: str, **kwargs) -> int
      Builds an UPDATE users SET payment_status=? ... WHERE telegram_id=?
      Accepted kwargs: receipt_file_id, paid_at, payment_option, payment_due.
      Returns cursor.rowcount (used by admin atomic confirm guard — Task 05-01).
      If status='paid', set paid_at=datetime.now().isoformat() automatically (caller can override via kwargs).
      Use additive UPDATE, never INSERT OR REPLACE.

    In handlers/states.py, append to the Registration StatesGroup (after the existing `confirm = State()` line):
      consent_pending = State()   # waiting for "Принимаю" callback on a consent card
      receipt_upload  = State()   # waiting for PDF document or photo receipt
      payment_option  = State()   # waiting for user to pick a payment option (inline keyboard)

    Add a new StatesGroup after the existing EditSetting class:
      class ReceiptReview(StatesGroup):
          reject_reason = State()   # admin types rejection reason text (mirrors Approval.reason)
  </action>
  <acceptance_criteria>
    - python -m py_compile handlers/states.py exits 0
    - python -c "from handlers.states import Registration, ReceiptReview; assert hasattr(Registration, 'consent_pending'); assert hasattr(Registration, 'receipt_upload'); assert hasattr(Registration, 'payment_option'); assert hasattr(ReceiptReview, 'reject_reason'); print('States OK')" exits 0 and prints "States OK"
    - python -c "from database.db import record_user_consent, get_user_consents, get_receipt_pending_users, get_receipt_pending_count, update_payment_status; print('Helpers OK')" exits 0 and prints "Helpers OK"
    - grep -c "record_user_consent" database/db.py returns >= 1
    - grep -c "INSERT OR IGNORE INTO user_consents" database/db.py returns >= 1
    - grep -c "payment_status = 'receipt_sent'" database/db.py returns >= 1
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile database/db.py handlers/states.py && python -c "from handlers.states import Registration, ReceiptReview; assert hasattr(Registration, 'consent_pending') and hasattr(Registration, 'receipt_upload') and hasattr(Registration, 'payment_option') and hasattr(ReceiptReview, 'reject_reason'); from database.db import record_user_consent, get_user_consents, get_receipt_pending_users, get_receipt_pending_count, update_payment_status; print('Phase 4 DB+States foundation OK')"</automated>
  </verify>
  <done>Five async helper functions in database/db.py (record_user_consent, get_user_consents, get_receipt_pending_users, get_receipt_pending_count, update_payment_status). Four new FSM states in handlers/states.py: Registration.consent_pending, .receipt_upload, .payment_option and ReceiptReview.reject_reason. All importable without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init_db → live DB | Migration runs against production data/forum.db with ~590 live users |
| user_consents table | Stores PII: timestamped record of which user accepted which consent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | init_db() migration block | mitigate | Use _ensure_column (ADD COLUMN only) + CREATE TABLE IF NOT EXISTS — never DROP COLUMN, never INSERT OR REPLACE on users. Existing ~590 rows must not lose data. Comment "additive, idempotent" labels the block for reviewers. |
| T-04-01-02 | Information Disclosure | user_consents table | mitigate | No logging of consent_key values or user_id in info-level logs (logger.error only on exception). Table is not exposed in any broadcast filter or public query. Index is on user_id only — no reverse lookup by consent_key that would produce a user list. |
| T-04-01-03 | Denial of Service | update_payment_status rowcount guard | mitigate | update_payment_status() returns rowcount so admin confirm handler can detect race (rowcount=0 → already processed). Prevents double-confirmation sends (mirrors APP-06 atomic guard pattern). |
</threat_model>

<verification>
After both tasks complete:
- python -m py_compile database/db.py handlers/states.py
- python -c "from database.db import record_user_consent, get_user_consents, get_receipt_pending_users, get_receipt_pending_count, update_payment_status; from handlers.states import Registration, ReceiptReview; print('All imports OK')"
- grep -c "Phase 4" database/db.py  # should be >= 1
- grep -c "user_consents" database/db.py  # should be >= 3
- grep -c "payment_status" database/db.py  # should be >= 3 (migration + add_user + helper)
</verification>

<success_criteria>
- init_db() is idempotent: running it twice against the same DB produces the same schema with no errors
- All 590+ existing users retain their current data (status, resume_file_id, etc.) — payment_status column populated with DEFAULT 'not_paid'
- user_consents table exists with UNIQUE(user_id, consent_key) enforcing deduplication
- Four helper functions importable from database.db; four new states importable from handlers.states
</success_criteria>

<output>
After completion, create .planning/phases/04-universal-modules/04-01-SUMMARY.md
</output>
