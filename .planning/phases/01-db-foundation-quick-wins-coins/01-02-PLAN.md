---
phase: 01-db-foundation-quick-wins-coins
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - handlers/registration.py
  - handlers/states.py
  - tests/test_registration_phase1.py
autonomous: true
requirements: [QW-01, QW-03, SCHED-02]
must_haves:
  truths:
    - "After filling the full form, the user sees a summary of all answers with a 'Всё верно ✓' / 'Изменить' keyboard before finalization"
    - "'Изменить' restarts the whole registration flow (no per-field edit); 'Всё верно ✓' finalizes"
    - "Short form (name-only) finalizes without a confirm step"
    - "When the resume step is enabled it is mandatory, accepts PDF/DOCX only, and a wrong file type re-prompts without crashing; only file_id is stored"
    - "Starting registration writes a reg_started row; finishing deletes it"
  artifacts:
    - path: "handlers/registration.py"
      provides: "Confirm step, resume step, reg_started lifecycle hooks"
      contains: "Registration.confirm"
    - path: "handlers/states.py"
      provides: "Registration.confirm and Registration.resume FSM states"
      contains: "confirm = State()"
    - path: "tests/test_registration_phase1.py"
      provides: "Pure-helper tests for summary builder and resume file-type validation"
      min_lines: 30
  key_links:
    - from: "registration.py _advance (last step)"
      to: "Registration.confirm"
      via: "summary + get_confirm_kb()"
      pattern: "get_confirm_kb\\(\\)"
    - from: "registration.py process_resume"
      to: "state resume_file_id"
      via: "file_id only after PDF/DOCX validation"
      pattern: "resume_file_id"
    - from: "cmd_start / _start_registration_flow"
      to: "reg_started row"
      via: "mark_reg_started; clear_reg_started in finalize_registration"
      pattern: "mark_reg_started"
---

<objective>
Deliver three registration-flow quick wins in `handlers/registration.py` + `handlers/states.py`: the QW-01 pre-finalize confirmation step, the QW-03 toggleable mandatory resume upload (PDF/DOCX, crash-safe), and the SCHED-02 `reg_started` dropout lifecycle hooks.

Capability delivered: a delegate reviews their full-form answers before submitting and can restart if wrong; when the organizer enables the resume step the delegate must attach a PDF/DOCX (wrong files are politely rejected, never crash the bot); and every started-but-abandoned registration is recorded persistently so admins can later target that audience.

Purpose: these are the user-facing registration improvements. They all live in registration.py/states.py, so they ship as one cohesive slice with exclusive ownership of those files.
Output: confirm step, resume step, dropout tracking hooks, and pure-helper tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-foundation-quick-wins-coins/01-CONTEXT.md
@.planning/phases/01-db-foundation-quick-wins-coins/01-PATTERNS.md
@.planning/phases/01-db-foundation-quick-wins-coins/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (db.py): consume directly, do not re-implement. -->
- `mark_reg_started(telegram_id, username)` and `clear_reg_started(telegram_id)` — reg_started lifecycle.
- `add_user(data)` now persists `data['resume_file_id']` and never resets `status`.
<!-- Existing registration.py / builders contracts -->
- `_advance(after_step, message, state, bot)` (registration.py:206) — calls finalize_registration when next_idx >= len(enabled). Confirm hook goes here.
- `_get_enabled_steps(data)` (registration.py:118), `_ask_step` (registration.py:137), `_is_step_enabled` (registration.py:111).
- `REG_FLOW` (registration.py:44), `REG_DEFAULTS` (registration.py:66), `REG_LABELS` (registration.py:88) — three parallel dicts; adding a step auto-wires the admin toggle UI.
- `_build_sheet_row(data)` (registration.py:252) — null-coalescing field shape to mirror for the summary.
- `_start_registration_flow(message, state, referrer_id, source_tag)` (registration.py:287) — preserves referrer/source across state.clear(); reuse for 'Изменить' restart.
- `process_full_name` (registration.py:404) — short-form path finalizes when registration_mode != "full" (keep confirm-free, D-03).
- `finalize_registration` (registration.py:650) — passes full data dict to add_user (line 677); state.clear() at 707.
- `get_confirm_kb()` (builders.py:136) → 'Всё верно ✓' / 'Изменить'. `get_skip_kb()` (builders.py:150).
- `settings_receive_file_doc` (admin.py:593) — F.document + message.document.file_id capture pattern for resume.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QW-01 pre-finalize confirmation step</name>
  <read_first>
    - handlers/registration.py (read _advance 206-222, _build_sheet_row 252-282, _start_registration_flow 287-306, process_full_name 404-427, finalize_registration 650-709)
    - handlers/states.py (the file being modified)
    - keyboards/builders.py (get_confirm_kb at 136 — import, do not recreate)
    - .planning/phases/01-db-foundation-quick-wins-coins/01-PATTERNS.md (section "confirm step")
  </read_first>
  <behavior>
    - _build_summary(data) returns an HTML string containing the user's full_name, email, city, education, work answers, expectations — using the same null-coalescing (`or "-"`) as _build_sheet_row and html.escape on free-text values.
    - On the full form, after the last enabled step _advance shows the summary + get_confirm_kb() and sets Registration.confirm (instead of calling finalize_registration directly).
    - 'Всё верно ✓' in Registration.confirm calls finalize_registration.
    - 'Изменить' in Registration.confirm restarts via _start_registration_flow (whole-flow restart, D-02 — no per-field edit).
    - Short form (registration_mode != "full") path in process_full_name still finalizes without confirm (D-03).
  </behavior>
  <action>
    Add `confirm = State()` to the `Registration` StatesGroup in `handlers/states.py`.
    In `handlers/registration.py`, import `get_confirm_kb` alongside the other builders imports (registration.py:16-30). Add a pure helper `_build_summary(data: dict) -> str` modeled on `_build_sheet_row` (registration.py:252): build an HTML summary of the enabled-form answers using `data.get(field) or "-"` and `html.escape(...)` on free-text fields (html is already imported, registration.py:2). Prefix with a heading like "Проверь свои ответы:".
    Modify `_advance` (registration.py:206): in the else branch where it currently calls `finalize_registration` (line 222), instead read `data`, build the summary, `await message.answer(summary, reply_markup=get_confirm_kb(), parse_mode="HTML")`, and `await state.set_state(Registration.confirm)`. Do NOT change the short-form path: `process_full_name` (registration.py:414-415) must keep calling finalize_registration directly when mode != "full" (D-03).
    Add two handlers: `@router.message(Registration.confirm, F.text == "Всё верно ✓")` → `await finalize_registration(message, state, bot)`; `@router.message(Registration.confirm, F.text == "Изменить")` → `await _start_registration_flow(message, state)` (D-02 restart). Place them near the other Registration handlers. The existing `cancel_registration` (registration.py:378, StateFilter(Registration)) already covers Отмена/cancel for the new state.
    Create `tests/test_registration_phase1.py` with a pure test importing `_build_summary` and asserting the returned string contains a sample full_name and uses "-" for an absent field, and that an HTML-special character in a free-text field is escaped (no raw `<` from user input).
  </action>
  <acceptance_criteria>
    - `handlers/states.py` contains `confirm = State()` inside the Registration group.
    - `grep -c "get_confirm_kb()" handlers/registration.py` returns >= 1 and `grep -c "Registration.confirm" handlers/registration.py` returns >= 2 (set_state + at least one handler).
    - `handlers/registration.py` defines `_build_summary` and the else branch of `_advance` no longer calls `finalize_registration` directly (it sets Registration.confirm); the short-form branch in process_full_name still calls finalize_registration.
    - `python -m pytest tests/test_registration_phase1.py -q` passes the summary test (field present, absent→"-", HTML escaped).
    - `python -c "import handlers.registration, handlers.states"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_registration_phase1.py -q && python -c "import handlers.registration, handlers.states"</automated>
  </verify>
  <done>Full-form registration shows an escaped summary + confirm keyboard before finalize; 'Всё верно ✓' finalizes, 'Изменить' restarts the whole flow; short form stays confirm-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QW-03 toggleable mandatory resume upload (PDF/DOCX, crash-safe)</name>
  <read_first>
    - handlers/registration.py (read REG_FLOW/REG_DEFAULTS/REG_LABELS 44-108, _ask_step 137-204, process_age 432-443 as validate-then-return analog, finalize_registration setdefaults 656-675)
    - handlers/admin.py (settings_receive_file_doc 593-608 — F.document file_id capture)
    - handlers/states.py (the file being modified)
    - .planning/phases/01-db-foundation-quick-wins-coins/01-PATTERNS.md (section "resume step")
  </read_first>
  <behavior>
    - _is_allowed_resume(filename) returns True for names ending '.pdf' or '.docx' case-insensitively, False otherwise (including empty/None).
    - When reg_q_resume is enabled (default off), the resume step is offered as the last REG_FLOW step before confirm and is mandatory.
    - A document with a disallowed extension re-prompts with a clear message and does NOT advance or crash.
    - A non-document message in the resume state re-prompts and does NOT crash.
    - A valid PDF/DOCX stores message.document.file_id into state (resume_file_id) and advances; only file_id is stored (no download).
  </behavior>
  <action>
    Add `resume = State()` to the `Registration` StatesGroup in `handlers/states.py`.
    In `handlers/registration.py`: append `("resume", "reg_q_resume")` to `REG_FLOW` as the LAST entry (registration.py:44-64) so it runs immediately before the confirm step; add `"reg_q_resume": "off"` to `REG_DEFAULTS` (D-08 default off) and `"reg_q_resume": "📄 Резюме"` to `REG_LABELS`. (Adding to these three dicts auto-wires the admin toggle UI and `_get_enabled_steps` — no admin.py change.)
    Add a pure helper `_is_allowed_resume(file_name: str | None) -> bool` that lowercases the name and checks `.pdf`/`.docx` suffix (None/empty → False).
    Add a branch to `_ask_step` (registration.py:137) for `step_key == "resume"`: prompt "Прикрепи резюме (PDF или DOCX):" and `await state.set_state(Registration.resume)`.
    Add `@router.message(Registration.resume, F.document)`: validate `message.document.file_name` via `_is_allowed_resume`; if invalid, answer "Принимаются только PDF или DOCX. Прикрепи файл ещё раз." and `return` (re-prompt, D-10 no crash); if valid, `await state.update_data(resume_file_id=message.document.file_id)` (file_id only, D-10) then `await _advance("resume", message, state, bot)`. Add a catch-all `@router.message(Registration.resume)` that answers "Пожалуйста, прикрепи документ (PDF или DOCX)." and returns (handles non-document input without crashing). The resume step is mandatory — provide NO skip path (D-09).
    In `finalize_registration`, add `data.setdefault("resume_file_id", None)` near the other setdefaults (registration.py:656-675) so add_user (Plan 01) persists it.
    Extend `tests/test_registration_phase1.py` with tests for `_is_allowed_resume`: 'cv.pdf'→True, 'cv.PDF'→True, 'cv.docx'→True, 'cv.exe'→False, ''→False, None→False.
  </action>
  <acceptance_criteria>
    - `handlers/states.py` contains `resume = State()` in the Registration group.
    - `grep -c "reg_q_resume" handlers/registration.py` returns >= 3 (REG_FLOW, REG_DEFAULTS, REG_LABELS).
    - `handlers/registration.py` defines `_is_allowed_resume` and two `Registration.resume` handlers (F.document + catch-all).
    - `grep -c "resume_file_id" handlers/registration.py` returns >= 2 (state update + finalize setdefault).
    - No download API is used for the resume: `grep -c "download" handlers/registration.py` returns 0.
    - `python -m pytest tests/test_registration_phase1.py -q` passes all `_is_allowed_resume` cases.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_registration_phase1.py -q</automated>
  </verify>
  <done>Resume is a default-off mandatory REG_FLOW step accepting only PDF/DOCX; wrong file types and non-documents re-prompt without crashing; only file_id is stored and persisted via add_user.</done>
</task>

<task type="auto">
  <name>Task 3: SCHED-02 reg_started lifecycle hooks</name>
  <read_first>
    - handlers/registration.py (read cmd_start 343-375, _start_registration_flow 287-306, finalize_registration 650-709)
    - .planning/phases/01-db-foundation-quick-wins-coins/01-PATTERNS.md (section "reg_started dropout tracking")
    - .planning/phases/01-db-foundation-quick-wins-coins/01-01-SUMMARY.md (confirm mark_reg_started/clear_reg_started signatures)
  </read_first>
  <behavior>
    - When a NEW user begins the registration flow, a reg_started row is written (telegram_id + username).
    - When registration completes (finalize_registration), the reg_started row is deleted.
    - Already-registered users hitting /start do not create reg_started rows (the write is on the new-user flow-start path only).
  </behavior>
  <action>
    Import `mark_reg_started` and `clear_reg_started` from `database.db` in `handlers/registration.py` (add to the existing `from database.db import ...` line at registration.py:14).
    Call `await mark_reg_started(message.from_user.id, message.from_user.username)` at the start of `_start_registration_flow` (registration.py:287) — this is the new-user entry path invoked from cmd_start:375, so already-registered users (who return early at cmd_start:371) are not tracked. Wrap in try/except and log on failure (fail-soft, matching the codebase's Bot-API/DB call style) so dropout tracking never blocks registration.
    Call `await clear_reg_started(message.from_user.id)` inside `finalize_registration` after `await add_user(data)` (registration.py:677), also wrapped fail-soft.
  </action>
  <acceptance_criteria>
    - `grep -c "mark_reg_started" handlers/registration.py` returns >= 2 (import + call) and `grep -c "clear_reg_started" handlers/registration.py` returns >= 2 (import + call).
    - The `mark_reg_started` call is inside `_start_registration_flow` (not in the already-registered early-return branch of cmd_start).
    - The `clear_reg_started` call is inside `finalize_registration` after `add_user`.
    - Both calls are wrapped in try/except (fail-soft).
    - `python -c "import handlers.registration"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import handlers.registration"</automated>
  </verify>
  <done>reg_started is written at new-user flow start and deleted on finalize, both fail-soft; already-registered /start users are not tracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram user → bot | uploaded document (resume) and free-text form answers cross into the bot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | DoS / Tampering | resume upload | mitigate | File-type validated by extension via `_is_allowed_resume`; only `file_id` stored (no download, no disk write, no execution). Non-document and wrong-type inputs re-prompt instead of raising. Verified by unit tests + `download == 0` grep. |
| T-02-02 | Info disclosure | summary rendering | mitigate | `_build_summary` html.escapes free-text fields so a user injecting `<...>` cannot break the HTML message. Verified by escape test. |
| T-02-03 | Info disclosure | reg_started | accept | Only telegram_id + username + timestamp stored (no resume, name, email). Minimal data by design (inherited from Plan 01 schema). |
| T-02-04 | Availability | dropout hooks | mitigate | mark/clear reg_started wrapped in try/except — a DB hiccup never blocks a real user's registration. |
</threat_model>

<verification>
- `python -m pytest tests/test_registration_phase1.py -q` — all pure-helper tests pass.
- `python -c "import handlers.registration, handlers.states"` — imports clean.
- Grep gates in acceptance_criteria satisfied.
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion 6 (incomplete registrations recorded persistently and deleted on completion) is satisfied by the reg_started hooks.
- QW-01 confirmation and QW-03 resume behaviors match D-01..D-03 and D-08..D-10.
</success_criteria>

<output>
After completion, create `.planning/phases/01-db-foundation-quick-wins-coins/01-02-SUMMARY.md`.
</output>
