---
phase: 01-db-foundation-quick-wins-coins
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - handlers/registration.py
  - handlers/admin.py
  - tests/test_subscription_phase1.py
autonomous: true
requirements: [QW-02, SCHED-02]
must_haves:
  truths:
    - "At /start the bot checks the user's membership of the channel in the contact_tg setting and records non-subscribers; it never blocks any user"
    - "The check fails open: if the bot is not admin in the channel (cannot read membership), no user is flagged or penalized"
    - "An admin can pick a 'не подписаны на канал' broadcast segment and see the count of non-subscribers"
    - "An admin can pick a 'не завершили регистрацию' broadcast segment targeting incomplete registrations"
  artifacts:
    - path: "handlers/registration.py"
      provides: "is_subscribed helper + non-blocking subscription check hook in cmd_start"
      contains: "get_chat_member"
    - path: "handlers/admin.py"
      provides: "two broadcast audience segment buttons + handlers"
      contains: "broadcast_unsubscribed"
    - path: "tests/test_subscription_phase1.py"
      provides: "Tests for membership-status mapping and segment authorization"
      min_lines: 25
  key_links:
    - from: "cmd_start"
      to: "users.subscribed"
      via: "is_subscribed → set_user_subscribed (fail-open)"
      pattern: "set_user_subscribed"
    - from: "admin broadcast keyboard"
      to: "segment user_ids"
      via: "get_non_subscriber_ids / get_incomplete_user_ids → target_users"
      pattern: "broadcast_(unsubscribed|incomplete)"
---

<objective>
Deliver subscription tracking and the two audience segments the user wants visible as "касты": the QW-02 non-blocking `getChatMember` subscription check at `/start` (recording non-subscribers, fail-open), and two new broadcast segment targets in `handlers/admin.py` — non-subscribers and incomplete registrations (SCHED-02 segment) — that plug into the existing broadcast send loop.

Capability delivered: the bot quietly learns who is not subscribed to the configured channel (never blocking anyone, D-04..D-07), and an admin can pick "не подписаны на канал" or "не завершили регистрацию" as a broadcast audience, see the count, and message exactly that segment.

Purpose: this slice touches cmd_start (registration.py) and the broadcast keyboard/handlers (admin.py) — files owned by Plans 02 and 03 respectively — so it runs last (Wave 3) to avoid conflicts, consuming the data helpers from Plan 01.
Output: subscription check, two broadcast segments, and tests for the membership mapping + segment authorization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-foundation-quick-wins-coins/01-CONTEXT.md
@.planning/phases/01-db-foundation-quick-wins-coins/01-PATTERNS.md
@.planning/phases/01-db-foundation-quick-wins-coins/01-01-SUMMARY.md
@.planning/phases/01-db-foundation-quick-wins-coins/01-02-SUMMARY.md
@.planning/phases/01-db-foundation-quick-wins-coins/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 (db.py): consume directly. -->
- `set_user_subscribed(telegram_id, subscribed: bool)` — persists 1/0 to users.subscribed.
- `get_non_subscriber_ids() -> list[int]` — users WHERE subscribed = 0.
- `get_incomplete_user_ids() -> list[int]` — telegram_ids in reg_started.
- `get_setting("contact_tg")` (db.py:67) — already holds the channel reference (reuse, D-04; no new channel config).
<!-- Existing broadcast machinery (admin.py) -->
- `show_admin_broadcast` (admin.py:657) and `cmd_broadcast` (admin.py:687) both build the SAME target keyboard (admin.py:663-667 and 689-693) — keep both in sync.
- `process_broadcast_local_file` (admin.py:711) — segment handler analog: resolve user_ids → `state.update_data(target_type="list", target_users=user_ids)` → `state.set_state(Broadcast.message)`.
- `process_broadcast` (admin.py:829) — send loop; consumes `target_users` unchanged. Do NOT modify it.
- Callback admin auth pattern: `if callback.from_user.id not in config.ADMIN_IDS:` (admin.py:659-661).
- `cmd_start` (registration.py:343) — subscription check hook point; user object available; aiogram `bot.get_chat_member(chat, user_id)` is native (no new dep).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QW-02 non-blocking subscription check at /start (fail-open)</name>
  <read_first>
    - handlers/registration.py (read cmd_start 343-375, and the fail-soft try/except style in finalize_registration 701-705)
    - .planning/phases/01-db-foundation-quick-wins-coins/01-PATTERNS.md (section "subscription check, NON-blocking")
    - .planning/phases/01-db-foundation-quick-wins-coins/01-01-SUMMARY.md (set_user_subscribed signature, contact_tg usage)
  </read_first>
  <behavior>
    - _membership_status_to_bool(status) → True for {"creator","administrator","member"}, False for {"left","kicked","restricted"} (treat restricted as not a member for reminder purposes).
    - is_subscribed(bot, channel, user_id) returns True/False from get_chat_member, and None when the call raises (bot not admin / unknown channel) — fail-open, never raises.
    - At /start, if a channel is configured (contact_tg) the bot checks membership and calls set_user_subscribed only when the result is a definite True/False; a None result records nothing (no flag) — no user is blocked or penalized at any point.
    - If contact_tg is unset, the check is skipped entirely.
  </behavior>
  <action>
    In `handlers/registration.py`, import `set_user_subscribed` from `database.db` (add to the import at registration.py:14).
    Add a pure helper `_membership_status_to_bool(status: str) -> bool` mapping member-like statuses to True. Add `async def is_subscribed(bot, channel, user_id) -> bool | None`: wrap `await bot.get_chat_member(channel, user_id)` in try/except; on success return `_membership_status_to_bool(member.status)`; on Exception return None (fail-open per D-07; log at debug/warning like the codebase's other Bot-API calls).
    In `cmd_start` (registration.py:343), after resolving the user and BEFORE any blocking behavior (there is none — this is observe-only), read `channel = await get_setting("contact_tg")`. If channel is truthy, call `result = await is_subscribed(bot, channel, user_id)`; if `result is not None`, `await set_user_subscribed(user_id, result)`. Do NOT branch bot behavior on the result — the user always proceeds (D-04). Note: cmd_start currently has no `bot` parameter — add `bot: Bot` to its signature (Bot is already imported, registration.py:5; aiogram injects it). Wrap the whole check in try/except so it can never break /start.
    Create `tests/test_subscription_phase1.py` testing `_membership_status_to_bool` for member/administrator/creator → True and left/kicked/restricted → False. Add a test that `is_subscribed` returns None when given a fake bot whose get_chat_member raises (assert no exception propagates).
  </action>
  <acceptance_criteria>
    - `handlers/registration.py` defines `_membership_status_to_bool` and `is_subscribed`, and `is_subscribed` contains a `try`/`except` returning `None`.
    - `grep -c "get_chat_member" handlers/registration.py` returns >= 1.
    - `grep -c "set_user_subscribed" handlers/registration.py` returns >= 2 (import + call); the call is guarded by `if result is not None`.
    - cmd_start does NOT block or early-return based on subscription status (no new `return` tied to is_subscribed result).
    - `python -m pytest tests/test_subscription_phase1.py -q` passes the status-mapping and fail-open tests.
    - `python -c "import handlers.registration"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_subscription_phase1.py -q && python -c "import handlers.registration"</automated>
  </verify>
  <done>/start checks channel membership against contact_tg and records definite subscribed/non-subscribed status, fails open on any error, and never blocks or penalizes a user.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SCHED-02/QW-02 broadcast segments — non-subscribers + incomplete registrations</name>
  <read_first>
    - handlers/admin.py (read show_admin_broadcast 657-670, cmd_broadcast 687-695, process_broadcast_local_file 711-749, process_broadcast 829-871)
    - .planning/phases/01-db-foundation-quick-wins-coins/01-PATTERNS.md (section "/coins command + broadcast segments" — segment part)
    - .planning/phases/01-db-foundation-quick-wins-coins/01-01-SUMMARY.md (get_non_subscriber_ids / get_incomplete_user_ids signatures)
  </read_first>
  <behavior>
    - Both broadcast entry points (show_admin_broadcast and cmd_broadcast) show two new buttons: "🚫 Не подписаны на канал" (broadcast_unsubscribed) and "📝 Не завершили регистрацию" (broadcast_incomplete).
    - Selecting a segment is admin-only; a non-admin callback is rejected.
    - Selecting "не подписаны на канал" resolves get_non_subscriber_ids(), shows the count ("N пользователей не подписаны на канал ..."), sets target_type="list"/target_users, and advances to Broadcast.message.
    - Selecting "не завершили регистрацию" resolves get_incomplete_user_ids() similarly.
    - An empty segment tells the admin the segment is empty and does not advance to message entry.
    - The existing send loop (process_broadcast) sends to target_users unchanged.
  </behavior>
  <action>
    In `handlers/admin.py`, import `get_non_subscriber_ids`, `get_incomplete_user_ids` from `database.db` (add to the import block at admin.py:13-24).
    Add two inline buttons to BOTH broadcast keyboards (admin.py:663-667 in show_admin_broadcast AND admin.py:689-693 in cmd_broadcast — keep them identical): `InlineKeyboardButton(text="🚫 Не подписаны на канал", callback_data="broadcast_unsubscribed")` and `InlineKeyboardButton(text="📝 Не завершили регистрацию", callback_data="broadcast_incomplete")`, placed before the Отмена button.
    Add two callback handlers modeled on `process_broadcast_local_file` (admin.py:711): `@router.callback_query(F.data == "broadcast_unsubscribed", Broadcast.target_selection)` and `@router.callback_query(F.data == "broadcast_incomplete", Broadcast.target_selection)`. Each: first enforce admin auth with `if callback.from_user.id not in config.ADMIN_IDS: await callback.answer("Недостаточно прав", show_alert=True); return` (admin.py:659-661 pattern). Resolve `user_ids` via the respective helper. If empty, `callback.message.edit_text("В этом сегменте сейчас нет пользователей.")`, `state.clear()`, return. Otherwise `await state.update_data(target_type="list", target_users=user_ids)`, delete/answer the prompt, send a message including the count (non-subscriber phrasing per CONTEXT line 106: "N пользователей не подписаны на канал, давайте пришлём им уведомление."), `await state.set_state(Broadcast.message)`. Do NOT modify `process_broadcast` — it already consumes target_users.
    Extend `tests/test_subscription_phase1.py` with a structural test asserting handlers/admin.py source contains both `broadcast_unsubscribed` and `broadcast_incomplete` callback handlers AND that each segment handler body contains the `config.ADMIN_IDS` authorization check (segment selection is admin-gated).
  </action>
  <acceptance_criteria>
    - `grep -c "broadcast_unsubscribed" handlers/admin.py` returns >= 3 (button in two keyboards + one handler) and `grep -c "broadcast_incomplete" handlers/admin.py` returns >= 3.
    - Both segment callback handlers contain `config.ADMIN_IDS` (authorization) and set `target_type="list"` + `target_users`.
    - `handlers/admin.py` calls `get_non_subscriber_ids` and `get_incomplete_user_ids`.
    - `process_broadcast` (admin.py:829) is unchanged (still reads target_users).
    - `python -m pytest tests/test_subscription_phase1.py -q` passes the segment structural + authorization tests.
    - `python -c "import handlers.admin"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_subscription_phase1.py -q && python -c "import handlers.admin"</automated>
  </verify>
  <done>Both broadcast entry points expose non-subscriber and incomplete-registration segments; each is admin-gated, shows its count, populates target_users, and reuses the existing send loop; empty segments are reported without advancing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot → Telegram channel API | getChatMember may fail (bot not admin) — must not break /start |
| admin → broadcast segments | admin selects an audience derived from DB state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | DoS / Availability | subscription check at /start | mitigate | is_subscribed wraps get_chat_member in try/except returning None; the whole check is additionally try/except-guarded — a channel/API failure can never block or crash /start (fail-open, D-07). Verified by the fail-open test. |
| T-04-02 | Info disclosure | membership leakage | mitigate | The bot never tells the user their membership status or blocks them; membership is recorded only as an internal subscribed flag for admin segmentation. None result records nothing. |
| T-04-03 | Elevation | segment broadcast | mitigate | Both segment callbacks re-check `callback.from_user.id in config.ADMIN_IDS` (callbacks are not covered by the message-level is_admin filter). Verified by structural authorization test. |
| T-04-04 | Info disclosure | incomplete-registration segment | accept | Segment resolves to telegram_ids only (from reg_started, which stores no PII per Plan 01); the broadcast sends the admin's message to those ids and never exposes their data to other users. |
</threat_model>

<verification>
- `python -m pytest tests/test_subscription_phase1.py -q` — all tests pass.
- `python -c "import handlers.registration, handlers.admin"` — imports clean.
- Grep gates in acceptance_criteria satisfied.
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion 5 (subscription checked not gated, non-subscribers targetable by admin reminder, fail-open) is satisfied.
- The two named broadcast segments (non-subscribers, incomplete registrations) are selectable per CONTEXT D-06 and the SCHED-02 dropout segment.
</success_criteria>

<output>
After completion, create `.planning/phases/01-db-foundation-quick-wins-coins/01-04-SUMMARY.md`.
</output>
