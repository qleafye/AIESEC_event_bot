---
phase: 01-db-foundation-quick-wins-coins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/db.py
  - tests/test_db_phase1.py
autonomous: true
requirements: [DB-01, DB-02, DB-03, COIN-01, SCHED-02]
must_haves:
  truths:
    - "Running init_db() against a DB with the old users schema leaves every existing user with status='approved' (no access loss)"
    - "Re-adding an existing user via add_user() preserves their status and resume_file_id (ON CONFLICT DO UPDATE, never DELETE+INSERT)"
    - "Coins balance equals SUM(delta) from an append-only ledger; +10 then -3 yields 7"
    - "A telegram_id can be marked reg_started and later cleared; incomplete users are queryable independent of FSM"
    - "Non-subscriber telegram_ids are queryable for the broadcast segment"
  artifacts:
    - path: "database/db.py"
      provides: "Phase-1 migrations, ON CONFLICT add_user, coins ledger helpers, reg_started helpers, subscription-flag helpers"
      contains: "ON CONFLICT(telegram_id) DO UPDATE"
    - path: "tests/test_db_phase1.py"
      provides: "Automated verification of migrations, ledger, reg_started, subscription flag"
      min_lines: 60
  key_links:
    - from: "database/db.py init_db()"
      to: "users.status"
      via: "_ensure_column with DEFAULT 'approved'"
      pattern: "_ensure_column.*status.*approved"
    - from: "database/db.py add_user()"
      to: "users row"
      via: "ON CONFLICT DO UPDATE excluding status"
      pattern: "ON CONFLICT\\(telegram_id\\) DO UPDATE"
    - from: "coins ledger"
      to: "balance"
      via: "SUM(delta)"
      pattern: "SUM\\(delta\\)"
---

<objective>
Establish the Phase-1 data foundation in `database/db.py`: safe additive schema migrations against the live `data/forum.db` (~590 users), the `add_user()` switch to `ON CONFLICT(telegram_id) DO UPDATE`, the append-only coins ledger, the persistent `reg_started` dropout table, and a subscription-flag column — all with helper functions consumed by the feature plans in later waves.

Capability delivered: existing users survive every migration with full access, re-registration never wipes their data, and the data layer for coins, dropout tracking, and subscription segments exists and is verified.

Purpose: db.py is touched by every Phase-1 feature. Owning all schema + helpers in one Wave-1 plan gives downstream plans (02 registration UX, 03 coins, 04 subscription/broadcast) clean contracts and zero file conflicts.
Output: Migrated schema, ledger + lifecycle + subscription helpers, and a pytest suite proving migration safety and ledger correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-foundation-quick-wins-coins/01-CONTEXT.md
@.planning/phases/01-db-foundation-quick-wins-coins/01-PATTERNS.md

<interfaces>
<!-- Existing db.py contracts. Reuse these; do not re-explore. -->
- `_column_exists(db, table, column) -> bool` (db.py:10) — PRAGMA-based, idempotent.
- `_ensure_column(db, table, column, definition)` (db.py:16) — additive ALTER, idempotent. Pattern for all new columns.
- `init_db()` (db.py:20) — single `await db.commit()` at the end (db.py:65). Add all CREATE TABLE / _ensure_column BEFORE that commit.
- `add_user(data: dict)` (db.py:91) — currently `INSERT OR REPLACE` (db.py:94). Column list + value tuple at db.py:94-133.
- `get_user_by_username(username)` (db.py:147) — normalizes leading '@', COLLATE NOCASE. Reuse verbatim downstream.
- `get_setting/set_setting/delete_setting` (db.py:67-88) — connection-per-call write pattern to mirror.
- Every function opens its own `async with aiosqlite.connect(config.DB_PATH) as db:` — no shared connection. Match this.
- `config.DB_PATH` is read at call time (not import time), so tests can monkeypatch `config.DB_PATH`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Safe migrations + ON CONFLICT add_user (DB-01, DB-02, DB-03)</name>
  <read_first>
    - database/db.py (the file being modified — read init_db lines 20-65 and add_user lines 91-134 in full before editing)
    - .planning/phases/01-db-foundation-quick-wins-coins/01-PATTERNS.md (section "database/db.py — schema migrations + ledger")
  </read_first>
  <behavior>
    - init_db() on a DB whose `users` table lacks `status` adds `status` with DEFAULT 'approved' → every pre-existing row reads status='approved'.
    - init_db() adds `resume_file_id TEXT` (nullable) and `subscribed INTEGER` (nullable) columns without touching existing data.
    - add_user() on an existing telegram_id updates mutable profile fields but leaves `status` unchanged (status is NOT in the INSERT column list nor the DO UPDATE SET clause).
    - add_user() persists `resume_file_id` from the data dict (included in INSERT + DO UPDATE SET).
  </behavior>
  <action>
    In `init_db()`, before the single `await db.commit()` at db.py:65, add three `_ensure_column(db, "users", ...)` calls: `status` with definition `TEXT DEFAULT 'approved'` (D-18 — existing ~590 users keep access), `resume_file_id` with `TEXT`, and `subscribed` with `INTEGER` (nullable; NULL = unknown / fail-open). Use the existing `_ensure_column` primitive (db.py:16) — do NOT add a migration library.
    Convert `add_user()` (db.py:91) from `INSERT OR REPLACE INTO users (...)` to `INSERT INTO users (...) VALUES (...) ON CONFLICT(telegram_id) DO UPDATE SET col=excluded.col` for every column currently in the value tuple (D-17 — REPLACE is DELETE+INSERT and wipes new columns). Add `resume_file_id` to the column list, the `VALUES (?)` placeholders, and the DO UPDATE SET clause, sourced from `data.get('resume_file_id')`. Do NOT add `status` to add_user's column list or SET clause — status is owned by the approval flow (Phase 2) and the migration default only. `telegram_id` is PRIMARY KEY (db.py:24) so it is a valid conflict target; keep it out of the SET clause.
    Create `tests/test_db_phase1.py` with pytest tests that wrap async calls in `asyncio.run()` (pytest-asyncio is unavailable; do NOT rely on it). Each test sets `config.DB_PATH` to a `tmp_path` file. Tests: (a) seed a minimal old-schema `users` table (telegram_id, full_name, registration_date) with one row, run init_db(), assert that row's `status == 'approved'`; (b) run init_db() on a fresh DB, add_user() a user, manually UPDATE its status to 'pending' and resume_file_id to 'abc123', add_user() again with the same telegram_id and a changed full_name, assert status is still 'pending', resume_file_id round-trips, and full_name updated.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' database/db.py | grep -c "ON CONFLICT(telegram_id) DO UPDATE"` returns >= 1.
    - `grep -v '^#' database/db.py | grep -c "INSERT OR REPLACE INTO users"` returns 0 (the add_user REPLACE is gone).
    - `grep -c "DEFAULT 'approved'" database/db.py` returns >= 1 and the line also contains `status`.
    - `database/db.py` contains `_ensure_column(db, "users", "resume_file_id"` and `_ensure_column(db, "users", "subscribed"`.
    - `python -m pytest tests/test_db_phase1.py -q` passes the migration-status test and the ON-CONFLICT-preserves-status test.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_db_phase1.py -q</automated>
  </verify>
  <done>init_db migrates status (DEFAULT 'approved'), resume_file_id, subscribed columns idempotently; add_user uses ON CONFLICT DO UPDATE and never touches status; tests prove existing users keep access and re-registration preserves status/resume_file_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coins ledger, reg_started, and subscription helpers (COIN-01, SCHED-02 data layer)</name>
  <read_first>
    - database/db.py (read get_setting/set_setting/delete_setting db.py:67-88 and get_referrals db.py:159 as the helper shape to mirror)
    - .planning/phases/01-db-foundation-quick-wins-coins/01-PATTERNS.md (sections "schema migrations + ledger" and "reg_started dropout tracking")
    - tests/test_db_phase1.py (extend the file created in Task 1)
  </read_first>
  <behavior>
    - add_coins(user_id, +10) then add_coins(user_id, -3) → get_balance(user_id) == 7. Each call INSERTs a row; no row is ever UPDATEd.
    - Negative balances are allowed (add_coins(user_id, -100) on a zero balance → get_balance == -100).
    - get_leaderboard(limit=10) returns rows ordered by SUM(delta) DESC with the user's display name; get_user_rank(user_id) returns the 1-based rank.
    - mark_reg_started(id, username) then get_incomplete_user_ids() includes id; after clear_reg_started(id) it no longer includes id.
    - set_user_subscribed(id, False) then get_non_subscriber_ids() includes id; set_user_subscribed(id, True) removes it.
  </behavior>
  <action>
    In `init_db()` (before the db.py:65 commit) add two `CREATE TABLE IF NOT EXISTS` blocks copying the `bot_settings` style (db.py:58-63): `coins` (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, delta INTEGER NOT NULL, reason TEXT, changed_by INTEGER, timestamp TEXT NOT NULL) and `reg_started` (telegram_id INTEGER PRIMARY KEY, username TEXT, started_at TEXT NOT NULL). Add an index `CREATE INDEX IF NOT EXISTS idx_coins_user ON coins(user_id)` (D-12 discretion).
    Add helper functions, each using the connection-per-call pattern (`async with aiosqlite.connect(config.DB_PATH) as db:`): `add_coins(user_id, delta, reason=None, changed_by=None)` — INSERT-only into coins with `datetime.now().strftime("%Y-%m-%d %H:%M:%S")` timestamp, parameterized placeholders only, never UPDATE (D-12). `get_balance(user_id) -> int` — `SELECT COALESCE(SUM(delta),0) FROM coins WHERE user_id=?`. `get_leaderboard(limit=10)` — `SELECT user_id, COALESCE(SUM(delta),0) AS bal FROM coins GROUP BY user_id ORDER BY bal DESC LIMIT ?`, LEFT JOIN `users` for full_name/username; return list of dicts. `get_user_rank(user_id) -> int|None` — count of users with a strictly greater summed balance, +1. `mark_reg_started(telegram_id, username)` — `INSERT OR REPLACE INTO reg_started`. `clear_reg_started(telegram_id)` — DELETE. `get_incomplete_user_ids() -> list[int]` — SELECT telegram_id FROM reg_started. `set_user_subscribed(telegram_id, subscribed: bool)` — UPDATE users SET subscribed=? WHERE telegram_id=? (1/0). `get_non_subscriber_ids() -> list[int]` — SELECT telegram_id FROM users WHERE subscribed = 0.
    Import `datetime` at the top of db.py if not present. Extend `tests/test_db_phase1.py` with tests for: balance arithmetic (+10,-3 → 7), negative balance allowed, leaderboard ordering + rank, reg_started mark/query/clear lifecycle, and subscription flag set/query.
  </action>
  <acceptance_criteria>
    - `database/db.py` defines `add_coins`, `get_balance`, `get_leaderboard`, `get_user_rank`, `mark_reg_started`, `clear_reg_started`, `get_incomplete_user_ids`, `set_user_subscribed`, `get_non_subscriber_ids` (one `def`/`async def` each).
    - `grep -c "UPDATE coins" database/db.py` returns 0 (ledger is append-only — no balance mutation).
    - `grep -c "SUM(delta)" database/db.py` returns >= 2 (balance + leaderboard).
    - All coins functions use `?` placeholders (no f-string SQL interpolation of user values): `grep -nE "execute\(f" database/db.py` shows no coins/reg_started/subscription query using f-string-injected user input.
    - `python -m pytest tests/test_db_phase1.py -q` passes all ledger, reg_started, and subscription tests.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_db_phase1.py -q</automated>
  </verify>
  <done>coins (append-only) and reg_started tables exist; all ledger/lifecycle/subscription helpers parameterized and tested; balance = SUM(delta), negative balances allowed, dropout and non-subscriber segments queryable independent of FSM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → db helpers | user_id / username / reason values pass into SQL |
| migration → live data | init_db runs against the production data/forum.db with ~590 real rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | coins ledger | mitigate | Append-only: helpers INSERT only, never UPDATE coins; balance is a derived SUM(delta). Verified by grep gate `UPDATE coins == 0` + balance test. |
| T-01-02 | Injection | all new db helpers | mitigate | Every query uses `?` placeholders; no user value is f-string-interpolated into SQL. Verified by acceptance grep. |
| T-01-03 | Elevation/DoS | status migration | mitigate | `status` added with DEFAULT 'approved' so no existing user loses access; add_user excludes status from DO UPDATE so re-registration cannot silently flip approval. Verified by migration + ON CONFLICT tests. |
| T-01-04 | Info disclosure | reg_started table | accept | Stores only telegram_id, username, timestamp — no PII (no name/email/phone/resume). Minimal-data-by-design; no further mitigation needed. |
| T-01-05 | Tampering | init_db idempotency | mitigate | `_ensure_column` guards with PRAGMA table_info; CREATE TABLE IF NOT EXISTS — re-running init_db on the live DB is safe and non-destructive. |
</threat_model>

<verification>
- `python -m pytest tests/test_db_phase1.py -q` — all tests pass.
- `python -c "import database.db"` — module imports without error.
- Grep gates in acceptance_criteria all satisfied.
</verification>

<success_criteria>
- ROADMAP Phase 1 success criteria 1, 2, and 3 (data-layer portion) are verifiable: migration preserves access, ON CONFLICT preserves data, balance = SUM(delta) computes 7 from +10/-3.
- Downstream plans 02/03/04 can import all helper contracts without touching db.py.
</success_criteria>

<output>
After completion, create `.planning/phases/01-db-foundation-quick-wins-coins/01-01-SUMMARY.md`.
</output>
