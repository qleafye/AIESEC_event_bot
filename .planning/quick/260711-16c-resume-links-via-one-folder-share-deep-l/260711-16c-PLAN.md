---
phase: quick-260711-16c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/nextcloud.py
  - config.py
  - handlers/registration.py
  - scripts/backfill_resumes.py
  - .env.example
  - tests/test_nextcloud_wk6.py
  - tests/test_backfill_resumes.py
autonomous: true
requirements: [QUICK-260711-16c]
must_haves:
  truths:
    - "Бот больше НЕ создаёт пофайловую OCS-шару — только WebDAV PUT + строит deep-ссылку"
    - "Ссылка в таблице — deep-link в одну ручную шару-папку по шаблону /s/<token>/download?path=%2F&files=<name>"
    - "Имя залитого файла = <ФИО>_<username|telegram_id> с сохранённым расширением"
    - "Текст-резюме заливается файлом .txt и тоже получает deep-ссылку"
    - "Рега никогда не падает из-за Nextcloud (fail-soft, bounded wait_for)"
    - "backfill берёт И файл-, И текст-резюме без resume_url"
  artifacts:
    - path: "services/nextcloud.py"
      provides: "_put_bytes, _file_link, upload_resume, upload_text_resume (no OCS)"
      contains: "def _file_link"
    - path: "config.py"
      provides: "NEXTCLOUD_PUBLIC_URL, NEXTCLOUD_FOLDER_SHARE_TOKEN"
      contains: "NEXTCLOUD_FOLDER_SHARE_TOKEN"
    - path: "handlers/registration.py"
      provides: "_resume_person_name + finalize file/text upload branches"
      contains: "_resume_person_name"
    - path: "scripts/backfill_resumes.py"
      provides: "select_pending_resumes (file+text) + dual-type loop"
  key_links:
    - from: "handlers/registration.py finalize"
      to: "services.nextcloud.upload_resume / upload_text_resume"
      via: "asyncio.wait_for timeout=20 → data['resume_url']"
      pattern: "upload_text_resume|upload_resume"
    - from: "services.nextcloud._file_link"
      to: "NEXTCLOUD_PUBLIC_URL + NEXTCLOUD_FOLDER_SHARE_TOKEN"
      via: "deep-link template with quote(remote_name)"
      pattern: "FOLDER_SHARE_TOKEN.*download.*files="
---

<objective>
Редизайн модели шаринга резюме: убрать пофайловые OCS-шары, перейти на deep-ссылки в ОДНУ ручную публичную шару-папку `resumes`. Файлы называть по ФИО, текст-резюме заливать как .txt-файл. Всё fail-soft — регистрация не падает из-за Nextcloud.

Purpose: Устранить дублирование/рассинхрон (пофайловая шара + отдельная шара всей папки существуют параллельно). Пароль ставится один раз на самой шаре-папке вручную.
Output: Переработанный `services/nextcloud.py`, новые config-поля, finalize + backfill на оба типа резюме, обновлённые тесты и .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Текущее состояние (executor использует напрямую, без доп. разведки) -->

config.py NEXTCLOUD-блок (строки 37-43):
```python
NEXTCLOUD_BASE_URL: str = ""
NEXTCLOUD_WEBDAV_URL: str = ""
NEXTCLOUD_USER: str = ""
NEXTCLOUD_APP_PASS: SecretStr | None = None
NEXTCLOUD_FOLDER: str = "resumes"
NEXTCLOUD_SHARE_PASSWORD: SecretStr | None = None   # оставить поле, код НЕ читает
NEXTCLOUD_VERIFY_TLS: bool = False
```

handlers/registration.py finalize — username/telegram_id УЖЕ в data (строки 1735-1736):
```python
data["telegram_id"] = message.from_user.id
data["username"] = f"@{message.from_user.username}" if message.from_user.username else "-"
```
ВНИМАНИЕ: username здесь либо "@name", либо "-" (НЕ None). `_resume_person_name` должен трактовать "-" и пустое как отсутствие → fallback на telegram_id, а ведущий "@" срезать.

finalize текущий upload-блок (строки 1768-1777) — заменить целиком (см. Task 2).
`resume_text` уже кладётся в FSM в process_resume_text (строка 1172); `resume_file_name` — в process_resume (строка 1160).

services/nextcloud.py `_safe_name` (строки 20-24) — ОСТАВИТЬ как есть (юникодный \w, кириллица сохраняется).

scripts/backfill_resumes.py текущий guard (строка 82): `if not config.NEXTCLOUD_BASE_URL:` — заменить.
select_pending_resumes (строки 33-44) возвращает `(telegram_id, resume_file_id)` — расширить.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переработать services/nextcloud.py на deep-links + config-поля</name>
  <files>services/nextcloud.py, config.py</files>
  <action>
config.py: после NEXTCLOUD_VERIFY_TLS добавить два поля — `NEXTCLOUD_PUBLIC_URL: str = ""` (публичный адрес для ссылок, напр. https://91.223.28.229:8443) и `NEXTCLOUD_FOLDER_SHARE_TOKEN: str = ""` (токен XXXX из ссылки публичной шары папки /s/XXXX). Существующие NEXTCLOUD_* НЕ удалять; NEXTCLOUD_SHARE_PASSWORD оставить как поле, но код его больше не читает.

services/nextcloud.py: полностью переработать. Импортировать `from urllib.parse import quote`. Обновить модуль-docstring (убрать упоминание OCS/share-password, описать deep-link модель).

Оставить `_safe_name` без изменений.

Добавить `async def _put_bytes(content: bytes, remote_name: str) -> bool`: WebDAV PUT байтов на `f"{config.NEXTCLOUD_WEBDAV_URL.rstrip('/')}/{folder}/{remote_name}"`, где `folder = config.NEXTCLOUD_FOLDER.strip('/')`. Auth = `aiohttp.BasicAuth(config.NEXTCLOUD_USER, config.NEXTCLOUD_APP_PASS.get_secret_value() if config.NEXTCLOUD_APP_PASS else "")`. `ssl_arg = None if config.NEXTCLOUD_VERIFY_TLS else False`. timeout total=15. Вернуть True на status в (200,201,204), иначе log.error(status+body[:300]) + False.

Добавить `def _file_link(remote_name: str) -> str`: вернуть `f"{config.NEXTCLOUD_PUBLIC_URL.rstrip('/')}/s/{config.NEXTCLOUD_FOLDER_SHARE_TOKEN}/download?path=%2F&files={quote(remote_name)}"`. Отдельная функция — чтобы легко сменить формат (download→просмотр).

Переписать `async def upload_resume(bot, file_id: str, filename: str) -> str | None`: весь body в try/except → None (никогда не бросает). Guard: если пусты NEXTCLOUD_WEBDAV_URL или NEXTCLOUD_PUBLIC_URL или NEXTCLOUD_FOLDER_SHARE_TOKEN → logger.debug + return None. Иначе: `buf = await bot.download(file_id); buf.seek(0); content = buf.read()`; `remote = f"{uuid.uuid4().hex[:8]}_{_safe_name(filename)}"`; `if await _put_bytes(content, remote): return _file_link(remote)` else None. Убрать весь OCS POST /shares и чтение NEXTCLOUD_SHARE_PASSWORD.

Добавить `async def upload_text_resume(text: str, filename: str) -> str | None`: тот же guard (WEBDAV/PUBLIC/TOKEN), `content = (text or "").encode("utf-8")`, `remote = f"{uuid.uuid4().hex[:8]}_{_safe_name(filename)}"`, PUT → `_file_link`. bot НЕ нужен. Fail-soft (try/except → None).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import ast; ast.parse(open('services/nextcloud.py',encoding='utf-8').read()); ast.parse(open('config.py',encoding='utf-8').read()); print('ok')"</automated>
  </verify>
  <done>_put_bytes/_file_link/upload_resume/upload_text_resume существуют; НЕТ вызова /ocs/ или /shares; config имеет NEXTCLOUD_PUBLIC_URL и NEXTCLOUD_FOLDER_SHARE_TOKEN.</done>
</task>

<task type="auto">
  <name>Task 2: finalize + именование в handlers/registration.py</name>
  <files>handlers/registration.py</files>
  <action>
Убедиться, что `upload_text_resume` импортируется из services.nextcloud рядом с `upload_resume` (найти существующий импорт upload_resume и дополнить).

Добавить хелпер (рядом с другими модуль-хелперами регистрации): `def _resume_person_name(data) -> str`. Логика: `name = (data.get("full_name") or "").strip()`; `uname = (data.get("username") or "").strip().lstrip("@")`; если `uname` пусто или == "-" → `uname = str(data.get("telegram_id"))`. Вернуть `f"{name}_{uname}"` СЫРОЙ строкой (санитизацию делает _safe_name внутри nextcloud). Пример: full_name="Иван Петров", username="@qleafye" → "Иван Петров_qleafye".

Заменить текущий upload-блок finalize (строки 1768-1777) на ветвление file/text:
- `if data.get("resume_file_id"):` → `ext = os.path.splitext(data.get("resume_file_name") or "")[1]`; `fname = f"{_resume_person_name(data)}{ext}"`; `url = await asyncio.wait_for(upload_resume(bot, data["resume_file_id"], fname), timeout=20)`.
- `elif data.get("resume_text"):` → `fname = f"{_resume_person_name(data)}.txt"`; `url = await asyncio.wait_for(upload_text_resume(data["resume_text"], fname), timeout=20)`.
- В обеих ветках: `if url: data["resume_url"] = url`. Обе ветки в общем try/except `(asyncio.TimeoutError, Exception)` → log.error, resume_url остаётся None. Блок ДО `add_user(data)` (как сейчас).
Убедиться, что `os` импортирован в модуле (если нет — добавить import os).
SHEET_COLUMNS «Резюме (ссылка)» НЕ трогать.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import ast,re; s=open('handlers/registration.py',encoding='utf-8').read(); ast.parse(s); assert '_resume_person_name' in s and 'upload_text_resume' in s; print('ok')"</automated>
  </verify>
  <done>_resume_person_name определён; finalize вызывает upload_resume для файла и upload_text_resume для текста, оба под wait_for(timeout=20) и общим fail-soft try/except до add_user.</done>
</task>

<task type="auto">
  <name>Task 3: backfill обоих типов + .env.example + тесты</name>
  <files>scripts/backfill_resumes.py, .env.example, tests/test_nextcloud_wk6.py, tests/test_backfill_resumes.py</files>
  <action>
scripts/backfill_resumes.py:
- `select_pending_resumes(db)`: SELECT `telegram_id, resume_file_id, resume_text, full_name, username FROM users WHERE (resume_file_id IS NOT NULL AND resume_file_id != '' OR resume_text IS NOT NULL AND resume_text != '') AND (resume_url IS NULL OR resume_url = '')`. Обновить docstring.
- Guard (строка 82): заменить на `if not config.NEXTCLOUD_WEBDAV_URL or not config.NEXTCLOUD_PUBLIC_URL or not config.NEXTCLOUD_FOLDER_SHARE_TOKEN:` → print «Nextcloud не настроен (нужны WEBDAV_URL/PUBLIC_URL/FOLDER_SHARE_TOKEN) — выход»; return.
- Импортнуть `from services.nextcloud import upload_resume, upload_text_resume` и `from handlers.registration import _resume_person_name` (или продублировать локальный person-name; предпочесть импорт хелпера).
- dry-run: печатать тип и кого — для каждого кандидата определить тип (`"file"` если resume_file_id иначе `"text"`) → `print(f"WOULD process {telegram_id} ({typ})")`. Распаковка теперь 5 полей.
- Реальный loop: распаковать `(telegram_id, resume_file_id, resume_text, full_name, username)`. Собрать `data = {"full_name": full_name, "username": username, "telegram_id": telegram_id}` для `_resume_person_name(data)` → `name`. Если resume_file_id: ext через `bot.get_file` fallback (как сейчас) → `fname = f"{name}{ext}"` → `url = await upload_resume(bot, resume_file_id, fname)`. Иначе (resume_text): `url = await upload_text_resume(resume_text, f"{name}.txt")` (bot не нужен). success/failed учёт как сейчас, UPDATE resume_url.

.env.example: в NEXTCLOUD-блоке добавить `NEXTCLOUD_PUBLIC_URL=` (публичный адрес для ссылок, напр. https://91.223.28.229:8443) и `NEXTCLOUD_FOLDER_SHARE_TOKEN=` с пояснением: пофайловых шар больше НЕТ — ссылки в таблице это deep-links в ОДНУ ручную публичную шару папки resumes; пароль ставится на самой шаре папки (NEXTCLOUD_SHARE_PASSWORD код больше не шлёт). Как получить токен: создать публичную шару папки resumes → часть XXXX из ссылки /s/XXXX. Нюанс: по deep-ссылке Nextcloud спросит пароль папки 1 раз за сессию. Обновить/пометить комментарий у NEXTCLOUD_SHARE_PASSWORD, что код его больше не использует.

tests/test_nextcloud_wk6.py:
- test_upload_resume_disabled: выставить пустые NEXTCLOUD_WEBDAV_URL/PUBLIC_URL/FOLDER_SHARE_TOKEN → upload_resume(...) is None.
- Добавить тест `_file_link`: при `NEXTCLOUD_PUBLIC_URL="https://x:8443"` и `NEXTCLOUD_FOLDER_SHARE_TOKEN="TOK"` → `_file_link("ab_Иван.txt")` == `"https://x:8443/s/TOK/download?path=%2F&files=" + quote("ab_Иван.txt")`.
- Добавить тест: `upload_text_resume` при пустом guard → None (корутина, не бросает); прогнать через asyncio.run.
- Убрать проверки, завязанные на старый OCS-POST (если есть). failsoft-тест: enabled config теперь требует все три (WEBDAV+PUBLIC+TOKEN) заданными, чтобы дойти до bot.download.

tests/test_backfill_resumes.py:
- Обновить сид/распаковку под 5 колонок. Кейсы: (a) файл без url → PICKED; (b) файл+url → SKIPPED; (c) пусто → SKIPPED; (d) НОВЫЙ: resume_text без url → PICKED; (e) resume_text+url → SKIPPED. Проверить, что picked = {file-кейс, text-кейс}. Распаковка строки: 5 полей.
  </action>
  <verify>
    <automated>BOT_TOKEN=123:test ADMIN_IDS=[1] .venv/Scripts/python.exe -m pytest tests/test_nextcloud_wk6.py tests/test_backfill_resumes.py -q</automated>
  </verify>
  <done>Оба тест-файла зелёные; select_pending_resumes берёт файл- И текст-резюме без url; backfill dry-run печатает тип; .env.example содержит NEXTCLOUD_PUBLIC_URL и NEXTCLOUD_FOLDER_SHARE_TOKEN.</done>
</task>

</tasks>

<verification>
- `grep -rn "ocs/v2\|api/v1/shares" services/` → пусто (OCS полностью удалён).
- `.venv/Scripts/python.exe -c "from services.nextcloud import upload_resume, upload_text_resume, _file_link, _put_bytes"` → импортируется.
- Полный прогон: `BOT_TOKEN=123:test ADMIN_IDS=[1] .venv/Scripts/python.exe -m pytest tests/test_nextcloud_wk6.py tests/test_backfill_resumes.py -q`.
</verification>

<success_criteria>
- Бот не создаёт пофайловых шар; резюме (файл и текст) заливаются WebDAV PUT, ссылка в таблице — deep-link в ручную шару-папку.
- Имя файла = ФИО + username/telegram_id, кириллица сохраняется.
- Регистрация fail-soft при выключенном/недоступном Nextcloud.
- backfill обрабатывает оба типа резюме без url; старые resume_url не перезаписываются.
- Тесты зелёные.
</success_criteria>

<output>
После завершения создать `.planning/quick/260711-16c-resume-links-via-one-folder-share-deep-l/260711-16c-SUMMARY.md`.
В SUMMARY отметить: старые resume_url (пофайловые /s/ из сегодняшнего теста) backfill НЕ перезаписывает — при желании их можно вручную очистить (SET resume_url='') и перезалить. Формат deep-ссылки вынесен в `_file_link` для лёгкой смены download→просмотр.
</output>
