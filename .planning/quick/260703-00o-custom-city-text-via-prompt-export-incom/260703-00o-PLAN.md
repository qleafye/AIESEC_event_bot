---
quick_id: 260703-00o
slug: custom-city-text-via-prompt-export-incom
date: 2026-07-03
status: complete
---

# Quick 260703-00o — Custom city text + export incomplete registrations

## Tasks
1. `handlers/registration.py`: on «Другое», re-prompt for free text (stay in state) for city,
   source, local_committee, position, university, select_input.
2. Incomplete-reg export: `db.get_incomplete_rows()`; `sheets.sync_named_worksheet()` (create/refresh
   a named tab); admin button + `admin_export_incomplete` handler → «Незавершённые» tab.

## Verify
- Import smoke; `get_incomplete_rows` 3-col; `pytest -q -p no:asyncio` green.
