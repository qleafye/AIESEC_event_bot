---
quick_id: 260702-x01
slug: conference-bed-sharing-question-with-dyn
date: 2026-07-02
status: complete
---

# Quick 260702-x01 — Conference bed-sharing question (dynamic follow-up)

## Goal
Add a CONF question: «готов делить двуспальную кровать?» (Да/Нет). If Да → ask «с кем»; if
Нет → skip to next. Off by default, part of the conference preset, exported to Sheet + CSV.

## Tasks
1. States + flow: `Registration.bed_sharing`/`bed_partner`; REG_FLOW/DEFAULTS/LABELS/CATEGORIES;
   conf preset; conditional gate in `_get_enabled_steps`; `_ask_step` branches + handlers;
   summary lines.
2. Persistence + export: `db.py` columns (`_ensure_column`), `add_user` INSERT/params,
   `CSV_HEADER_LABELS`; `SHEET_COLUMNS` (inserted mid-list to keep last header stable).

## Verify
- add_user/get_user round-trip; gate on Да/Нет; preset/sheet include cols; category coverage;
  `pytest -q -p no:asyncio` green.
