---
quick_id: 260710-nkk
slug: sheet-cols-resume-status
date: 2026-07-10
status: complete
---

# Quick Task 260710-nkk — SUMMARY

Правки таблицы/заявок по фидбеку Тани (YL'26). Пункт 3 (резюме в облако) отложен
по решению Андрея (зальёт на своё облако сам). Пункт 6 уже был сделан гейтингом.

## Что сделано

- **п.1 Порядок столбцов** — `SHEET_COLUMNS` пересобран: системные колонки
  (ID Telegram / Username / Дата регистрации / **Статус** / ФИО / Детали), затем
  вопросы ровно в порядке `REG_FLOW` (порядок показа в анкете).
- **п.2 Резюме обязательно** — шаг резюме без «Пропустить» и без reply-клавы;
  ответ = файл PDF/DOCX или текст, иначе переспрос.
- **п.4 Текст-резюме** — карточка заявки админа показывает текст резюме (обрезка
  300 симв.); кнопка «📎 Резюме» доступна и при файле, и при тексте; в таблице
  новая колонка «Резюме (текст)».
- **п.5 Статус** — колонка «Статус» (Новая/Одобрена/Отклонена); автосинк в
  таблицу при аппрув/реджект (по telegram_id) и при «одобрить все» (батч);
  data-validation выпадашка + условное форматирование (жёлтый/зелёный/красный)
  на исходной вкладке. Новая admin-кнопка «♻️ Пересобрать таблицу».

## Файлы

- `handlers/registration.py` — SHEET_COLUMNS reorder, `_status_label`,
  `STATUS_LABELS`, статус до append, resume required.
- `services/sheets.py` — `update_status_in_sheet`, `bulk_update_status_in_sheet`,
  `rebuild_main_sheet`, `_apply_status_formatting_sync`, `STATUS_DROPDOWN`.
- `handlers/admin.py` — карточка заявки (текст резюме + кнопка), автосинк на
  approve/reject/mass, кнопка + хендлер «Пересобрать таблицу».
- `tests/` — порядок колонок, Статус/Резюме(текст), текст-резюме в карточке.

## Commits

- `89ad1b5` feat(sheets): автосинк статуса + пересборка + выпадашка/цвета
- `1600cae` feat(reg): столбцы в порядке анкеты + Статус/Резюме(текст) + резюме обязательно
- `75d1083` feat(admin): текст-резюме в заявке + автосинк статуса + кнопка Пересобрать
- `3c6c347` test: покрытие новых колонок и карточки

## Проверка

- `python -m pytest tests/` → **113 passed** (env BOT_TOKEN + ADMIN_IDS='[1]').
- Импорт всех изменённых модулей — ok.

## Ручные шаги / заметки для Андрея

1. Убедиться, что тумблер «📄 Резюме» включён в админке (иначе шаг не спросят,
   `reg_q_resume` дефолт **off**).
2. После деплоя один раз нажать **«♻️ Пересобрать таблицу»** — иначе старые строки
   на листе разъедутся относительно новой шапки (шапка правится in place, данные —
   нет). Пересборка перезаписывает лист целиком (осторожно с ручными правками).
3. Выпадашка/цвета живут на исходной вкладке; через IMPORTRANGE-миррор они НЕ
   передаются — подключить тул Тани к самой таблице (как и планировалось).
4. Повторная пересборка может накопить дубли conditional-format правил (безвредно
   визуально) — при желании чистить руками.
5. Отложено: резюме-файл → облако + ссылка в таблицу (п.3).
