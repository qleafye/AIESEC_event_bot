---
quick_id: 260703-06r
slug: handle-on-department-and-aiesec-role-plu
date: 2026-07-03
status: complete
---

# Quick 260703-06r — Summary

Follow-ups: close the remaining «Другое» gaps + auto-sync incomplete registrations.

## «Другое» on department & aiesec_role (`handlers/registration.py`)
Previous task fixed the custom-handler steps but missed the two `_store_choice`-routed steps that
also have a «Другое» button (department, aiesec_role) — tapping it stored the literal «Другое».
Added the guard inside `_store_choice` itself, so every current and future choice step routed
through it (department, aiesec_role, arrival/housing/transport/volunteer/… harmless no-op) now
re-prompts for free text on «Другое». Full «Другое» coverage.

## Auto-sync «Незавершённые» tab (`services/scheduler.py`)
Added `sync_incomplete_sheet_job()` (no-args, picklable interval-job target) that full-refreshes the
«Незавершённые» sheet tab from `get_incomplete_rows()`. Registered as an APScheduler interval job,
`incomplete_sync_hours` setting (default **2h**). Managers no longer need to tap the admin button;
the manual «📝 Незавершённые → таблица» button stays for on-demand refresh.

## Verification
- `sync_incomplete_sheet_job` is picklable (required by SQLAlchemyJobStore); imports resolve.
- `_store_choice` contains the «Другое» guard.
- `python -m pytest tests/ -q -p no:asyncio` → **109 passed**
