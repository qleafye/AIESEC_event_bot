---
quick_id: 260703-06r
slug: handle-on-department-and-aiesec-role-plu
date: 2026-07-03
status: complete
---

# Quick 260703-06r — «Другое» on remaining steps + auto-sync incomplete

## Tasks
1. `handlers/registration.py`: add the «Другое» → free-text guard inside `_store_choice`
   (covers department, aiesec_role, and any future choice step routed through it).
2. `services/scheduler.py`: `sync_incomplete_sheet_job()` interval job (default every 2h,
   setting `incomplete_sync_hours`) → refresh the «Незавершённые» tab automatically.

## Verify
- Job picklable; imports OK; `_store_choice` has guard; `pytest -q -p no:asyncio` green.
