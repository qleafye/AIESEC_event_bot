---
phase: quick-260724-dw1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - docker-compose.yml
  - handlers/registration.py
  - services/sheets.py
  - main.py
  - tests/test_registration_phase1.py
  - tests/test_sheets_admin_alert.py
autonomous: true
requirements:
  - P0-aiogram-pin
  - P0-resume-size-guard
  - P0-sheets-fail-alert
  - P0-compose-version
user_setup: []

must_haves:
  truths:
    - "A fresh pip install can never pull aiogram 4.x (upper bound present)."
    - "An oversized resume document is rejected with a clear RU message and the FSM stays on the resume step (no download, no Nextcloud upload)."
    - "A resume within the size limit still advances registration exactly as before."
    - "When Google Sheets append exhausts all retries, admins receive a Telegram alert."
    - "The admin alert never raises or blocks the append path (fail-soft)."
    - "docker-compose.yml has no invalid version key and parses under compose v2."
  artifacts:
    - path: "requirements.txt"
      provides: "aiogram upper bound"
      contains: "aiogram>=3.0.0,<4.0.0"
    - path: "handlers/registration.py"
      provides: "_resume_too_large guard in process_resume"
      contains: "_resume_too_large"
    - path: "services/sheets.py"
      provides: "admin-alert on exhausted retries for both append paths"
      contains: "_alert_admins_sheet_failure"
    - path: "tests/test_registration_phase1.py"
      provides: "resume size-guard regression tests"
      contains: "_resume_too_large"
    - path: "tests/test_sheets_admin_alert.py"
      provides: "exhausted-retry alert fires + fail-soft tests"
      contains: "append_to_sheet"
  key_links:
    - from: "handlers/registration.py::process_resume"
      to: "_resume_too_large(message.document.file_size)"
      via: "size guard before _advance"
      pattern: "_resume_too_large\\("
    - from: "services/sheets.py::append_to_sheet"
      to: "_alert_admins_sheet_failure"
      via: "call after the retry loop's logger.error"
      pattern: "_alert_admins_sheet_failure"
    - from: "services/sheets.py::append_to_named_sheet"
      to: "_alert_admins_sheet_failure"
      via: "call after the retry loop's logger.error"
      pattern: "_alert_admins_sheet_failure"
    - from: "main.py"
      to: "sheets.set_alert_bot(bot)"
      via: "startup wiring alongside init_scheduler"
      pattern: "set_alert_bot"
---

<objective>
Ship the four low-risk P0 hardening fixes from the v1.0 milestone audit / CONCERNS.md:
1. Pin an aiogram upper bound so a fresh install can never pull a breaking 4.x.
2. Add a resume file-size guard mirroring the receipt guard already in handlers/payment.py.
3. Send an admin alert when a Google Sheets append exhausts its retry window (mirror the allowlist_refresh_job pattern).
4. Remove the invalid `version: 'version'` key from docker-compose.yml.

Purpose: Close known reliability/observability/reproducibility gaps flagged as P0 without touching the fixed core stack, Nextcloud TLS defaults, or any existing behavior beyond the four fixes.
Output: Format-preserving edits to requirements.txt, docker-compose.yml, handlers/registration.py, services/sheets.py, main.py, plus regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONCERNS.md
@CLAUDE.md

<interfaces>
<!-- Receipt-guard pattern to mirror for FIX 2 — handlers/payment.py -->
```python
_RECEIPT_MAX_BYTES = 10 * 1024 * 1024

def _receipt_too_large(file_size) -> bool:
    return bool(file_size) and file_size > _RECEIPT_MAX_BYTES

# usage inside process_receipt_document:
if _receipt_too_large(message.document.file_size):
    await message.answer("❌ Файл слишком большой (максимум 10 МБ). Пришли чек меньшего размера.")
    return
```

<!-- Current resume handler to guard — handlers/registration.py:1604 -->
```python
@router.message(Registration.resume, F.document)
async def process_resume(message: types.Message, state: FSMContext, bot: Bot):
    if not _is_allowed_resume(message.document.file_name):
        await message.answer("Принимаются только PDF или DOCX. Прикрепи файл ещё раз.")
        return
    await state.update_data(
        resume_file_id=message.document.file_id,
        resume_file_name=message.document.file_name,
    )
    await _advance("resume", message, state, bot)
```

<!-- Admin-alert pattern to mirror for FIX 3 — services/scheduler.py::allowlist_refresh_job -->
```python
for admin_id in config.ADMIN_IDS:
    try:
        await _bot.send_message(admin_id, "⚠️ ...")
    except Exception as e:
        logger.error(f"... alert to {admin_id} failed: {e}")
```

<!-- sheets.py retry-loop tails to hook (both end with a logger.error then fall through) -->
```python
# append_to_sheet (services/sheets.py:115-133)
    logger.error(f"Failed to append to Google Sheet after {MAX_RETRIES} attempts for telegram_id={(data[0] if data else '?')!r}")

# append_to_named_sheet (services/sheets.py:419-442)
    logger.error(f"Failed to append to tab {tab_name!r} after {MAX_RETRIES} attempts for telegram_id={(data[0] if data else '?')!r}")
```

<!-- main.py startup wiring point — after init_scheduler(bot), ~line 129 -->
<!-- sheets.py has NO bot reference today; scheduler gets one via init_scheduler(bot). -->
<!-- config.ADMIN_IDS: List[int] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin aiogram upper bound + drop invalid compose version key</name>
  <files>requirements.txt, docker-compose.yml</files>
  <action>
FIX 1 (P0 aiogram pin, CONCERNS.md:8-11 / :121-124): In requirements.txt change the first line `aiogram>=3.0.0` to `aiogram>=3.0.0,<4.0.0`. Touch NO other line — do not add, remove, or re-pin any other dependency, and do not add a lockfile.

FIX 4 (P0 compose version, CONCERNS.md:32-35): In docker-compose.yml delete the entire first line `version: 'version'`. Compose v2 needs no top-level version key. Leave `services:` as the new first line and everything else byte-identical (do not reformat indentation or reorder keys).

Both edits are format-preserving; no behavioral code changes.
  </action>
  <verify>
    <automated>grep -q "aiogram>=3.0.0,<4.0.0" requirements.txt && ! grep -q "version: 'version'" docker-compose.yml && python -c "import yaml,sys; d=yaml.safe_load(open('docker-compose.yml',encoding='utf-8')); sys.exit(0 if 'version' not in d and 'bot' in d.get('services',{}) else 1)"</automated>
  </verify>
  <done>requirements.txt bounds aiogram to `<4.0.0`; docker-compose.yml has no `version` key and still declares the `bot` service; no other lines changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resume file-size guard in process_resume + regression tests</name>
  <files>handlers/registration.py, tests/test_registration_phase1.py</files>
  <behavior>
    - _resume_too_large(0) is False (Telegram sometimes reports 0/None size — do not reject).
    - _resume_too_large(None) is False.
    - _resume_too_large(_RESUME_MAX_BYTES) is False (boundary: exactly at limit is allowed).
    - _resume_too_large(_RESUME_MAX_BYTES + 1) is True.
    - _resume_too_large(500) is False (a normal small resume passes).
  </behavior>
  <action>
FIX 2 (P0 resume size guard, CONCERNS.md:49-53). In handlers/registration.py, near the existing `_is_allowed_resume` helper (line ~1192), add a local size constant and pure predicate mirroring handlers/payment.py's `_RECEIPT_MAX_BYTES` / `_receipt_too_large` (replicate locally — do NOT import from handlers.payment; a module-level cross-import risks a circular import and the values are independently tunable). Add a finding-ID comment referencing the audit P0 resume-size finding, in the terse style of the surrounding code:
- `_RESUME_MAX_BYTES = 10 * 1024 * 1024` (10 MB — same generous cap as receipts; resumes are downloaded and pushed to Nextcloud, so an unbounded file is worse here than for a file_id-only receipt).
- `def _resume_too_large(file_size) -> bool: return bool(file_size) and file_size > _RESUME_MAX_BYTES` (the `bool(file_size)` guard makes a 0/None size pass, matching `_receipt_too_large`).

Then in `process_resume` (the `@router.message(Registration.resume, F.document)` handler, line ~1604), insert the size check AFTER the existing `_is_allowed_resume` extension check and BEFORE `state.update_data(...)` / `_advance(...)` — i.e. before anything stores the file_id or advances the FSM (the Nextcloud upload happens later in finalization, so rejecting here prevents both the store and the later download/upload). On oversize, answer a clear RU message and `return` without advancing:
`"❌ Файл слишком большой (максимум 10 МБ). Прикрепи резюме меньшего размера."`
Do not add a size guard to `process_resume_text` (text has no file_size) and do not touch `process_resume_invalid`.

Tests: append to tests/test_registration_phase1.py (which already imports pure resume helpers). Import `_resume_too_large` and `_RESUME_MAX_BYTES` from handlers.registration and add plain `def test_*` cases covering every bullet in <behavior> (None, 0, at-limit, over-limit, small). Follow the file's existing no-async pure-helper convention (no asyncio.run needed — these are sync helpers).
  </action>
  <verify>
    <automated>python -m pytest tests/test_registration_phase1.py -q</automated>
  </verify>
  <done>`_resume_too_large` exists and is used in process_resume before the FSM advance; an oversize document is rejected with the RU message and never stored/advanced; all new size-guard tests pass; the text/invalid resume paths are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Admin alert on exhausted Sheets retries (both append paths) + tests</name>
  <files>services/sheets.py, main.py, tests/test_sheets_admin_alert.py</files>
  <behavior>
    - After append_to_sheet exhausts all MAX_RETRIES (sync call always raises), an admin alert send is attempted once per id in config.ADMIN_IDS.
    - Same for append_to_named_sheet on exhausted retries.
    - If the alert bot is unset (set_alert_bot never called), the append still completes fail-soft with no exception.
    - If a per-admin send raises, it is swallowed (other admins still attempted; the append never raises).
    - On a successful append (no exception), no alert is sent.
  </behavior>
  <action>
FIX 3 (P0 Sheets silent failure, CONCERNS.md:55-59). In services/sheets.py add a bot-injection hook and a fail-soft admin-alert helper mirroring services/scheduler.py::allowlist_refresh_job, then call it after BOTH retry loops exhaust.

1. Module-level: `_alert_bot = None` and `def set_alert_bot(bot): global _alert_bot; _alert_bot = bot`. sheets.py has no bot today (scheduler holds its own via init_scheduler); this is the parallel injection point.
2. Add `async def _alert_admins_sheet_failure(context: str) -> None:` — fully fail-soft (wrap the whole body in try/except so it can NEVER raise or block the caller). If `_alert_bot is None`, return early (log a warning once). Otherwise iterate `config.ADMIN_IDS` and, per id, `await _alert_bot.send_message(admin_id, text)` inside its own try/except that logs on failure (mirror allowlist_refresh_job exactly). The RU alert text should name the failing tab/context, e.g.:
`f"⚠️ Google Sheets: не удалось записать строку после {MAX_RETRIES} попыток ({context}). SQLite не затронут — синхронизация таблицы отстаёт. Проверьте квоту/доступ Google API."`
3. In `append_to_sheet`: KEEP the existing `logger.error(...)` at the end, then add `await _alert_admins_sheet_failure(f"основная вкладка, telegram_id={(data[0] if data else '?')!r}")`.
4. In `append_to_named_sheet`: KEEP the existing `logger.error(...)`, then add `await _alert_admins_sheet_failure(f"вкладка {tab_name!r}, telegram_id={(data[0] if data else '?')!r}")`.
Add a finding-ID comment (audit P0 Sheets-alert) at the helper, in the codebase's terse style. Do not change MAX_RETRIES/RETRY_DELAYS, the backoff, the cache reset, or the fail-soft posture of the loops.

5. In main.py, wire the bot into sheets at startup: after `await init_scheduler(bot)` (~line 129), call `sheets_service.set_alert_bot(bot)` (main.py already does `import services.sheets as sheets_service`). Place it near the scheduler/allowlist warm-up so the alert bot is live before polling starts.

Tests: create tests/test_sheets_admin_alert.py following tests/test_sheets_phase5.py conventions (plain `def test_*`, `asyncio.run(go())`, monkeypatch, patch `sheets.asyncio.sleep` to a no-op async fn so the retry backoff is instant). For each of both append functions:
- Set `config.GOOGLE_SHEET_ID`/`GOOGLE_CREDENTIALS_FILE` to fake non-empty values so the retry loop actually runs (mirror test_append_to_named_sheet_swallows_raising_sync_call).
- monkeypatch the corresponding `_append_to_sheet_sync` / `_append_to_named_sheet_sync` to always raise.
- monkeypatch `config.ADMIN_IDS` to a known list (e.g. [111, 222]).
- Inject a fake bot recording `send_message` calls via `sheets.set_alert_bot(fake_bot)` where `send_message` is an async method appending (chat_id, text) to a list.
- Assert the append coroutine returns without raising AND the fake bot received one send per admin id.
Add a test that with `set_alert_bot(None)` (bot unset) the exhausted append still returns without raising. Add a test that a fake bot whose `send_message` raises does not propagate (append still returns, both admins attempted). Reset the injected bot at test end (set_alert_bot(None)) to avoid cross-test leakage.
  </action>
  <verify>
    <automated>python -m pytest tests/test_sheets_admin_alert.py -q</automated>
  </verify>
  <done>Both append functions call `_alert_admins_sheet_failure` after their retry loops exhaust; the helper is fully fail-soft (no bot → no crash, per-send errors swallowed); main.py injects the bot via set_alert_bot; existing logger.error lines retained; all new tests pass and existing sheets tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram user → bot (resume upload) | Untrusted document of arbitrary size crosses into download + Nextcloud upload |
| Google Sheets API → bot | External dependency whose failure is currently silent past the retry window |
| Package registry → build | Unpinned aiogram upper bound lets a future breaking major enter a fresh build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-dw1-01 | Denial of Service | process_resume (registration.py:1604) | mitigate | Reject oversized documents (`_resume_too_large`, 10 MB cap) before any download/Nextcloud upload |
| T-dw1-02 | Repudiation/observability | append_to_sheet / append_to_named_sheet | mitigate | Admin alert on exhausted retries so a prolonged Sheets outage/quota exhaustion cannot go unnoticed |
| T-dw1-03 | Tampering (supply chain) | requirements.txt aiogram pin | mitigate | Add `<4.0.0` upper bound so a rebuild cannot silently pull a breaking major |
| T-dw1-04 | Info disclosure (Nextcloud TLS off) | config.NEXTCLOUD_VERIFY_TLS | accept | Out of scope per plan constraint; tracked separately (self-signed-cert deployment) |
</threat_model>

<verification>
- `grep aiogram requirements.txt` shows `aiogram>=3.0.0,<4.0.0`; no other dep line changed.
- `docker-compose.yml` parses as valid compose v2 (no `version` key, `bot` service intact).
- `python -m pytest tests/test_registration_phase1.py tests/test_sheets_admin_alert.py -q` passes.
- Full suite still green: `python -m pytest -q` (no regressions in existing sheets/registration tests).
- Manual read: process_resume rejects oversize before `_advance`; both sheets append loops call `_alert_admins_sheet_failure` after their retained `logger.error`.
</verification>

<success_criteria>
- All four P0 audit findings closed: aiogram pinned `<4.0.0`, resume size guard live, Sheets exhausted-retry admin alert live, invalid compose `version` key removed.
- No behavior changed outside the four fixes; Nextcloud TLS default (`NEXTCLOUD_VERIFY_TLS=False`) untouched.
- New regression tests cover the resume guard and the Sheets alert path (fires on exhaustion, fail-soft when bot unset / send raises).
- Edits are format-preserving and follow the codebase's fail-soft + finding-ID-comment conventions.
</success_criteria>

<output>
After completion, create `.planning/quick/260724-dw1-p0-hardening-pack/260724-dw1-SUMMARY.md`
</output>
