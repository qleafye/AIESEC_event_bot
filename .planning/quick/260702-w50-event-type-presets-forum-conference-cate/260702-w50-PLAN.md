---
quick_id: 260702-w50
slug: event-type-presets-forum-conference-cate
date: 2026-07-02
status: complete
---

# Quick 260702-w50 — Event-type presets + categorized question toggles

## Goal
Let the DXP manager pick an event type (Forum/Conference) that bulk-applies the right
question set + payment flag, and group the question toggles by type so the 40 questions
are navigable. Must not change live bot behavior until a preset is tapped.

## Tasks
1. `handlers/registration.py`: add `REG_PRESETS` (forum/conf → reg_q_* on-list +
   payment_enabled) and `REG_CATEGORIES` (disjoint display buckets covering all flow keys).
   Data only — no engine/defaults change.
2. `handlers/admin.py`: `🎛 Тип события` settings button → preset menu → confirm card →
   apply (`_apply_event_preset`). Regroup question view by category with header rows.

## Verify
- Category buckets disjoint and cover every REG_FLOW key once; preset keys valid.
- `python -m pytest tests/ -q -p no:asyncio` green.

## Distribution (confirmed with user)
- FORUM: age, vk, source, education, university, course, study_field, work, work_sphere,
  skills, expectations. Payment OFF.
- CONF: age, vk, phone, lc, work, department, aiesec_role, english, allergies, food,
  arrival, transport, payment_date, cc_shop, exp_organizers, volunteer. Payment ON.
- Extra bucket holds the rest, toggled individually on top of a preset.
