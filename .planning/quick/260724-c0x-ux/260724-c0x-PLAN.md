---
phase: quick-260724-c0x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - handlers/admin.py
  - tests/test_settings_groups_c0x.py
autonomous: true
requirements: [UX-c0x-settings-subscreens]
must_haves:
  truths:
    - "Экран «⚙️ Настройки форума» больше не дампит ~40 полей одним плоским сообщением"
    - "Поля настроек сгруппированы в под-экраны по 5 группам (Событие/Медиа, Регистрация, Оплата, Party, Согласия) + safety-net «Прочие»"
    - "Длинные значения не показываются инлайн — только флаг «✏️ задано» / «— не задано» / «по умолчанию», полное значение открывается по тапу через существующий settings_edit"
    - "Незаданные поля внутри под-экрана свёрнуты под заголовок «── не настроено ──», не шумят в основном виде"
    - "Существующие callback'и редактирования (settings_edit:, settings_photo:, settings_file:) работают без изменений"
  artifacts:
    - path: "handlers/admin.py"
      provides: "SETTINGS_GROUPS grouping constant + per-group sub-screen render/keyboard + settings_group callback"
    - path: "tests/test_settings_groups_c0x.py"
      provides: "Coverage: коллекция группировки, отсутствие инлайн-дампа, флаги задано/не-задано, целостность callback'ов"
  key_links:
    - from: "build_settings_keyboard"
      to: "settings_group:{token}"
      via: "per-group nav buttons replace per-field buttons"
      pattern: "settings_group:"
    - from: "build_settings_group_keyboard"
      to: "settings_edit:{key}"
      via: "existing edit callback reused unchanged for each field in group"
      pattern: "settings_edit:"
---

<objective>
Быстрый low-risk рефактор ТОЛЬКО рендера и навигации экрана «⚙️ Настройки форума» в `handlers/admin.py`. Сейчас `render_settings_text()` (строки ~405-485) и `build_settings_keyboard()` (строки ~488-561) вываливают ~32 поля `SETTINGS_FIELDS` + фото/файлы одним плоским сообщением с инлайн-обрезкой значений посреди слова и ~15 повторами «не указано». Разбиваем на под-экраны по группам, переиспользуя тот же приём, что уже применён к вопросам регистрации (`REG_CATEGORIES` → `_categorized_question_keys()` → `admin_reg_questions` под-экран, строки ~2018-2086).

Purpose: менеджеру DXP должно быть читаемо — сейчас, со слов пользователя, «выглядит пиздец, даже как разрабу сложно что-то разглядеть».
Output: под-экраны настроек по группам + флаги задано/не-задано вместо инлайн-дампа значений.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<constraints>
СТРОГИЕ границы этой quick-задачи (из brief):
- ТОЛЬКО рендер + навигация существующего экрана настроек. Только `handlers/admin.py` (+ новый тест-файл).
- НЕ вводить SETTINGS_SCHEMA-реестр (key→{type,group,label,...}). Разрешена ТОЛЬКО группировка вида группа→[keys] по образцу `REG_CATEGORIES` — это НЕ per-key реестр метаданных.
- НЕ менять сами настройки, их семантику, значения, БД. Только КАК они рендерятся/навигируются.
- НЕ ломать существующие callback'и: `settings_edit:{key}`, `settings_photo:{prefix}`, `settings_file:{prefix}`, `settings_cancel`, `settings_back`, а также все `settings_toggle_*` / `toggle_*` — их обработчики и текст-кнопок остаются байт-идентичны.
- Если для req №2 (сворачивание незаданных) потребуется что-то дороже одной noop-header-строки — оставить только флаг «— не задано» без отдельной секции и пометить в SUMMARY.
</constraints>

<interfaces>
<!-- Точные существующие структуры, против которых планируем. Executor НЕ должен исследовать код заново. -->

handlers/admin.py:341 — плоский список полей (token, label, prompt):
```
SETTINGS_FIELDS = [ ("event_date","🗓 Дата",...), ("event_time",...), ... ]
```
Полный список ключей (в порядке файла): event_date, event_time, event_place_name,
event_place_address, contact_person, contact_vk, contact_tg, start_text, event_name,
source_options, reg_complete_text, approve_text, reject_text, consent_button_text,
pending_reminder_interval, event_type, consent_list, payment_options, payment_requisites,
payment_requisites_by_lc, payment_deadline, payment_reminder_text, payment_overdue_text,
penalty_schedule, city_options, study_field_options, goal_options, formats_options,
university_options, party_closed_text, party_sheet_tab.

handlers/admin.py:388 — дефолты отображения для незаданных text-полей:
```
_SETTINGS_DISPLAY_DEFAULTS = { "party_closed_text": "...", "party_sheet_tab": "Party" }
```

handlers/admin.py:393 / 400 — медиа:
```
PHOTO_FIELDS = [("program",...),("speakers",...),("start",...),("venue",...)]   # settings_photo:{prefix}, ключ {prefix}_photo_file_id
FILE_FIELDS  = [("reg_bonus",...)]                                              # settings_file:{prefix}, ключи {prefix}_photo_file_id / {prefix}_doc_file_id
```

handlers/admin.py:457-468 — БЛОК, который надо убрать (инлайн-дамп значений с обрезкой до 60 символов):
```
for key, label, _ in SETTINGS_FIELDS:
    value = await get_setting(key)
    ...
    if len(value) > 60: status = ...[:60] + "…"
    lines.append(f"{label}: {status}")
```
handlers/admin.py:471-481 — инлайн-статусы PHOTO_FIELDS/FILE_FIELDS (тоже перенести в группы).

handlers/admin.py:554-559 — БЛОК кнопок, который надо заменить на per-group nav:
```
for key, label, _ in SETTINGS_FIELDS: buttons.append([... callback_data=f"settings_edit:{key}"])
for prefix, label, _ in PHOTO_FIELDS: buttons.append([... callback_data=f"settings_photo:{prefix}"])
for prefix, label, _ in FILE_FIELDS:  buttons.append([... callback_data=f"settings_file:{prefix}"])
```

handlers/admin.py:564 — точка входа:
```
@router.callback_query(F.data == "admin_settings")
async def show_admin_settings(callback): text=render_settings_text(); edit_text(text, markup=build_settings_keyboard())
```

ОБРАЗЕЦ для переиспользования — под-экран вопросов регистрации:
- handlers/admin.py:2018 `_categorized_question_keys()` — группировка через REG_CATEGORIES + leftover в «📦 Прочие» (ничего не прячется).
- handlers/admin.py:2062-2064 — noop section header: `InlineKeyboardButton(text=f"── {header} ──", callback_data="reg_q_noop")`.
- handlers/admin.py:2078 `@router.callback_query(F.data == "admin_reg_questions")` — паттерн под-экрана: render + build_keyboard + Назад.

handlers/registration.py:364 — REG_CATEGORIES: список (header, [keys]). Тот же shape, что использовать для SETTINGS_GROUPS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ввести SETTINGS_GROUPS и превратить лендинг настроек в экран групп (убрать инлайн-дамп)</name>
  <files>handlers/admin.py</files>
  <action>
Добавить рядом с `SETTINGS_FIELDS` (после строки ~384) константу группировки по образцу `REG_CATEGORIES` — это НЕ per-key реестр, а группа→ключи:

`SETTINGS_GROUPS = [ (label, token, [keys...]), ... ]` со следующим распределением всех 32 ключей SETTINGS_FIELDS:
- («🎪 Событие/Медиа», "event"): event_date, event_time, event_place_name, event_place_address, contact_person, contact_vk, contact_tg, start_text, event_name, event_type
- («📝 Регистрация», "reg"): source_options, reg_complete_text, approve_text, reject_text, pending_reminder_interval, city_options, study_field_options, goal_options, formats_options, university_options
- («💳 Оплата», "pay"): payment_options, payment_requisites, payment_requisites_by_lc, payment_deadline, payment_reminder_text, payment_overdue_text, penalty_schedule
- («🎉 Party», "party"): party_closed_text, party_sheet_tab
- («📋 Согласия», "consent"): consent_button_text, consent_list

Добавить helper `_settings_group_keys(token) -> list[str]` и leftover-защиту по образцу `_categorized_question_keys()`: любой ключ SETTINGS_FIELDS, не попавший ни в одну группу, добавляется в trailing группу («📦 Прочие», "misc") — ничего не должно прятаться. Медиа (PHOTO_FIELDS/FILE_FIELDS) закрепить за группой "event" (обрабатывается в Task 2, не в SETTINGS_GROUPS).

В `render_settings_text()` (строка ~405): СОХРАНИТЬ верхний summary-блок (строки 406-455: тумблеры формы/модерации/оплаты/party + счётчики вопросов/меню) БЕЗ изменений. УДАЛИТЬ инлайн-дамп полей (строки 457-468) и инлайн-статусы медиа (строки 471-481). Вместо них добавить одну короткую строку-подсказку, напр. «✏️ Тексты и медиа — по кнопкам групп ниже». Финальную подсказку про «-» (строка 484) сохранить.

В `build_settings_keyboard()` (строка ~488): ВСЕ существующие toggle- и nav-кнопки (строки 533-553, callbacks settings_toggle_*, toggle_*, admin_*) оставить байт-идентичными. ЗАМЕНИТЬ три цикла per-field/photo/file (строки 554-559) на по одной nav-кнопке на группу из SETTINGS_GROUPS: `InlineKeyboardButton(text=group_label, callback_data=f"settings_group:{token}")`. Кнопку «← Назад» (`settings_back`, строка 560) сохранить.

Все toggle-обработчики уже вызывают render_settings_text()+build_settings_keyboard() после переключения — они автоматически останутся на лендинге, менять их НЕ нужно.
  </action>
  <verify>
    <automated>python -c "import asyncio; from handlers import admin as a; assert hasattr(a,'SETTINGS_GROUPS'); keys=[k for _,__,ks in a.SETTINGS_GROUPS for k in ks]; allk=[k for k,_,_ in a.SETTINGS_FIELDS]; missing=[k for k in allk if k not in keys and k not in a._settings_group_keys('misc')]; print('MISSING',missing); assert not missing"</automated>
  </verify>
  <done>SETTINGS_GROUPS покрывает все ключи SETTINGS_FIELDS (или leftover в «Прочие»); render_settings_text больше не содержит инлайн-значений полей; build_settings_keyboard эмитит settings_group:{token} вместо per-field settings_edit; все toggle/back callbacks не тронуты.</done>
</task>

<task type="auto">
  <name>Task 2: Под-экран группы — флаги «задано/не-задано» + сворачивание незаданных, callback settings_group</name>
  <files>handlers/admin.py, tests/test_settings_groups_c0x.py</files>
  <action>
Добавить (рядом с render_questions_text/build_questions_keyboard как образцом) функции под-экрана группы:

`render_settings_group_text(token) -> str`: заголовок «⚙️ Настройки → {group_label}». Для каждого ключа группы вычислить статус-флаг БЕЗ дампа значения:
- если `get_setting(key)` непусто → «✏️ задано»
- иначе если key в `_SETTINGS_DISPLAY_DEFAULTS` → «<i>по умолчанию</i>»
- иначе → «<i>— не задано</i>»
Строка вида `f"{label}: {flag}"`. Для группы "event" в конце добавить строки медиа: PHOTO_FIELDS → «✅ загружена / <i>— не задано</i>» (ключ `{prefix}_photo_file_id`), FILE_FIELDS → «✅ загружен / <i>— не задано</i>» (ключи `{prefix}_photo_file_id` или `{prefix}_doc_file_id`). НЕ выводить сами значения инлайн.

`build_settings_group_keyboard(token)`: по кнопке на поле, ПЕРЕИСПОЛЬЗУЯ существующие callback'и — `settings_edit:{key}` для полей SETTINGS_FIELDS (текст `f"✏️ {label}"`), а для группы "event" также `settings_photo:{prefix}` (текст `f"📷 {label}"`) и `settings_file:{prefix}` (текст `f"📎 {label}"`). Сворачивание незаданных (req №2, дёшево, по образцу reg_q_noop): сначала кнопки заданных полей, затем одна noop-строка `InlineKeyboardButton(text="── не настроено ──", callback_data="settings_group_noop")`, затем кнопки незаданных полей. Последняя строка — «← Назад к настройкам», callback_data=`admin_settings` (переиспользуем существующий лендинг-обработчик show_admin_settings — новый back-callback не нужен). Добавить no-op обработчик для `settings_group_noop` (просто `await callback.answer()`), по образцу reg_q_noop.

Добавить обработчик:
`@router.callback_query(F.data.startswith("settings_group:"))` → проверка admin (как в show_admin_settings), token = split(":",1)[1], `edit_text(render_settings_group_text(token), parse_mode="HTML", reply_markup=build_settings_group_keyboard(token))`, `callback.answer()`.

Тест-файл `tests/test_settings_groups_c0x.py` по образцу tests/test_admin_phase5.py (asyncio.run, FakeCallback/FakeMessage, _flat_callback_data, config.DB_PATH=tmp, config.ADMIN_IDS):
1. Покрытие: каждый ключ SETTINGS_FIELDS присутствует ровно в одной группе SETTINGS_GROUPS или в leftover «Прочие» (ничего не потеряно).
2. Лендинг больше не дампит значения: задать длинное значение (напр. set_setting('start_text', 'x'*200)), render_settings_text() НЕ содержит 'x'*61 и НЕ содержит '…'-обрезки поля.
3. Лендинг-клавиатура эмитит `settings_group:` и НЕ эмитит `settings_edit:` (per-field ушли в под-экраны); все прежние toggle-callbacks (напр. 'toggle_payment_enabled','settings_toggle_reg') всё ещё присутствуют.
4. Под-экран группы "pay": build_settings_group_keyboard('pay') содержит `settings_edit:payment_options`; render показывает «✏️ задано» для заданного поля и «— не задано» для пустого; клавиатура содержит back-callback `admin_settings`.
5. Целостность callback'ов: под-экран "event" содержит `settings_photo:` и `settings_file:`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_settings_groups_c0x.py -q</automated>
  </verify>
  <done>settings_group:{token} рендерит под-экран группы с флагами задано/не-задано (без инлайн-значений); незаданные поля свёрнуты под «── не настроено ──»; кнопки полей используют неизменённые settings_edit/settings_photo/settings_file; back ведёт на admin_settings; новый тест зелёный.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_settings_groups_c0x.py -q` — новый тест зелёный.
- `python -m pytest -q` — существующие admin-тесты (test_admin_phase5.py и др.) не сломаны (лендинг-toggle callbacks и settings_edit-поля живы).
- Grep-проверка отсутствия инлайн-дампа: в теле `render_settings_text` больше нет цикла `for key, label, _ in SETTINGS_FIELDS`.
</verification>

<success_criteria>
- Экран настроек: верхний summary тумблеров сохранён; ~40 текстовых полей больше не выводятся плоским списком с обрезкой.
- 5 групп-кнопок (+ «Прочие» при leftover) ведут в под-экраны; каждое поле открывается по тапу через существующий edit-экран (полное значение видно там, без обрезки посреди слова).
- «не указано» больше не повторяется ~15 раз на одном экране; незаданные поля свёрнуты под секцию в под-экране.
- Никакие настройки/семантика/БД/значения не тронуты; все существующие callback'и редактирования и тумблеров работают.
- Изменения только в handlers/admin.py (+ новый тест). Реестр SETTINGS_SCHEMA НЕ вводился.
</success_criteria>

<output>
After completion, create `.planning/quick/260724-c0x-ux/260724-c0x-SUMMARY.md`
</output>
