# 🎉 Party Flow — как работает и как настроить

Гайд для тестировщика/менеджера. В боте появился новый трек регистрации —
**«вечеринка» (party)**: отдельный поток для гостей, которые идут только на
вечеринку, а не на полное мероприятие.

## Суть

Один бот теперь обслуживает **три трека**:

- **Полный** — обычная регистрация делегата (как было).
- **Вечеринка с ночёвкой** (`party_overnight`).
- **Вечеринка без ночёвки** (`party_noovernight`).

У каждого трека — свой набор вопросов, своя модерация, свои тарифы и своя
вкладка в Google-таблице.

## Как гость попадает в party-трек

**Способ 1 — deep-link (основной).** Даёшь гостю ссылку:

- `t.me/<имя_бота>?start=party_over` — вечеринка **с ночёвкой**
- `t.me/<имя_бота>?start=party_noover` — вечеринка **без ночёвки**

Бот сразу ставит трек и ведёт по party-вопросам. Если гость в середине пришлёт
голый `/start` — трек сохраняется, не сбрасывается.

**Способ 2 — развилка в боте (опционально).** Если включить настройку
`party_fork_question`, гость без deep-link увидит экран «Выбери формат участия» и
сам выберет трек.

## Настройки (всё в админке, .env трогать НЕ надо)

Админка → Настройки:

| Настройка | Что делает | По умолчанию |
|-----------|-----------|--------------|
| **Трек вечеринки** (`party_enabled`) | Главный рубильник. Пока ВЫКЛ — party-ссылки показывают «закрыто» | ВЫКЛ |
| **Модерация вечеринки** (`party_approval`) | Ручная (админ одобряет) или Авто | Ручная |
| **Вопрос-развилка формата** (`party_fork_question`) | Показывать ли экран выбора трека без deep-link | ВЫКЛ |
| **Текст «вечеринка закрыта»** (`party_closed_text`) | Что видит гость по party-ссылке, когда трек ВЫКЛ | стандартный текст |
| **Вкладка Google-таблицы (Party)** (`party_sheet_tab`) | Имя вкладки для party-заявок | `Party` |

**Вопросы для party** — Админка → «📋 Вопросы регистрации» → переключатель
**«• Полный | Party»** сверху. В режиме Party у каждого вопроса три состояния:

- ➕ **Наследует** — берётся общая настройка
- ✅ **Вкл** — спрашивать в party
- ❌ **Выкл** — не спрашивать в party

Быстрый старт: кнопка **🎉 Party** в «Тип события» — разом настраивает типовой
party-набор (возраст, телефон, VK, город, аллергии, питание + вопросы про
ночёвку). Полный трек при этом НЕ меняется.

**Тарифы для party** — в `payment_options` можно ограничить тариф треком, добавив
третье поле: `Название | Цена | party_overnight`. Без третьего поля тариф виден
всем треками.

## Что происходит при завершении

- Заявка уходит в очередь модерации; на карточке видно **🎉 Трек: вечеринка с
  ночёвкой / без**.
- После одобрения гость получает party-текст одобрения (`approve_text__party`,
  если задан) и только party-тарифы.
- Строка пишется **только во вкладку Party**, на главную вкладку НЕ попадает.
  Обычные регистрации — наоборот, только на главную.

## Важно про поведение

- Пока **`party_enabled` = ВЫКЛ**, party-ссылка показывает текст «закрыто» +
  кнопку «Перейти к полной регистрации» (гость может уйти в обычный поток).
- Ночёвочные вопросы (проживание / кровать / сосед) задаются только треку
  **«с ночёвкой»**; «без ночёвки» их пропускает.
- Старые ссылки (`?start=12345` реферал, `?start=src_vk` источник) работают как
  раньше.

## Куда идут party-регистрации

Тот же Google-документ (`GOOGLE_SHEET_ID`), те же креды — просто **отдельная
вкладка** внутри той же таблицы (имя из `party_sheet_tab`, дефолт `Party`).
Вкладка создаётся автоматически, если её нет. **Ничего в .env добавлять не
нужно.**

## Чтобы проверить

Подробный чеклист из 10 шагов — в
`.planning/phases/05-participant-tracks-party-delegates/05-HUMAN-UAT.md`.
Начни с: включи «Трек вечеринки», применить пресет 🎉 Party, поставь ручную
модерацию — и пройди по ссылке `?start=party_over`.
