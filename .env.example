# ===========================
# AIESEC Event Bot — .env
# ===========================
# Скопируйте этот файл как .env и заполните значения.

# ---------------------------
# Telegram
# ---------------------------

# Токен бота от @BotFather
BOT_TOKEN=123456789:ABCDefGHIjkLLmnoPQRstuVWxyz

# Telegram ID администраторов (JSON-список)
# Узнать свой ID: отправьте /start боту @userinfobot
ADMIN_IDS=[12345678, 87654321]

# Прокси (опционально, если Telegram API недоступен напрямую)
# Форматы: http://user:pass@host:port или socks5://user:pass@host:port
# PROXY_URL=socks5://user:pass@1.2.3.4:1080

# ---------------------------
# Google Sheets (опционально)
# ---------------------------
# Если не заполнить — бот работает только с локальной SQLite БД.
# Подробная инструкция по настройке — в README.md

# ID таблицы из URL: https://docs.google.com/spreadsheets/d/ВОТ_ЭТОТ_ID/edit
GOOGLE_SHEET_ID=

# Имя вкладки для данных бота (напр. Реги бот). Пусто = первая вкладка по позиции.
# Кириллица ок. БЕЗ кавычек. Если вкладки нет — бот создаст её сам.
GOOGLE_SHEET_TAB=

# Путь к JSON-ключу сервисного аккаунта (относительно корня проекта)
GOOGLE_CREDENTIALS_FILE=google_credentials.json

# ---------------------------
# Nextcloud — резюме-файлы в облако (опционально)
# ---------------------------
# Если пусто — фича выключена: рега работает как раньше, резюме остаётся file_id в боте.
# Заполнить — резюме-файл делегата льётся в Nextcloud, в таблицу пишется пароль-ссылка.
#
# ВАЖНО (hairpin/NAT): BASE_URL и WEBDAV_URL — это адрес, по которому К СЕБЕ ходит БОТ.
# Если бот крутится на ТОМ ЖЕ сервере/в той же сети, что и Nextcloud — НЕ указывай
# внешний IP (91.223.28.229): роутер должен «завернуть» пакет сам на себя (NAT loopback),
# многие роутеры так не умеют → таймаут. Ставь ВНУТРЕННИЙ адрес: http(s)://127.0.0.1:8443
# или LAN-IP сервера. TLS self-signed + VERIFY_TLS=false → несовпадение хоста не важно.
# Публичность самой ССЫЛКИ на резюме это не ломает: OCS отдаёт share-URL по
# Nextcloud-настройке overwrite.cli.url — выстави её в публичный адрес/домен на сервере:
#   docker exec -u www-data <nextcloud_container> php occ config:system:set overwrite.cli.url --value="https://91.223.28.229:8443"
# Если бот на ДРУГОЙ сети (внешний VPS/твой ПК) — hairpin не грозит, ставь внешний IP.

# Базовый URL Nextcloud (для OCS Share API). Внутренний адрес если бот рядом (см. выше).
NEXTCLOUD_BASE_URL=
# WebDAV-эндпоинт пользователя бота (обычно BASE + /remote.php/dav/files/<user>/)
NEXTCLOUD_WEBDAV_URL=
# Логин пользователя-бота в Nextcloud (не admin!)
NEXTCLOUD_USER=aiesec-bot
# App-password этого пользователя (Settings → Security → Create new app password)
NEXTCLOUD_APP_PASS=
# Папка внутри пользователя, куда складывать резюме
NEXTCLOUD_FOLDER=resumes
# Общий пароль на ВСЕ share-ссылки резюме. В таблицу НЕ пишется — знает только МКТ-команда.
NEXTCLOUD_SHARE_PASSWORD=
# Проверять TLS-сертификат. self-signed → false. Купишь домен + Let's Encrypt → true.
NEXTCLOUD_VERIFY_TLS=false

# ---------------------------
# Логи
# ---------------------------
# Уровень файла logs/bot.log: DEBUG | INFO | WARNING | ERROR.
# docker logs всегда только WARNING+ (чтобы не срало). Полный лог — в файле.
LOG_LEVEL=INFO

# ---------------------------
# Список ВУЗов (опционально)
# ---------------------------
# JSON-список вариантов для полной формы регистрации.
# Если не указать — используется список по умолчанию (СПб ВУЗы).
# UNIVERSITIES=["ИТМО","Политех","ЛЭТИ","ГУАП","ВШЭ"]
